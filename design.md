# PM Copilot — 产品经理全流程 AI Agent · 设计文档（macOS 原生版）

> 版本：v0.10.2（Agent 成色三步：自主影响分析 + 阶段计划卡 + 主动简报轮）· 日期：2026-09-21 · 状态：待用户评审
>
> **v0.10.2 变更记录**：**「真 Agent 成色」三项能力落地（定位审计的后续——模型从「生成器」走向「决策者」）**——① **§6.11 扩至四工具**：新增 `query_impact`（模型自主查询产物依赖图，`ArtifactDependencyGraph` 纯函数经 AgentTool 通道开放给模型；变更提案 impacts 编制前先核实影响面，「影响分析 App 算好喂给模型」翻转为「模型调用工具自己算」——提问权交还模型、计算仍是确定性，§4 刻意设计 9 不变）。② **新增 §6.12 阶段计划卡（plan-act-reflect）**：②③④ 生成/修订产物的回合先输出 `artifact:plan` 块（mission + steps）再执行——计划对用户可见（PlanCardView 先于正文渲染，流式与落盘双路径同位）、产物与计划对应、自评审对照计划收尾；歧义反问 / 变更提案 / 快速通道 / 纯讨论轮明文豁免（防仪式化），① 澄清不注入。③ **新增 §6.13 Agent 主动简报轮**：💀 新登记 → 主动风险简报回合；封板被池拦截 → 主动变更池催办回合——Agent 不再只在被问到时说话（sendSystemTurn 合成指令通道、零新增系统行前缀、闸门不满足即放弃不排队）。配套：测试 +9（累计 628 全绿），PRD v1.3.5 同步。
>
> **v0.10.1 变更记录**：**新增 §6.11 内置 Function Calling（模型自主工具调用）**——LLMClient 接入 OpenAI 兼容 tools 协议，模型在对话中可自主发起 `load_skill` / `web_search` / `propose_competitive_analysis` 三类调用，结果回流继续生成；工具消息循环局部不持久化、执行权在系统侧、闸口不变量不破（详见 §6.11）。定位审计（2026-09-20）后的落地项，PRD §11 V2 路线首项提前实现。
>
> **v0.10 变更记录**：**四项新机制 + 十一处既有设计修订**——本次为 09-13 ~ 09-18 实际落地能力的文档回填，每条均有代码与测试证据。
> ① **新增 §6.6 快速通道**：用户明确要求跳步时直达下游产物——LLM 只发 `artifact:fast-forward` 请求块，**App 白名单裁决**后执行（① 可跳 prototype/prd；② 可跳 prd；③ 无下游）；跳过阶段写 `skipped.json` 标记、不生成产物、位置按闭环链重推导、可事后补做；目标产物照常走机器门与确认坞。配套 **PRD 前置确认分诊**（未决项 1-3 → `prd_preflight` 前置确认卡，选完 App 自动串链；≥4 → 照常回答 + 文字清单）+ **PRD 产物块硬禁令**（任何阶段禁止 LLM 直接输出 `artifact:prd`，落盘只走前置确认卡与快速通道两条路径）。
> ② **新增 §6.7 变更治理（候选变更池 + 草稿预演）**：`changes.jsonl`（提案登记行 + 处置回写行，append-only 折叠视图）；LLM 只分诊（`artifact:backtrack` 提案块：suggestion now/pool + category + target + **impacts 影响清单** + instruction + mode revise/redo），**用户裁决执行**；新功能/范围变化类默认建议进池；**封板前池内提案必须逐条毕业处置**（纳入后续 / 放弃 / 顺延），封板闸拦 pooled>0。**草稿预演**：会话标 `stage_draft`，产物镜像到 `05-artifacts/proposals/<sessionId>/`（主线零写入），「合并入主线」= 对拷 + 引擎推进 + 提案 adopted + 退草稿 + 决策留痕。
> ③ **新增 §6.8 多会话并行与后台完成**：busy 判据由全局单流闸改为**版本级 / 会话级**（`isSessionBusy` / `isVersionBusy` / `isProjectBusy`）；后台完成回复按 **origin 会话**钉回（`ReplyOrigin`），同版本触发闸口、跨版本落挂起行不推闸；存储层加进程级 `ioLock`（并发 appendLine 不丢行）与 `writeMeasured` 乐观校验（`expectedSHA` 失配抛 `ArtifactConflict`、不落盘）；**原型冲突分槽**（磁盘被外部改写 → 新原型落「原型-修订-MMdd-HHmm」修订槽、主槽不动，`setAsMasterPrototype` 选主）。
> ④ **`risks.jsonl` 由「💀 触发信号状态机」升级为四态台账 + 采纳落实闭环**（§5.2 / §6.1 / §6.2）：状态收敛为 **open（待处理）/ mitigating（已挂方案）/ resolved（已解除）/ accepted（已接受）**（旧五枚举仅历史解码兼容）；登记必带 `impact`（后果）与 `plan`（建议方案）；**结算点从事件驱动状态机改为阶段确认门**（跨门批量核 mitigating）；**采纳落实闭环**（`implementRiskAdoption`：代发受理 → AI 生成实施执行包落 `05-artifacts/risk-plans/` → 流完成后 mitigating + 带证据指针决策日志；中断保持 open 可重试；降级「仅记账」必须显式告知）；**排队逃生门**（「不等了，只记账」）；自评审对账 trace 迁入台账（`self-review.jsonl` 投影 + 「N 轮对账 · 累计修正 M 处」计数器，对话流只报新增）。
> ⑤ **FR9 右栏四 Tab → 三 Tab**（`InspectorPanel`）：「知识点」Tab 移除（语义检索与推荐收口左栏知识库整页），Tab 集合 = **文件 / 决策日志 / 风险**；「产物」Tab 升级为**分区台账**（`ArtifactsLedger`：01~07 编号目录产物按四类归档 + 筛选 chips + 右键「预览 / 添加到对话 / 设为主原型」）+ 工作空间文件档案树双区可拖分隔。
> ⑥ **统一停靠卡**（`StageDockCard`）替代「确认坞 + 作答坞」双卡叠挂：**作答段 → 分支确认段 → 确认段**同卡切换、段切换卡不重建；新增**问题卡向导**（`QuestionCardView`：多题逐答 radio/checkbox + 自定义 + 跳过 → 上一步回改 → 回顾页 → 提交拼装【问题卡作答】消息）。
> ⑦ **确认坞分档路径 + 弹坞纪律**（`ConfirmDock`）：选项按闸口分档（① 三推进含两个跳步档、② 两推进含跳 PRD、③ 两推进含「到此为止」）；**每版本每阶段只自动弹一次**，`confirm-silence.json` 版本级持久静默（跨启动跨会话），静默期修订轮由 `gateInviteSuffix` 融一句推进邀请。
> ⑧ **回退坞与重做双模**（`BacktrackDock`）：④ 阶段收起态虚线胶囊 → 展开选「重做原型 / 重做结构」（后果写副标题）→ `requestBacktrack` 回退 + 自动重生成；`artifact:backtrack` 协议 `mode` 区分 revise（带上一版作修订基底）/ redo（从零重做）。
> ⑨ **决策档案**（`DecisionArchive`，新增 §6.9）：`decisions.jsonl` 的**确定性 HTML 投影**——按天 `decisions/YYYY-MM-DD.html` + `decisions/版本总览.html`；零 LLM、内容比对幂等（同内容跳过、点「生成」无条件全量重建）、HTML 不嵌生成时刻；渲染分流富话题卡 / 记账卡（系统确定性写入）/ 简单卡（旧格式降级说明）；`risk_hit` 无 createdAt 按文件序回退归日。**档案是派生产物、删了可重建**（与「删库可重建」同级推广）。配套**写入时富化**（`topicId` / `userAsk` / `ownership` 认知所有权标注 / 转折点）+ **话题结束才写**（讨论中不写，话题闭合时一次性写完整结构）。
> ⑩ **封板复盘**（`ReleaseRetro`）：封板毕业仪式从「清账」升级为**「对账」**——`changes.jsonl` + `events.jsonl` + `decisions.jsonl` 确定性 fold，产出变更处置分布 / 回退分布（按目标阶段）/ 闸口 outcome 分布（approved / approved_after_revision / fast_track）/ 方法论使用报告（采纳 / 跳过 / 连续被跳过需校准）；两处消费：封板第一段确认弹窗一行摘要 + release-notes 尾部持久化段（markdownSection）；**纯磁盘事实聚合、不经 LLM 转述**。封板另加**两段式确认**（说明后果 → 最终确认）与**变更池毕业闸**。
> ⑪ **记忆两档作用域 + 治理**（§5.2 / §6.3）：`MemoryStore` 收敛为**项目池**（`<项目>/memory.jsonl` + 各版本 discussions 投影）与**全局池**（`~/PMAgent/memory.jsonl`）；**version 不再是作用域**（降为条目 `versions` 溯源标签，旧条目读时投影）；注入序 **项目 > 全局**、同池新覆盖旧；⚑ 硬边界（constraint / rejection）裁剪时永不丢、经验最先裁；**碑文协议**（失效条目不物理删除、留回链）；**整理动作白名单** + **「提升为全局」为两池唯一通道**。
> ⑫ **知识卡写时富化 + AI 代写案例 + 聚光灯推荐**（§5.2 / §6.2）：抽取 prompt 强制 `content` 三段多行（定义 / 做法 / 适用边界）+ 新增 `principle`（为什么有效）落卡为 `## 为什么有效` 节，旧卡兼容解析；方法论采纳时先落确定性草稿再由 AI 依阶段上下文代写一条案例（append-only 第二条，失败静默）；推荐收口左栏知识库整页**聚光灯**（一次主推一张 + 处置结算条 + 左栏待处置数字胶囊），采纳 / 跳过经 `methodAdopt` / `methodSkip` 事件留痕供封板复盘消费；**对话知识引用条**（`KnowledgeCiteBar`：本轮注入命中的卡，chip 点开详情）。
> ⑬ **系统行静默化 + 开场承接**（§6.2 / §6.4.1）：⚡快速通道受理 / ⏳跨门风险提醒 / ✅阶段推进三类系统行标 `silent`（UI 不渲染），语义由当轮 AI 回答的**开场承接句**承载（`injectedOpening` 仅链首注入）；静默行仍作链标记（`isFastForwardChainedBefore` / `mergeableNotes`）与**值班单交接条**（`DutyHandover`：段级计量 + 展开交接明细时间轴）节点；思考卡加**阶段时间线**（`phaseTrail`：已完成跳打勾、当前跳流光）。
> ⑭ **模型与稳定性**：`ThinkingEffort`（Low / Medium / High / Max，默认 High = 不发参；classify 等内部调用压 low）；`ContextTail` 动态材料尾条（前缀缓存命中）；transcript 置顶 + 60k 硬上限 + 剥产物块；max_tokens 抬至 65536 消 PRD 截断续写；**流式性能三刀**（`StreamBox` per-session 拆盒 + 发布尾窗化 + 识别/展示解耦，见 §12 性能条目与 bugs.md B001）；图表确定性拼接、反呓语约束、重复请求不重排。
> ⑮ §10 Eval 27 → 39 条（新增 E24 ~ E35）；§11 里程碑补 M5 实际进展；§12 补性能与并发风险条目；§15 开放问题补两项。PRD v1.3.0 同步。
> ⑯ **新增 §6.10 产物依赖图与重规划循环**（自适应主线补上「任务图 + 重规划」肌肉）：`ArtifactDependencyGraph`（2-Orchestration，全纯函数）——节点 = 真实产物（ArtifactPath 中文路径事实源；原型多槽位按目录前缀归并为家族节点），边 = 严格锚点（页面清单 ← 映射表、流程基准 ← 核心流程图、PRD ← 映射表/核心流程图/原型/竞品分析；功能架构图与业务流程图为信息性参考、无下游）；`downstream(ofSeeds:)` BFS 传递闭包 + 引用解析（具体引用优先、目录级回退、模糊文本解析为零不猜）。两个消费点：**重规划循环**——采纳变更提案 / 回退坞执行回退前生成「将依次重做哪些产物」的确定性计划，折叠进「🔄 已回到」行（**不新发独立系统行**，防切断快速通道链游走——B004 同教训；backfill 补做不产计划；文案只含产物中文名零内部路径）；**impacts 纪律 App 侧落地**——`verifiedTarget`：②/③ 回退建议引用不到任何具体产物 → 不采信 target（提案卡降级仅登记，① 增补澄清豁免）。14 条新测试（ArtifactDependencyGraphTests），全量 598 绿。
>
> **v0.9.12 变更记录**：**确认坞两段式提交简化为单页直接确认 + 系统事件文案「去黑话」**——① 确认坞取消第二页「确认摘要」（‹1/2› 分页控件随之下线）：两段式的设计意图（让用户知道将执行什么）由「确认并进入」选项卡副标题以人话承载（如「结构定稿存档，AI 随即开始生成可交互网页原型」），页脚主按钮文案随所选选项变化（确认并进入 / 继续修改 / 稍后再说）并直接执行；**「不确认不推进」闸口原则不变**——选项选择与按钮提交仍是两个显式动作，防误触语义保留。动机：摘要页的内容对用户决策零增益（原摘要写的是「将写入 02-structure/confirmed.json（闸口事实源）」级别的实现细节），却在每个闸口多付一次点击（三闸口 × 每项目）。② 系统事件文案从系统视角改写为用户视角（同批黑话清理）：确认回合注记去内部路径（「✅ 结构产物已确认——进入 ③ 原型」，不再出现 02-structure/confirmed.json 等存储路径）；📦 事件「已落盘」改「已生成」并统一使用产物中文名（与产物 chip 映射同词汇表：功能架构图 / 核心流程图 / 交互原型 / 产品需求文档 / 竞品分析报告）；🔒 闸口拦截去「确认闸口」术语，改「还没有确认——确认后才能…」句式；「落盘失败」统一改「保存失败」。**文案原则：界面只说「你定稿了什么、AI 接下来做什么」，内部文件路径与架构术语（闸口事实源等）留在实现层。**PRD v1.2.10 FR2a / A18 / 概述段同步。
>
> **v0.9.11 变更记录**：**技能 front-matter 扩字段（type / best_for / pitfalls）**——① `best_for`（3-5 个典型场景短句）入 schema，skills 表 embedding 编码范围由 name + when_to_use 扩为**四字段**（+ best_for + tags），提升中英 query 语义命中率；② 新增 `type`（component / interactive）：决定命中后生效形态——component 注入正文（现行机制不变，首批 14 个全部 component），interactive 触发问诊式流程（问 3-5 题 → 带理由编号建议 → 选路径执行，澄清 Agent 同款回路，V2 预留）；③ 新增 **`pitfalls`（结构化反模式，雷达信号源）**：反模式只存在于正文时读取依赖语义检索，**未命中 = 雷达漏检**，对哨兵机制致命——故提到 front-matter，自评审按「当前阶段」**规则路由确定性读取**该阶段相关技能全部 pitfalls（不依赖语义命中；「模板类文档走确定性路由」原则——§4 刻意设计 2——从文档扩展到技能元数据），实例化进自检清单（§6.2 清单实例化机制扩展）；`type` / `pitfalls` 不编码进 embedding。§5.2 技能 schema、§5.3 skills 表、§6.2、§15 开放问题 1 已同步；内容侧配套 **skills-inventory.md v1.1**（V2 候选池升级为分级池：P1 3 项 / P2 约 11 项 / 机制层 4 项 / 明确不进组）。PRD 同步待办：FR8 技能元数据枚举一行。
>
> **v0.9.10 变更记录**：**卡片库语义收窄为方法论卡片，经验萃取物归记忆层（三层语义划分）**——① 原卡片库 source_type 中的 discussion 类萃取物（用户偏好、领域事实）改归**记忆层**：记忆条目类型新增「经验」（带 source_ref 出处与 confidence），沿用覆盖语义——「发生过什么」（what/why）会过时，由 supersededBy 管；「怎么做事」（how）不会过时，只会被改良。② 方法论卡片新增**实战注记**机制：方法论被使用后，在卡片「实战注记」区 append 一条带项目出处的心得（只增不覆盖，越用越厚——官方定义谁都有，注记才是资产）；技能文件同构支持。③ 新增**方法论主动推荐**：阶段开始时 Agent 扫描双库按项目特征推荐适用方法（含理由、可拒绝、trace 可观测）——技能库从被动命中升级为主动调度。④ 新增**记忆校准注入**：Agent 使用某方法论时，同步注入记忆层中该用户的历史使用倾向（「上次用 KANO 把所有需求都标成基本型」）——方法校准不了使用者，记忆补位。⑤ 各阶段自检清单的检查项可由方法论卡 checklist 实例化（雷达个人化——检查项来自自己的方法论资产而非通用清单）；「这条记下来」弹窗新增归属分流（方法论卡片 / 经验记忆）。§0、§4（架构图 + 刻意设计 3/4）、§5.1、§5.2（卡片 schema 重写 + 记忆条目扩展）、§5.3、§6.2（④ 输入 + 自检清单实例化）、§6.3（注入 / 沉淀 / 手动入口）、§6.4、§6.5、§9 界面表、§10（Eval 26→27 条：E10 修订 + 新增 E23）、M4、附录 B（新增决策 15）同步修订。PRD v1.2.9 FR7/FR8/FR9/A10/A22/埋点同步。
>
> **v0.9.9 变更记录**：**④ PRD 环新增「模板分级智能选择」与「成功指标对话式定义」**（借鉴 slgoodrich/agents 的 requirements-engineer 机制，**机制借鉴、模板正文自写**——该仓库为 PolyForm Noncommercial 许可，禁直接搬运内容）——① 模板从单一 `templates/prd.md` 升级为**分级模板集** `templates/prd/{lean,standard,full}.md`（精简 1-2 页 / 标准 3-5 页 / 完整 5-10 页，同目录整组走规范路由确定性取最新版，「不用 RAG」约束不变）；进入 ④ 环时按**三维度评分**（技术复杂度 / 风险影响 / 范围广度，各 0-10）自动选档，**评分依据锚定上游已确认产物**（澄清要点 / 结构模块数与集成点 / 原型页面数），不凭空打分；评分卡（分数 + 理由 + 所选档）在生成 PRD 前的对话流展示、用户可一键切换——模板选择本身成为「上下文透明可解释」的又一个实证点。② 「数据指标」章新增**四步对话式定义**（期望结果 → 有无基线 → 目标值 → 测量时间点）：**数字 100% 来自用户**，Agent 只引导不建议不编造（防「漂亮数」污染 PRD）；无基线走「实施期建立基线」格式；任一步可跳过、按待定问题兜底不阻塞（提问沿用 FR2 交互回路，属澄清语义，不违反「④ 环一键流水线」的设计原则 1）。§6.2 ④ 环、§6.5 迁移表、§10（Eval 25→26 条，新增 E22）、M3 已同步修订。PRD v1.2.8 FR5 / A21 / 埋点同步。
>
> **v0.9.8 变更记录**：**阶段推进确认升级为「确认坞（ConfirmDock）」**——确认交互从对话流内联按钮改为**输入框上方停靠卡 + 两段式提交**（选择 ①确认并进入 / ②继续修改 / ③稍后再说 → 下一步展开确认摘要（将执行什么/推进到哪）→ 提交生效）；提交后卡收起、对话流内落确认记录胶囊（封版回看可见，E13），Agent 思考卡出现继续推进；停靠卡期间输入框始终可用——自由作答中明确推进指令（「进入下一个阶段」）等价选①并自动结算卡片，其余反馈等价选②；「稍后再说 / 继续修改」将卡折叠为输入框上方细条，可随时展开、不阻塞输入。对话流内 GateBar 相应简化为两态（已确认绿胶囊记录 / 待确认灰色暂停标记）；① 澄清的选项式提问（2-4 候选）改用同一确认坞模式。§6.2、§9 界面表、E18、M2、§12 风险表已同步修订。PRD v1.2.7 FR1/FR2a/A18 同步。
>
> **v0.9.7 变更记录**：**WKWebView 组件提前至 M1 预搭**——原型渲染组件（WKWebView 接入 + 本地单文件 HTML 加载渲染 + 「在浏览器打开」兜底入口）从 M2 原型环前移到 M1 骨架期落地，M1 验收线增补 **E21**（断网加载本地单文件 HTML：无白屏、页面可交互、沙箱/本地文件读取权限配置正确）。动机是**风险前移**：E15 的断网白屏失败模式中「组件接入」部分在 M1 即验证掉，M2 原型环只剩 HTML 生成本身的质量风险（prompt 硬约束那条，§12），组件不再是 M2 的未知数；M1 预算 +约 0.5 天，按时间盒纪律消化（砍深度不延期）。M2 原型 Agent 改为「复用 M1 预搭组件」。§10（Eval 24→25 条）、§11 M1/M2 行同步修订。
>
> **v0.9.6 变更记录**：**新增 §6.4.1 对话思考过程展示（ThinkingCard）**——Agent 每轮回复前置思考卡，两态：**思考中**（旋转 spinner + 流光「正在思考…」，长静默期防假死感）/ **完成后默认折叠**（摘要行「思考了 Ns · M 步 · 技能 ×K」，点击展开推理步骤与技能调用行混排）；v0.9.1 从右栏面板移出的「技能调用展示」在此落地（命中技能 + 触发词 + 注入内容 + 耗时）；推理步骤是**面向用户可读的推理摘要**而非原始思维链（保留可解释性、不泄露原始 CoT）；think 数据随消息存 session，完整 trace 仍在开发者模式检查器。§9 界面表（工作区·对话态）、E20（Eval 24 条）同步修订。PRD v1.2.6 FR9 / A20 同步。
>
> **v0.9.5 变更记录**：**漏项雷达判据显式化**——§6.3 四档声明块新增**「判断依据」**前置：每个点不是模型自由发挥，而是两项输入的逐项对账（① 本阶段自检清单维度固定不可自选；② 上游已确认产物——结构对澄清要点、原型对映射表、PRD 对结构+原型，每阶段清单本质都是「和上游对账」+ 本阶段自身一致性检查）；新增**判据定位**说明（清单 = LLM 判断脚手架，上限取决于清单特化程度，下限由「维度固定 + 连续 3 轮零修正告警」兜住）；四档各标注本质（✅ 对账结果 / ❓ 等输入 / ⏭️ 边界声明 / 💀 证伪）；✅ 档补「须列具体名称，禁『均已覆盖』式空话」。PRD v1.2.5 FR6 同步。
>
> **v0.9.4 变更记录**：**新建任务页右栏面板**——「面板」入口补全至新建任务页（标题栏右上角，与对话页同位同构，此前仅对话页可见）；新建任务页输入框新增**关联项目选择器**（不关联 / 关联已有项目），面板内容随之**项目关联驱动**：关联项目 → 产物 Tab 预览该项目文件树（项目无产物则空态）；未关联 → 四 Tab 全部空态；**决策日志在新建任务页恒为空**——决策与 WHY 是任务执行中才写入 decisions.jsonl 的运行时数据，任务未开始即无记录（同理漏项雷达/知识点为空态，不是缺失而是真实状态）。配套修正：「默认」项目会话的产物 Tab 为空态（此前会错误回退显示其他项目的文件）。§5.1.1、§6.4、§9 界面表、E19 同步修订。
>
> **v0.9.3 变更记录**：**风险登记册用户侧可读性**——用户界面状态翻译为**人话三态**（还盯着 = open / 被说中了 = triggered / 安全落地 = closed_*），risks.jsonl 的 5 个工程枚举不再直出；每条 💀 卡精简为**一句话假设 + 触发条件/结局 + 状态徽章**，命中卡改**对照式**（当时担心 ↦ 实际发生 ↳ 回写决策日志）；右栏默认只展开「在盯」风险，命中/关闭折叠为「已了结 N」计数行（点开才看）；状态机枚举、软上限、收敛动作等工程口径**退入开发者模式**（⌘D）。§6.3 闭环机制不变（落盘 schema / 状态机 / 软上限照旧，仅展示层翻译），§6.4、§9 界面表同步修订。
>
> **v0.9.2 变更记录**：**右栏 Inspector 升级为四 Tab**（产物 / 决策日志 / 漏项雷达 / 知识点）——原「上下文」面板更名「知识点」Tab；决策日志（紧凑卡 + 点击展开五要素）与漏项雷达（四档声明 + 💀 风险登记册活跃/命中/关闭）成为右栏常驻 Tab，与左栏独立页面、按阶段弹窗入口并存。§4 架构图、§6.4、§9 界面表、M4 里程碑同步修订。
>
> **v0.9.1 变更记录**：**右栏「上下文」面板瘦身为只展示注入的知识卡**（query + 命中卡 + scope 过滤说明）——token 构成条、Skill 注入、记忆注入、落盘校验四段从用户界面移除；**完整检查器降级为开发者模式**（⌘D 切换，含 token 构成 / 检索 trace / 未命中 skill 列表 / 分支技能触发记录），E11/E12「某东西没发生」的验证故事保留在开发者模式下。理由：token 预算与相似度分数是工程调试信息，对 PM 用户无价值；技能调用在对话思考过程中展示，记忆写入由对话系统行展示。§4 架构图、§6.4、§8、§9 界面表、E4a/E11、M4、§12 风险表已同步修订。
>
> **v0.9 变更记录**：**自评审机制从单一 checklist 升级为三层闭环**——① 自检清单扩为**漏项雷达四档**（✅已覆盖 / ❓可能遗漏 / ⏭️刻意不展开 / 💀致命漏洞假设），💀可为空是明文合法输出（防凑数）；② 新增**决策 WHY 记录**触发判据（备选方案存在性——生成过程中真实写出过 B 方案才算关键决策，须记全五要素）；③ 💀从一次性输出升级为**风险登记闭环**（落盘带触发信号 → 挂状态机事件由状态机驱动结算 → 软上限 3 条 + 收敛动作 → 命中后回写决策日志 → 封板统一终态结算）；④ 自评审失效告警改**悬空口径**（盯💀事后被真实事件结算，不盯💀有无——防 Goodhart 凑数指标制造它想防的病）。schema、状态机、Eval、迁移表、风险表已同步修订。
>
> **v0.8 变更记录**：**新增②结构设计阶段**——主线由三阶段扩为 澄清 → 结构 → 原型 → PRD **四阶段**：澄清确认后先生成功能架构图 / 核心流程图（复杂产品另出业务流程图）/ 模块-页面映射表（Mermaid 源码 .md，mermaid.js 本地打包渲染、零外部依赖），**经用户确认后才生成原型**，PRD 以「结构 + 原型」双重基准生成。新增**阶段推进确认**模式：每阶段产物落盘且自评审完成后，Agent 在对话中主动询问（① 确认并进入下一阶段 ② 继续修改 ③ 稍后再说），不确认不推进。人工闸口由两个扩为**三个**（澄清要点 / 结构 / 原型）；过期传播链延长为 结构修改→原型过期→PRD 过期（分级标注影响范围：局部/全部）。目录、状态机、schema、MCP、Eval、里程碑已同步修订。
>
> **v0.7 变更记录**：**取消末端毒舌评审阶段**——主线由四阶段收敛为 澄清 → 原型 → PRD **三阶段**；质量改为**各 Agent 每轮内建自评审**（产出 → 对照阶段特化自检清单自检 → 发现即改当轮修正，摘要随产物落盘）；打回机制取消（失败重试保留，那是运行错误不是质量问题）。原「评审独立性」原则废弃，其立场盲区风险以三条补偿：清单化自检（维度固定不可自选）+ 独立毒舌评审降级为可选分支技能（保留 MCP `review_doc` 入口）+ 两个人工确认闸口不变。schema、状态机、Eval、里程碑已同步修订。
>
> **v0.6 变更记录**：1. **流水线改序：原型前置**——主线变为 澄清 → 原型 → PRD → 评审；② 原型生成后新增**用户确认闸口**，PRD 以已确认原型为基准，功能需求与原型页面/流程一一对应。2. **竞品调研从主线降级为技能/插件分支**——不在主线上；技能库手动选用或对话意图自动命中（trace 可追溯），产物为 PRD 的可选输入；该分支是 V2 增值能力的扩展模板。目录结构、状态机、schema、Eval、里程碑已同步修订。
>
> **v0.5 变更记录**：补齐旧方案吸纳清单剩余项——新增 §6.5 初始内容资产迁移、§8 非功能需求、§11 硬门禁与 M0 验收线、附录 A 干中学技术映射表、附录 B 决策 WHY 记录（含「知识与技能同层双库」的决策补录）。
>
> **v0.4 变更记录**：
> 1. **技能与知识合并为同一层**——「知识与技能层」（同层双库：卡片库 + 技能库）。两者共用横切层基础设施（索引·scope 过滤·Context Builder 收口），但内容语义与检索机制保持分离（卡片 =「是什么」被检索，技能 =「怎么做」被调用）；架构图相应回到五层。
> 2. **原型生成拍板进 MVP**（旧方案红字阻塞项 Q1 的裁决）——自研路线：PRD → LLM 生成单文件 HTML → WKWebView 本地渲染；流水线从四阶段扩为**五阶段**（澄清 → 调研 → PRD → 原型 → 评审），评审新增「原型一致性」维度与定向打回。新增约 3~5 天工作量，**先记为预算债，执行时按门禁纪律砍深度消化，不延期**（见 §12 风险表）。
>
> **v0.3 变更记录**：平台决策已定死——**只做 macOS 原生 App，不做 Web 版**。原 v0.2 的 Next.js / Supabase / Vercel 架构整体作废，本文档为全量重写。流程主线采用「四阶段流水线」骨架，并入「三横切层 + 版本容器」作为基座；⑤数据 / ⑥迭代 / ⑦复盘三环划入 V2 可插拔阶段。

---

## 0. 一句话定位

一个**开源的 macOS 原生 PM 全流程 AI Agent**：输入一句话产品想法，经过**需求澄清 → 结构设计 → 原型生成 → PRD 生成**的四阶段流水线（**默认节奏而非硬向导**——用户明确要求跳步时走**快速通道**直达下游产物，被跳阶段留痕可补做）——先把产品结构（功能架构图 / 核心流程图 / 模块-页面映射表，Mermaid 开放格式）显式化并对齐认知，**结构经用户确认后**生成可点击原型，**原型经用户确认后**才撰写 PRD；每阶段完成时 Agent 在输入框上方**统一停靠卡**主动发起阶段推进确认（单页直接确认 + 分档路径：常规推进 / 跳步直达 / 到此为止——不确认不推进）；每个主线 Agent **每轮内建自评审**（产出 → 对照清单自检 → 当轮修正 + 漏项雷达四档声明），不设末端总评审；**过程中的新想法默认进候选变更池**（保住灵感、不打断收敛；要真改则走「提案 → 影响清单 → 用户裁决 → 回退重生成」，封板前池内提案必须逐条毕业处置）；**竞品分析与独立毒舌评审不在主线上**，以技能/插件分支按需触发（技能库手动选用 / 对话意图自动命中）；每轮对话的结论、约束、否决项、经验萃取与关键决策**自动沉淀**——结论与经验进记忆层（**项目 / 全局两档作用域**，覆盖而非并存）、方法论卡片进卡片库（去重合并 + 实战注记 append）、决策进 append-only 决策日志并**确定性渲染为可读档案**；PM 方法论（KANO / RICE / 用户故事地图…）作为技能入库，**元数据常驻、命中才加载正文，阶段开始以聚光灯主动推荐**；全部产物以**产品版本（v1.0 / v1.1 …）**组织在真实文件夹 + Git 中，可通过 **MCP（stdio）** 被 Claude Desktop / Cursor 调用。

**双目标**：开源产品（差异化钩子 = 对话自动沉淀知识 + 方法论技能化 + 上下文透明可解释 + 先结构后原型再 PRD 一键生成且断网可用）+ AI Agent 岗位面试作品集（JD 五项能力全覆盖）。

**JD 能力映射**：

| JD 能力 | 承载模块 |
|---|---|
| Prompt 工程 | 四个主线 Agent + 分支技能人格、Context Builder 三层组装、结构化输出约束、各 Agent 内建自评审约束设计 |
| Tool Calling | 竞品分析分支的 web_search / web_fetch、对话意图自动命中、Agent Loop、write-then-verify |
| RAG | 方法论卡片库 + 技能库，检索注入（前）与沉淀（后）的读写双向闭环（含实战注记飞轮） |
| MCP | 对外 MCP Server（stdio，异步任务模式） |
| Workflow | 四阶段流水线状态机（含结构与原型确认闸口、阶段推进确认）、各 Agent 内建自评审、版本封板 |

**为什么是 Mac 原生而非 Web**（决策记录，面试可讲）：

1. **本地优先是架构立场，不是省事**——数据不出机、可离线、MCP 走 stdio，三件事互相成立；没有服务端、没有账号、没有冷启动，演示日的故障面积小一个数量级
2. **MCP 在本地 App 上是更原生的形态**——stdio 本就是为本地进程设计的；Web 版反而要额外处理远程传输与鉴权
3. **唯一已验证的栈**——SwiftUI / Keychain / URLSession async 均已跑通，开工摩擦最小
4. **真实代价（已接受）**：拿不到可点开的链接，用「GitHub 源码 + 3 分钟录屏 + 屏幕共享现场演示」补偿

**为什么原型生成走自研而非墨刀 MCP**（决策记录）：墨刀依赖外部云服务与账号，与本地优先、开源叙事冲突；自研「LLM 产单文件 HTML + WKWebView 渲染」断网可用、零外部依赖，且生成过程本身可被 write-then-verify 与内建自评审约束。墨刀降级为 V2 可选导出通道。

---

## 1. 背景与竞品格局

- **起念 AI**（国内，公测）：引导式需求分析 → 需求分析包，但原型弱、纯云端、闭源
- **墨刀 AI**（万兴科技）：竞品调研 → PRD → 原型 → 评审一站式，生态封闭，已开放 MCP（`generate_prd` / `generate_react` 等 4 工具，异步任务模式），接口设计是本项目的直接参照
- **lighthought/app-maker**（开源，AGPL）：6 角色多 Agent 流水线（Analyst/PM/UX/Architect/PO/Dev），REST + Asynq/Redis 队列编排，Agent 引擎外包给 BMad CLI；架构模式（队列化阶段、文档传上下文、分阶段重试）值得借鉴，代码不可抄（AGPL）
- **空白**：开源世界没有「PM 全流程 Agent」，商业产品均未做「对话沉淀知识 + 方法论技能化 + MCP 双向 + 上下文透明可解释 + 断网可用的原型生成」

## 2. 目标用户与非目标

**目标用户**：
1. 想用 AI 提效的 PM（主）：输入想法，先拿到结构图与可点击原型、确认后拿到 PRD，竞品调研按需触发
2. 会写代码的 Builder（次）：clone、自构建、改造、贡献方法论技能包
3. 面试官（隐含）：作品需在 15 分钟内讲清五项能力的工程决策

**非目标（MVP 明确不做）**：
- 不做 Web 版 / 可分享链接（平台已定死 Mac，见 §0）
- 不做原型**编辑**画布（只做生成 + 归档；Figma 式精细编辑、墨刀 MCP 同步为 V2 可选）
- 不做数据报告分析、迭代管理深化、复盘（V2 的可插拔 Agent 阶段）
- 不做多人协作 / 权限（单用户本地工具）
- 不做多语言（中文为主，英文 README）
- 不做 App Store 上架（审核周期不可控；开源分发 = GitHub 源码）

## 3. 核心设计原则

1. **Agent 的核心是 Agent Loop，不是对话框**——对话仅用于需求澄清（输入天然模糊）、结构确认与修改反馈、原型确认与修改反馈、阶段推进确认、运行中干预；主线其余阶段一键流水线 + 进度可视
2. **文件是唯一事实源**——知识点、纪要、PRD、决策日志全是本地 Markdown / JSONL；SQLite 只存索引，可随时删掉并从文件全量重建；用户在 Finder 里手改文件是合法行为，不是异常
3. **产品版本是用户视角的唯一版本概念**——v1.0 / v1.1 = 一批需求的发布单元；Git commit 是底层快照机制，UI 不称「版本」
4. **横切层是基座，不是附加功能**——记忆层缺了，主线各阶段会退化成几个互不相干的聊天窗口；知识与技能层缺了，方法论无法规模化进上下文、产品越用越不值钱
5. **作用域隔离必须在检索层，不能靠 prompt**——scope 过滤（版本级 > 项目级 > 全局）写在检索层，prompt 里写「请只考虑当前项目」在多项目场景下必然被绕过
6. **评审内建，不设末端门禁**——每个 Agent 每轮对照自检清单自评、发现即改，质量在产出过程中保证而非产出后把关；自评有立场盲区（「灯下黑」），以清单化自检（维度固定不可自选）+ 可选独立毒舌评审分支（第二意见）+ 三个人工确认闸口（澄清要点 / 结构 / 原型）补偿
7. **BYOK 一切可插拔**——模型走 OpenAI 兼容端点，Key 存 Keychain 不进 git；每阶段可配不同模型
8. **每个 AI 输出必须有来源入口**——卡片能跳回出处、检索结果能点开原文、上下文构成可视。这是「信任」的唯一实现方式

---

## 4. 系统架构（五层）

```
┌──────────────────────────────────────────────────────────┐
│ 交互层   SwiftUI 单窗口三栏（NavigationSplitView）         │
│          左：项目/版本树+空间导航  中：工作区             │
│          右：三 Tab 面板（文件·决策日志·风险 · 检查器入 ⌘D）│
│          输入区上方：统一停靠卡（作答/分支确认/确认段）     │
├──────────────────────────────────────────────────────────┤
│ 编排层★  手写状态机（Swift actor）· Agent Loop             │
│          阶段闸口与分档路径 · 快速通道跳步 · 变更治理       │
│          多会话并行（会话/版本/项目三级 busy）· 后台完成     │
│          单阶段重试 · 全流程中断 · 运行态持久化              │
├──────────────────────────────────────────────────────────┤
│ Agent 层 ①澄清 Clarifier → ②结构 Structurer（用户确认闸口）      │
│          → ③原型 Prototyper（用户确认闸口）→ ④ PRD Writer      │
│          （每个 Agent 内建自评审：产出→自检→当轮修正）      │
│          分支：竞品分析 / 独立毒舌评审（技能/插件，不在主线）│
│          （角色可插拔，V2 分支加数据/迭代/复盘 Agent）        │
├──────────────────────────────────────────────────────────┤
│ 横切层★  Context Builder（唯一收口，token 预算约束）        │
│          记忆层（结论/约束/否决项/经验 · 两档作用域 + 保护层）│
│          知识与技能层（同层双库：方法论卡片库 + 技能库）      │
│          方法论卡去重合并+实战注记 · 技能库渐进披露+主动推荐 │
│          记忆校准注入 · 规范路由 · 决策日志（append-only）  │
│          确定性渲染（决策档案 / 封板复盘 · 零 LLM · 可重建） │
├──────────────────────────────────────────────────────────┤
│ 存储开放层  项目文件夹 + Git · SQLite 索引（可重建）         │
│          写锁 + 乐观校验（冲突不静默覆盖）                  │
│          MCP Server（stdio）· Keychain · BYOK 配置        │
└──────────────────────────────────────────────────────────┘
```

**架构上的九个刻意设计**（面试考点）：

1. **Context Builder 是唯一入口**。规则、记忆、技能正文、检索结果全部经它组装并受 token 预算约束。没有这个收口，多层能力叠加后上下文一定爆
2. **规范路由与 RAG 并列，不是二选一**。PRD 模板这类规范文档走确定性路由（固定路径取最新版），知识文档走向量检索——**过期规范比没有规范更危险**，向量检索会召回历史版本
3. **知识与技能同层、库分开；方法论与记忆分语义（三层语义划分）**。技能库与卡片库同属知识与技能层、共用基础设施（索引·scope 过滤·Context Builder 收口），且都是「怎么做事」（how）——技能库是方法论的可执行形态（流程化，被调用，渐进式披露），卡片库是方法论的知识形态（被检索，top-k，去重合并 + 实战注记 append）；而「发生过什么」（what/why——结论/约束/否决项/经验）归**记忆层**。判据：**「怎么做事」不会过时、只会被改良（卡片库，越用越厚，实战注记只增不覆盖）；「发生过什么」会过时（记忆层，新覆盖旧，supersededBy 管）**。混层的代价双向发生：把用户偏好当方法论跨项目复用（过期记忆冒充通用知识），或把方法论当记忆去覆盖（好方法被新结论静默顶掉）
4. **五条飞轮闭环**：决策日志 → 规则/方法论；方法论卡 → 向量索引 → 下次检索；**实战注记：方法论被使用 → 卡片注记区 append 一条带项目出处的心得 → 官方定义谁都有，注记才是资产**；自评审发现的漏项 → 澄清要点表回补；💀 命中 → 决策日志（预测 vs 实际，复盘资产）。产品越用越好用，这是唯一能讲成护城河的部分
5. **整张图跑在一台 Mac 上**：无服务端、无账号、无冷启动。数据不出机、可离线（除模型调用与联网调研）、MCP 走 stdio；原型为单文件 HTML，**断网也能渲染**
6. **容器层是唯一写入落点，也是唯一的作用域来源**。任何产出不许直接落盘，必须经由「当前项目 / 当前版本」写入；检索侧强制 scope 过滤——防串味是架构保证，不是靠提示词请求模型自觉
7. **主线精简、增值能力分支化**。竞品分析不是流水线阶段，而是技能/插件分支——技能库手动选用或对话意图自动命中（trace 可追溯），产物是 PRD 的可选输入而非必经环节；结构先于原型、原型先于 PRD，PRD 只描述已确认的结构与原型。V2 数据分析等增值能力沿用此分支模板，主线永远保持最短闭环
8. **LLM 只请求、App 只裁决（v0.10 确立的权限边界）**。所有影响流水线状态的动作（跳步 `artifact:fast-forward`、回退/变更提案 `artifact:backtrack`、PRD 落盘）都由模型输出**结构化请求块**、App 侧白名单校验后执行——prompt 只描述协议，**不是安全边界**；模型幻觉出的非法目标被白名单挡掉。这条把「模型可以决定产品状态」的可能性从架构上消除
9. **确定性计算优先于生成（v0.10 扩展）**。凡是能从既有事实算出来的东西（PRD 模板选档路由、技能 pitfalls 读取、决策档案 HTML、封板复盘统计）一律走确定性计算，**不交给 LLM**——LLM 只用于「必须理解语义」的环节（生成、分诊、抽取、推理）。代价是这些机制改起来更笨重，收益是它们**不会漂移**：同一份输入永远同一份输出，幂等可验证

---

## 5. 数据模型与目录规范

### 5.1 目录结构（文件是唯一事实源）

```
~/PMAgent/
  skills/                        全局，不属于任何项目，跨项目共享
    <skill名>.md                 front-matter: name / when_to_use / tags
  cards/                         全局方法论卡片（「怎么做事」，跨项目直接用不降级）
  index.sqlite                   索引（可丢弃重建）
  Projects/
    默认/                        「默认」项目（系统预建，不可删除）
      unversioned/               同下：未指定版本的文件落点
      project.json
    <项目名>/                     一个产品 = 一个真实文件夹（可 git init）
      project.json               目标收敛句、长期约束[]、否决项[]
      knowledge/                 跨版本共享的方法论卡片（Markdown + 实战注记）
        2026-09-09-KANO需求分类.md
      v1.0/                      产品版本 = 一次发布的发布单元
        version.json             版本号·需求清单·状态·发布信息
        release-notes.md         封板时自动生成（尾部含「本版复盘（决策稳定性）」段，v0.10）
        01-requirements/         澄清要点表（封板后不可变）
          clarification.md
          clarification-amend.json  增补澄清收束标记（v0.10，可缺省）
        02-structure/            ② 环生成（Mermaid 源码 .md，封板后不可变）
          architecture.md        功能架构图（模块/子功能层级）
          core-flows.md          核心流程图（P0 场景用户路径）
          business-flows.md      业务流程图（仅复杂产品出）
          module-page-map.md    模块-页面映射表（③④ 环的锚点）
          confirmed.json         结构确认记录（确认闸口依据）
          skipped.json           阶段跳过标记（v0.10 快速通道：跳过即留痕、可事后补做）
        03-prototypes/           ③ 环生成（单文件 HTML）+ 手动归档（截图/链接）
          prototype-v1.html
          prototype-修订-0918-1432.html  原型冲突降级槽（v0.10：磁盘被外部改写时
                                         新原型落此、主槽不动；同分钟自动 -2 递增）
          confirmed.json         原型确认记录（确认闸口依据）
          skipped.json           阶段跳过标记（v0.10）
        04-prd/                  ④ 环生成（基于已确认结构+原型，封板后不可变）
          prd-v1.md
        05-analysis/             分支：竞品分析包（可选；存在时 ④ 环引用）
          competitors-overview.md
        05-artifacts/            执行包与提案产物（v0.10）
          risk-plans/r_xx.md     风险采纳落实的执行包（采纳闭环产物）
          proposals/<sessionId>/ 草稿预演产物镜像（该会话独占，主线零写入）
        06-discussions/          结构化纪要（每阶段一份）
          01-clarify-notes.md
          02-structure-notes.md
          03-prototype-notes.md
        07-reports/              V2：数据报告
        decisions/               决策档案（v0.10，确定性 HTML 投影·删了可重建）
          YYYY-MM-DD.html        按天档案（纯函数渲染、内容比对幂等）
          版本总览.html           版本总览（中文名——档案是给人看的产物）
        decisions.jsonl          决策日志（append-only，永不删除；含 risk_hit 回写条目；
                                 富化字段 topicId / userAsk / ownership / 转折点，v0.10）
        changes.jsonl            变更治理台账（v0.10，append-only：提案登记行 + 处置回写行）
        risks.jsonl             风险台账（四态：open / mitigating / resolved / accepted，v0.10）
        self-review.jsonl        自评审对账记录（v0.10，四档声明完整审计的投影源）
        confirm-silence.json     确认坞静默标记（v0.10，版本级持久：每版本每阶段只弹一次）
        discussions.jsonl        讨论原始记录（append-only）
      v1.1/                      新版本 = 新目录
        ...
      memory.jsonl               项目记忆池（v0.10：跨版本长效条目 + 碑文兜底落点）
      unversioned/               「默认无版本号」目录：未指定版本的文件落点
        imported-screens.md      手动归档素材（未指定版本）
        chat-log-0912.md         临时讨论记录（未归版本）
  memory.jsonl                   全局记忆池（v0.10：跨项目共享，「提升为全局」的唯一落点）
```

**唯一真相源约定**：`index.sqlite` 只是加速索引，必须能随时删掉并从文件全量重建。这条定下来，双写一致性问题就不存在了——索引脏了就重建，以文件系统为准。

### 5.1.1 任务列表三级导航（左栏 · 项目 → 版本 → 会话）

左栏任务列表与磁盘目录一一对应，采用三级嵌套展示。**第三层是会话（Session）：每个节点是一个会话窗口，点击即打开该会话**；流水线阶段状态不占用导航层级（阶段进度展示在项目主页的四阶段状态点上）。

| 层级 | 内容 | 命名规则 | 交互 |
|---|---|---|---|
| 第一层 | 项目 | 与 `Projects/<项目名>/` 文件夹同名，用户自定义 | 点击展开/收起，不切换任务 |
| 第二层 | 版本号 + 「默认无版本号」 | 与 `v1.0/` 等版本目录同名（语义化版本 `v主.次`，小写 v 开头）；「默认无版本号」固定映射 `unversioned/` 目录 | 点击展开/收起；封板版本显示 🔒 只读 |
| 第三层 | 会话列表（版本下的对话窗口） | 会话标题默认取首条用户消息摘要，可重命名；右侧显示最后活跃时间 | 点击打开对应会话窗口；进行中会话显示运行点，封版版本下的会话为封存快照（只读） |

**「新建会话」按钮**：任务列表头部常驻「＋ 新建会话」按钮——在当前项目/当前版本下创建新会话并自动跳转；未选择任何项目时落入「默认」项目的 `unversioned/`；当前处于封版版本时落入该项目首个活跃版本（封版目录不可写入）。

**会话（第三层）的产品语义**：

- 一个版本下可包含任意多个会话；主线四阶段流水线的完整运行是其中一个会话（通常为首条），用户随时可开新会话追问、修改、单独调用某一能力
- 会话归属版本（或 `unversioned/`）；会话内的结论、决策、知识点沉淀到所属项目，与具体会话无关（会话可删，沉淀不丢）
- 已封版版本下的会话为封存快照，与封板时逐字一致，只读
- 会话原始记录 append-only 写入 `discussions.jsonl`；会话窗口只是其上的视图投影

**「默认无版本号」目录（`unversioned/`，每项目固定一个）**：

- **功能**：存放未指定版本的会话与文件，避免「不知道版本号」阻塞使用。三类典型来源——
  1. 用户手动归档素材（截图/链接）时未选择目标版本
  2. 临时讨论、草稿等尚未进入任何版本 scope 的会话与文件
  3. MCP 调用只传了 `project` 未传 `version` 时的会话与产物落点
- **命名**：磁盘目录固定为 `unversioned/`（ASCII，避免中文路径问题）；UI 显示为「默认无版本号」
- **迁移**：会话/文件在后续被归入某版本 scope 时，由系统移动到对应版本目录并在 discussions.jsonl 记录；不强制用户手动整理

**「默认」项目文件夹（系统预建，不可删除）**：

- **触发条件**：仅当用户未选择任何项目时（首次启动、新建任务前的初始状态），「默认」文件夹出现在项目层级顶部作为初始展示项；用户选中任一真实项目后，「默认」文件夹不再置顶显示（磁盘上始终存在）
- **使用场景**：
  1. 首次使用的引导态——左栏不为空，用户知道从哪里开始
  2. MCP 调用未指定 `project` 参数时，会话与产物落盘到「默认」项目（不报错、不丢数据）
  3. 用户快速开始（不建项目直接发消息）时的兜底容器
- **回收**：用户可随时在「默认」项目中选中会话或文件 → 「移动到项目」将其归入正式项目

**新建任务页的项目关联（v0.9.4）**：

- **入口**：新建任务页（首页）输入框底部常驻「关联项目」选择器——**不关联项目**（默认落「默认」项目 `unversioned/`，走 §5.1.1 兜底）或**关联任一已有项目**（发送后新会话与产物落该项目的 `unversioned/`，事后可迁移归位）
- **面板联动**：右栏面板内容随关联状态实时切换——关联项目 → **文件 Tab** 预览**该项目**的文件树（只读预览，选择产物可查看；项目尚无产物则空态）；未关联 → 文件 Tab 空态并提示引导。**预览 ≠ 写入目标混淆**：面板只是让用户发消息前看清「我关联的项目里已有什么」，避免重复提问已澄清的问题（已有 clarification.md 时 Agent 可引用）
- **空态即真实状态**：新建任务页的决策日志 / 风险 Tab 恒为空态——决策与 WHY 是执行中才写入 decisions.jsonl 的运行时数据，风险声明与方法论卡注入同样只存在于会话运行中；**新任务无这些数据不是缺失，是尚未开始**。空态文案引导而非填充示例数据（防「假数据先行」）
- **「默认」项目一致性（v0.9.5 修订）**：对话页打开「默认」项目下的会话时，**文件 Tab** 预览「默认」容器自己的文件树——产物落了盘就该能看（兜底容器兜到的货不可成为「产物黑洞」）；不回退显示其他项目的文件树（每个项目只读自己目录），防 scope 串味认知不变。产物 chip（「📦 xx已生成」）同步可点击直达预览（md → Mermaid 渲染 / html → 原型预览），risk/decision 落 jsonl 由右栏对应 Tab 承载

### 5.2 关键 schema

**project.json**
```json
{
  "id": "proj_8f3k",
  "name": "我的健身App",
  "goalStatement": "面向健身小白的训练打卡应用，解决「坚持不下去」的问题",
  "constraints": ["不做专业增肌人群", "iOS 优先"],
  "rejections": ["社区功能——竞品已验证留存提升有限"],
  "createdAt": "2026-09-09",
  "currentVersion": "v1.1",
  "versions": ["v1.0", "v1.1"]
}
```

**version.json**
```json
{
  "version": "v1.1",
  "status": "in-progress",
  "scope": ["社区动态", "周报自动生成"],
  "createdAt": "2026-09-09",
  "releasedAt": null,
  "gitTag": null,
  "inheritedFrom": "v1.0"
}
```

**决策日志（decisions.jsonl，append-only，每行一条）**
```json
{
  "id": "d_001",
  "version": "v1.1",
  "decision": "目标用户锁定健身小白",
  "why": "专业人群已有 Keep 等成熟方案，差异化空间在小白的坚持反馈",
  "rejectedAlternatives": [
    {"option": "覆盖全人群", "reason": "首版资源撑不起双场景"}
  ],
  "confidence": 0.8,
  "toBeVerified": true,
  "createdAt": "2026-09-09T14:30:00+08:00",
  "topicId": "t_004",                  -- v0.10 富化：话题分组（同一话题的决策聚合展示）
  "userAsk": "用户当初想解决什么",      -- v0.10 富化：用户诉求原文（记录「为什么要做这个决策」）
  "ownership": "user|ai_adopted",      -- v0.10 富化：认知所有权标注（用户原创 / AI 建议被采纳）
  "turningPoints": ["转折点：竞品数据推翻了原始假设"]  -- v0.10 富化：关键转折点
}
```
5 字段（决策 / WHY / 排除方案 / 置信度 / 待验证）缺一不可；「待验证」未闭环的在 UI 高亮。**用户每次决策自动产生同构记录，构成数据飞轮**——这是把 20 次迭代日志的经验产品化，不是搬语料。
**记什么（关键决策判据，见 §6.2）**：跨阶段影响 / 不可逆 / 备选方案存在性（生成过程中真实写出过 B 方案），满足任一即须记全五要素；其余一句话 WHY 即可——判据防两个极端：琐碎全记（稀释真决策）与漏记真决策。
**写入时富化 + 话题结束才写（v0.10）**：富化字段（topicId / userAsk / ownership / turningPoints）在**话题闭合时**由 LLM 一次性写全（讨论过程中不写，累计到话题收尾：用户显式收尾 / 新话题开启 / 阶段切换 / 手动兜底）——减少工具调用次数、提高记录质量，上下文作缓冲区 + 多重兜底降丢风险。事后 LLM 分析猜不出「这是用户原创还是 AI 建议」，所以必须在会话中采集（附录 B 决策补录）。
**risk_hit 回写条目（第 6 种条目类型）**：风险条目结算为「已解除」或被跨门重开时，自动 append 一条 `{"type": "risk_hit", "riskId": "r_001", "predicted": "…", "actual": "…"}`——「当初预测 vs 实际发生」的对照记录，是复盘时最值钱的资产。
**展示层 = 确定性 HTML 档案（v0.10，见 §6.9）**：原始 jsonl 不再在 UI 直接投影，统一渲染为按天档案 + 版本总览；档案删了可从 jsonl 重建。

**风险台账（risks.jsonl，append-only，读侧折叠为四态）**
```json
{
  "id": "r_001",
  "version": "v1.1",
  "stage": "prototype",
  "hypothesis": "若模块-页面映射表的 P0 页面数超过 5，单文件 HTML 原型将难以维护且生成耗时失控",
  "impact": "原型生成耗时翻倍，且后续每轮改动都要重排全部页面",   -- v0.10：后果（说不清后果不允许登记）
  "plan": "页面数钳制在 5 以内；超出的模块合并为二级 Tab 承载",      -- v0.10：建议应对方案（一句可执行）
  "trigger_signal": null,                       -- v0.10：可选——结算点已改为阶段确认门，
                                                --   旧版五枚举仅历史条目保留
  "status": "open",                             -- v0.10 四态：open（待处理）| mitigating（已挂方案·等验证）
                                                --   | resolved（已解除·验证通过）| accepted（已接受·风险自留）
                                                --   旧版 triggered / closed_unfired / closed_falsified / merged 仅解码兼容
  "origin_ref": "03-prototypes/self-review-round2.md",  -- 产生该条目的自评审摘要
  "resolution": null,                           -- 结算时回填：决策日志编号 / 重开原因 / 自留说明
  "planArtifact": "05-artifacts/risk-plans/r_001.md",  -- v0.10：采纳落实闭环落盘的执行包（nil = 未走落实）
  "createdAt": "2026-09-10T10:00:00+08:00",
  "closedAt": null
}
```
规则（v0.10 四态闭环）：① 登记必带 `impact`（后果）与 `plan`（建议方案）——说不清后果的不允许登记（防「登记了但没人知道会怎样」）；② **采纳 ≠ 解除**——用户点「采纳方案」进入 **mitigating（已挂方案）**，跨阶段确认门时批量核验，确认没出事才 **resolved（已解除）**；用户也可选 **accepted（风险自留）**，封板时带入 PRD 的已知风险；③ 活跃（open + mitigating）软上限 3 条，超限先收敛（关闭已证伪 / 降级 open_question / 合并同源）而非拒收；④ 被跨门重开（上游产物重做撞上未处理条目）→ 状态回到 open + 决策日志留 risk_hit 对照；⑤ 封板时全部未处置条目统一结算（终态闭合）。**SQLite 仅索引此文件（可重建），与「文件是唯一事实源」一致。**

**变更治理台账（changes.jsonl，append-only，每行一条）**
```json
-- 行一：提案登记（LLM 分诊产物，无 type 字段）
{
  "id": "chg_007",
  "idea": "支持消息通知提醒打卡",                 -- 模型从用户消息提炼，非原文照抄
  "category": "目标范围",                        -- 局部修订 | 页面流程 | 模块核心 | 目标范围 | 需验证
  "target": "clarify",                          -- 建议回退目标（clarify/structure/prototype）；nil = 仅登记不回退
  "mode": "revise",                             -- revise | redo（回退执行时透传）
  "instruction": "用户的具体诉求（原样保留）",
  "impacts": ["02-structure/核心流程图.md · 打卡模块", "01-requirements/clarification.md · 核心场景"],
                                                -- 影响清单（产物级引用）——target 非空时必填，App 侧校验
  "checkpointStage": "structure",                -- 登记时的检查点阶段
  "createdAt": "2026-09-15T10:00:00+08:00"
}
-- 行二：处置回写（type == "resolution"，同一提案以最新回写行为准）
{"type": "resolution", "id": "chg_007", "resolution": "pooled",
 "note": "留到 v1.2 做", "decidedAt": "2026-09-15T10:05:00+08:00"}
```
处置枚举（`ChangeResolution`）：**adopted**（纳入当前版本，执行回退 + 重生成；池毕业「纳入后续版本」同值、note 区分）/ **pooled**（放入候选池）/ **dropped**（未采纳：提案卡「继续讨论」或池中明确放弃）/ **deferred**（顺延：下一版本再议）。折叠规则：按文件序处理，同一提案最新回写行胜出（`ChangeLedger.fold`）。
**草稿预演扩展字段（可选，旧行解码兼容）**：`kind: "stage_draft"` + `draftSessionId`（提案目录按会话 id 分槽）+ `draftStage`（草稿已推进到的阶段）。

**记忆条目（`<项目>/memory.jsonl` 项目池 + `~/PMAgent/memory.jsonl` 全局池）**
```
id、类型（结论/约束/否决项/经验）、内容、
pool（project | global）、           -- v0.10 两档作用域（取代旧 scope 三档）
versions[]、                          -- v0.10：版本降为溯源标签（不再是作用域）
source_ref（「经验」必填：出自哪个项目哪次讨论）、
confidence（「经验」带，其余可省）、
是否失效、supersededBy（被谁覆盖）
```
**核心语义：新结论覆盖旧结论，不并存**——「不做多用户」被推翻后，以新结论为准，旧条目标记失效并留回链，而不是两条同时生效导致自相矛盾。**过期记忆比没记忆更危险。**
**两档作用域（v0.10）**：`MemoryStore` 维护**项目池**（该项目的 `memory.jsonl` + 各版本 discussions 投影）与**全局池**（跨项目共享）；注入序 **项目 > 全局**（具体性优先），同池新覆盖旧；**version 不再是作用域**——条目的 `versions[]` 只作溯源标签，旧 version 作用域条目读时投影迁移。
**治理规则（v0.10）**：⚑ **硬边界**（约束 / 否决项）在上下文预算裁剪时**永不丢**，假设类经验最先裁；**碑文协议**——失效条目不物理删除，保留可追溯的碑文回链；**整理动作白名单**（合并 / 失效 / 提升）——不许 AI 自由改写；**「提升为全局」是两个记忆池之间唯一的流通通道**，需用户显式操作。
**「经验」类型（v0.9.10 新增）**：原卡片库 discussion 类萃取物（用户偏好、领域事实——「发生过什么/为什么」）改归记忆层，带 source_ref 与 confidence、沿用覆盖语义；判据与「怎么做事」的分界见 §4 刻意设计 3。跨项目引用经验时按**假设态**注入（记忆校准用，见 §6.3），不冒充通用结论。

**结构化纪要（06-discussions/，Markdown + 前置 YAML）**
```yaml
---
stage: clarify                  # clarify | structure | prototype | prd | review（分支任务不占流水线阶段位）
version: v1.1
date: 2026-09-09
---
## 决策项
- D1: 目标用户锁定健身小白（已写入 decisions.jsonl#d_001）
## 关键信息
- K1: 竞品 Keep 社区功能留存提升 15%（出处：05-analysis/…md）
## 开放问题
- Q1: 打卡提醒是否依赖推送权限？
## 待办
- [ ] 确认 iOS 通知权限申请文案
```

**方法论卡片（knowledge/ 或全局 cards/，Markdown + 前置元数据块）——卡片库语义收窄：只收「怎么做事」（how）**
```markdown
---
id: kp_001
source_type: methodology        # methodology | manual（discussion 类萃取物改归记忆层，见下）
source_ref: v1.1/06-discussions/02-structure-notes.md#D3
project: 我的健身App             # 空 = 全局；方法论卡跨项目直接用，不降级
confidence: 0.9
supersededBy: null              # 冲突消解：方法论被实质改良时旧卡让位（非日常覆盖）
created: 2026-09-09
---
KANO 模型：将需求分为基本型/期望型/兴奋型三类，先分类再排优先级——
**基本型**：满足度再高也不产生兴奋，缺失则强烈不满；
**做法**：先过目标用户画像，再给每条需求归类，最后按「必须有 / 越多越好 / 惊喜」排布；
**适用边界**：需求清单超过 15 条、且用户群画像清晰时最有效；单点小功能别用。

## 为什么有效（v0.10，写入时富化）
人对照某个具体的人做判断远准于抽象打分——「基本型」不是一个分数，
是「用户会不会因为缺它而骂人」这个可想象的具体场景。

## 实战注记（append-only · 只增不覆盖——越用越厚）
- 2026-09-10 · 健身App v1.1：把「数据精度」错标为基本型——该项目
  小白用户实为期望型；分类前先过一遍目标用户画像，别按常识标
- 2026-09-18 · 健身App v1.1：AI 代写案例——在结构阶段的模块排序上用它砍掉了
  3 个伪核心模块，预期结果是原型页面数从 8 降到 5（可在卡片库修订）
```
规则：**正文三段结构（定义 / 做法 / 适用边界）+「为什么有效」原理段（v0.10 写入时富化）**——避免单行整段卡与「只有结论没有原因」（旧卡无此节，解析兼容）；**实战注记区只增不覆盖**，每条带项目出处与日期——卡片价值随使用复利增长，这是与记忆层「覆盖语义」的本质区别（判据见 §4 刻意设计 3）。带 checklist 的方法论卡（如评审清单、发布检查单）可供各阶段自检清单**实例化**（见 §6.2）。

**技能（skills/，Markdown + front-matter）**
```markdown
---
name: KANO 模型
type: component              # component | interactive（v0.9.11）：component → 命中后
                             #   注入正文（现行）；interactive → 触发问诊式流程（V2 预留）
when_to_use: 需求优先级排序、区分基本型/期望型/兴奋型需求时
best_for:                    # 3-5 个典型场景短句（v0.9.11，提升语义命中率）
  - 新产品从 0 到 1 砍需求清单
  - 功能池超过 15 条需要分层
tags: [prioritization, methodology]
pitfalls:                    # 结构化反模式（v0.9.11，雷达信号源）：命名失败模式
  - 全标基本型：未过目标用户画像，把期望型误标为基本型
---
（正文：方法论内容，仅命中后加载；「反模式」章节与 pitfalls 逐条对应）

## 实战注记（append-only，与方法论卡同构）
- 2026-09-10 · 健身App v1.1：……
```
字段规则（v0.9.11）：① `name + when_to_use + best_for + tags` 四字段编码进 embedding；② **`pitfalls` 不走语义检索**——自评审按「当前阶段」规则路由确定性读取该阶段相关技能的全部 pitfalls，实例化进自检清单（§6.2）——反模式若只在正文，检索未命中 = 雷达漏检，对哨兵机制致命；③ `type` 决定命中后生效形态（注入正文 / 触发问诊）。首批 14 个技能清单与 V2 分级候选池见 skills-inventory.md。

### 5.3 SQLite 索引（仅索引，可重建）

```sql
create table knowledge_points (
  id text primary key,           -- 与文件 id 一致
  project_id text not null,
  content text not null,         -- v0.10：三段结构（定义/做法/适用边界）+「为什么有效」
                                 --   （principle 落在卡片正文节，索引不新增列）
  source_type text not null,     -- methodology | manual（discussion 类归记忆层，v0.9.10）
  source_ref text not null,
  embedding blob not null,       -- 向量序列化存储（仅对正文编码，注记不参与检索）
  annotation_count int default 0, -- 实战注记条数（注记正文留在卡片文件，索引只存计数，可重建）
  confidence real default 1.0,
  superseded_by text,            -- 冲突消解链
  created_at text
);

create table skills (
  id text primary key,
  name text not null,
  type text not null default 'component',  -- component | interactive（v0.9.11，命中后生效形态）
  when_to_use text not null,     -- 元数据常驻，正文不进索引
  best_for text,                 -- 典型场景短句，JSON 数组（v0.9.11，编码进 embedding）
  tags text,                     -- 中英双语，JSON 数组（v0.9.11 起入库，编码进 embedding）
  pitfalls text,                 -- 结构化反模式，JSON 数组（v0.9.11）——不编码 embedding，
                                 --   供自评审按阶段规则路由确定性读取（§6.2）
  doc_path text not null,
  embedding blob not null,       -- 对 name + when_to_use + best_for + tags 四字段编码
  hit_count int default 0,
  enabled boolean default true
);

create table pipeline_runs (     -- 流水线运行态（重启不丢）
  id text primary key,
  project_id text not null,
  version text not null,
  current_stage text not null,   -- clarify | structure | prototype | prd（分支任务不占流水线状态）
  structure_confirmed boolean default false,  -- ② 环确认闸口状态（未确认不得进 ③）
  prototype_confirmed boolean default false,  -- ③ 环确认闸口状态（未确认不得进 ④）
  status text not null,          -- running | suspended | failed | done
  self_review_fixes int default 0, -- 自评审发现并修正的问题数（观测自评审有效性；连续为 0 触发告警）
  radar_risk_hits int default 0,  -- 风险命中结算次数（告警口径：盯事后结算，不盯💀有无）
  clarify_rounds int default 0,  -- 已用澄清轮次（上限 5）
  error json,
  updated_at text
);
-- 阶段位置可从磁盘重建：deriveStage 按 confirmed.json / skipped.json 标记推导，
-- 不依赖内存缓存（v0.10：多会话并行与重启下状态不漂移）

create table risks (              -- 风险台账索引（事实源为 risks.jsonl，可重建；v0.10 四态）
  id text primary key,
  project_id text not null,
  version text not null,
  stage text not null,
  hypothesis text not null,
  impact text,                   -- 后果（用户/进度影响；v0.10 登记必填）
  plan text,                     -- 建议应对方案（v0.10）
  trigger_signal text,           -- 旧版事件枚举（v0.10 起可空——结算点改为阶段确认门）
  status text not null,          -- open | mitigating | resolved | accepted（旧五枚举仅历史解码兼容）
  origin_ref text not null,
  resolution text,
  plan_artifact text,            -- 执行包产物相对路径（采纳落实闭环；v0.10）
  created_at text,
  closed_at text
);

create table changes (            -- 变更治理台账索引（事实源为 changes.jsonl，可重建；v0.10）
  id text primary key,
  project_id text not null,
  version text not null,
  idea text not null,
  category text,
  target text,                   -- 建议回退目标（clarify/structure/prototype）；nil = 仅登记不回退
  mode text,                     -- revise | redo
  impacts text,                  -- 影响清单（JSON 数组，产物级引用）
  checkpoint_stage text not null,
  resolution text,               -- nil = pending；adopted | pooled | dropped | deferred
  resolution_note text,
  kind text,                     -- nil = 普通提案；stage_draft = 草稿预演
  draft_session_id text,         -- 草稿预演所属会话（提案目录分槽；v0.10）
  draft_stage text,              -- 草稿已推进到的阶段（v0.10）
  created_at text,
  decided_at text
);

create table mcp_tasks (         -- MCP 异步任务（get_task 轮询的数据源）
  id text primary key,
  type text not null,            -- generate_structure | generate_prd | generate_prototype
  payload json not null,
  status text not null,          -- pending | running | done | failed
  result json,
  created_at text
);
```

索引重建：扫描 knowledge/、cards/、skills/ 文件目录 → 逐条 embedding → upsert。

**约定说明**：
- **方法论卡归属单一项目**：文件存在各项目 knowledge/ 内；全局 cards/ 仅放跨项目通用的方法论卡（「怎么做事」跨项目直接用，不降级为假设态）；用户偏好、领域事实类讨论萃取物不进卡片库，归记忆层「经验」。检索注入默认仅当前项目 + 全局 cards/ + 全局 skills/，**其余一律不跨项目**（记忆校准注入的经验条目除外——按假设态注入，见 §6.3）
- **作用域优先级**：版本级 > 项目级 > 全局，冲突时窄的赢
- **向量检索选型**：MVP 用**内存暴力余弦**（初始语料几百到一两千条 chunk，毫秒级）；写死切换阈值（约 2000 条 chunk）并留出检索接口，到点换 SQLiteVec——「因为量小所以不上向量库」本身就是一条可讲的技术决策

### 5.4 版本生命周期

```
planning（定版本号+需求清单）
   → in-progress（跑流水线，产物落盘，后台自动 commit 快照）
   → released（封板：git tag + 冻结目录 + 自动生成 release-notes.md）
```

- **封板**：手动触发 + **两段式确认**（v0.10：第一段说明后果并展示一行复盘摘要，第二段最终确认——封板是不可逆动作，值得多一次点击）；从 version.json 的 scope + 决策日志自动提炼「本版更新说明」；封板后版本目录只读，UI 顶部黄条提示；**变更池毕业闸（v0.10）**：该版本池内若有未处置提案（`pooled > 0`），封板被拦并引导先去处置——不留「说不清」的尾巴；**风险终态结算（v0.10）**：封板时 risks.jsonl 全部未处置条目（open / mitigating）统一结算为终态——每条有始有终
- **封板复盘 · 本版决策稳定性（v0.10，`ReleaseRetro`）**：封板毕业仪式从「清账」升级为**「对账」**——`changes.jsonl` + `events.jsonl` + `decisions.jsonl` 的**确定性 fold**（纯磁盘事实聚合，不经 LLM 转述、不虚构），产出四类信号：① 变更处置分布（提案 / 纳入 / 放弃 / 顺延 / 池内未毕业 / 从未处置）；② 回退分布（`stageInvalidate` 事件按目标阶段计数，含驳回留痕）；③ 闸口确认 outcome 分布（`approved` / `approved_after_revision` / `fast_track`，①②③ 确认漏斗写入）；④ **方法论使用报告**（`methodAdopt` / `methodSkip` 事件：本版采纳的卡、被跳过次数、同版本被跳过 ≥2 次的「待校准」卡——该质疑：补充案例、更新或退场）。两处消费：**封板第一段确认弹窗的一行摘要** + **release-notes 尾部持久化段**（`## 本版复盘（决策稳定性）`）。三类信号全空时不渲染该段。复盘素材同时作为**下版本的开工素材**（知识反哺三阶段之一，另两项见 §14）
- **继承规则（精确）**：开新版本时**继承** project.json 的约束与否决项、上一版 PRD 可选引用；**不继承**讨论记录与中间稿——v1.1 可以推翻 v1.0 的决策，但要能在决策日志里看到「为什么变」
- **对比**：两版对比 = scope diff + 文档 git diff；同名产物并排 diff
- **回看**：v1.1 里回看 v1.0 的 PRD 与原型必须与封版快照**逐字一致**
- **快照**：阶段完成/产物保存自动 `git commit`（串行化队列，用户无感），仅用于回看与撤销——用户想要更细历史，自己 git 就行，不造第二个 git

---

## 6. 流水线与横切层设计

### 6.1 状态机

```
[选/建版本] → CLARIFY → STRUCTURE →【用户确认闸口】→ PROTOTYPE →【用户确认闸口】→ PRD → [封板(可选)]
                 ↑           ↑                          ↑
                 │           └── 确认前：基于反馈修改图 ──┘
                 └──── 结构回退（③④ 发现结构缺口：改②→重新确认→原型/PRD 标过期）

原型回退（④ 发现原型缺口：改③→重新确认→PRD 标过期）
跳步（v0.10 快速通道，见 §6.6）：① →③ / ① →④ / ② →④ / ③ 到此为止
    被跳阶段写 skipped.json、不生成产物、位置按闭环链重推导、可事后补做
分支（不在主线上）：竞品分析 / 独立毒舌评审（技能/插件，手动选用或按需调用，不阻塞主线）
```

**过期传播链（分级）**：结构修改并重新确认 → 原型标记过期（AI 标注影响范围：局部 / 全部）→ PRD 相应过期。分级标记避免链式重做吓退用户。

**闸口路径选择（v0.10，`GateRoute`）**：确认坞的选项不止「下一步」——按当前闸口分档：

| 闸口 | 推进类选项 | 终止类 |
|---|---|---|
| ① 澄清后 | 进入结构（`next`）/ 跳过结构直出原型（`skipToPrototype`）/ 跳过中间阶段直出 PRD（`skipToPRD`） | — |
| ② 结构后 | 进入原型（`next`）/ 跳过原型直出 PRD（`skipToPRD`） | — |
| ③ 原型后 | 进入 PRD（`next`） | 到原型为止（`stopHere`，本版不出 PRD） |

**闸口确认 outcome（v0.10，写入 events.jsonl 供封板复盘消费）**：`approved`（直接确认）/ `approved_after_revision`（修订后确认）/ `fast_track`（经快速通道跳步推进）。

**弹坞纪律（v0.10，`confirm-silence.json`）**：同一版本同一阶段的确认坞**只主动弹一次**（产物落盘时）——用户选「稍后再说 / 继续修改」后该阶段转为**静默**，静默状态版本级持久化、跨启动跨会话生效（不重复弹、💬 留痕不重复落），修订落盘也不弹；推进改由用户发起（自由作答「进入下一阶段」/ 摘要条兜底行）；静默中的修订轮由 `gateInviteSuffix` 在 AI 回复末尾融一句推进邀请（邀请 ≠ 弹坞）。

**风险结算挂载点（v0.10 修订）**：结算点从「事件驱动状态机」改为**阶段确认门**——跨门时批量核 `mitigating`（已挂方案·等验证）条目；旧版五枚举触发信号（`structure_regen` / `prototype_regen` / `prd_stale` / `decision_overturned` / `release`）仅作历史条目解码兼容，新登记不再产生。状态机仍负责：上游产物重做撞上未处置条目 → **重开回 open** + 决策日志留 `risk_hit` 对照（v0.10 四态闭环，见 §5.2 / §6.2）。

- ① CLARIFY 对话式，用户确认要点表后发起**阶段推进确认**进入②；② STRUCTURE 产出结构图后停在**用户确认闸口**（主线第二个人工闸口），确认后进入③；③ PROTOTYPE 生成后停在**用户确认闸口**（主线第三个人工闸口），确认后进入④；④ 一键完成
- **阶段推进确认（v0.8 新增，通用机制；v0.10 改为统一停靠卡 + 分档路径）**：每阶段产物落盘且自评审完成后，Agent 在**输入框上方的统一停靠卡确认段**主动询问是否进入下一阶段——**单页直接确认**（选择处理方式 → 页脚按钮随所选选项直接生效，不做第二页摘要）；选项按闸口分档（见上表）；**用户不确认不推进**；用户随时主动说「进入下一个阶段」等价于确认；停靠期间输入框始终可用；提交后卡收起、对话流内落确认记录胶囊（封版回看可见）；**每版本每阶段只自动弹一次**（弹坞纪律，见上）
- **快速通道（v0.10，见 §6.6）**：用户明确要求跳步 → LLM 发 `artifact:fast-forward` 请求块 → App 白名单裁决 → 跳过中间阶段直达目标产物；被跳阶段写 `skipped.json`
- **回退（v0.10 双模）**：`artifact:backtrack` 协议 `mode` 区分 **revise**（带上一版作修订基底，只改用户说的部分）/ **redo**（不带上一版、从零重做）；④ 阶段另有回退坞快捷入口（见 §9）
- **变更治理（v0.10，见 §6.7）**：新想法 / 重做上游诉求 → LLM 只分诊出提案块 → 用户裁决 → 纳入当前版本（执行回退）/ 进池 / 继续讨论；默认进池
- 每阶段完成：产物落盘（write-then-verify）+ **内建自评审**（Agent 每轮产出后对照清单自检、当轮修正，摘要随产物落盘）+ 结构化纪要生成 + 知识点抽取回写 + 决策写入决策日志 + 状态推进
- **质量在过程中保证，不设末端评审与打回**：自评发现的澄清类漏项回写要点表；③④ 发现结构缺口 → 回②修改 → 重新确认 → 原型/PRD 自动标记过期重生成；④ 发现原型缺口 → 回③修改 → 重新确认 → PRD 自动标记过期重生成
- 失败：单阶段可重试（只重跑该阶段——失败是运行错误不是质量问题，与自评审无关），不整链重跑
- 运行态持久化：当前阶段 / 结构与原型确认状态 / 澄清轮次存于 `pipeline_runs`，退出 App 再打开恢复到中断前；**引擎可从磁盘重建**（`deriveStage` 按 confirmed / skipped 标记重推导阶段位置——重启与多会话并行下状态不漂移）

### 6.2 主线 Agent 与分支技能的 Prompt 要点

**① 需求澄清（Clarifier）**
```
角色：资深产品顾问，苏格拉底式提问风格。
任务：围绕用户的一句话想法，通过多轮对话暴露需求盲区。
约束：每次只问一个最关键的问题；最多 5 轮；
     【主动提问——必须保证】凡遇到影响后续产出的疑问，必须暂停向
     用户提问，用户作答后基于答案继续，不得自行假设跳过；交互回
     路固定为「提问 → 作答 → 继续」（对齐主流 AI Agent 产品的
     执行中提问模式）；
     【选项式提问】每轮提问可附 2-4 个候选选项供用户点选，同时
     保留自由输入（可自定义答案或补充说明）；用户可跳过任一问题，
     跳过项按 open_questions 处理，流程不卡死；
     每轮记录「已知信息 / 盲区 / 本轮新决策」；
     5 轮耗尽仍有必填项缺失 → 强制收束：缺失项写入 open_questions
     后放行进入②（UI 明示「强制收束」，要点表不齐不阻塞流水线）；
     结束时输出澄清要点表（JSON Schema 约束：
     target_user / core_scenario / core_value / constraints / open_questions）。
```

**② 结构设计（Structurer）——Mermaid 产出型 Agent，先于原型**
```
角色：资深产品架构师。
输入：01-requirements/clarification.md（核心场景/核心价值/约束）
     + 命中的信息架构/流程设计 skill（若有）+ 横切层注入。
任务：把澄清要点转成结构性产物，落盘到 02-structure/：
     a) 功能架构图（模块/子功能层级，Mermaid graph TD）——必出
     b) 核心流程图（P0 场景用户路径，Mermaid flowchart，闭环校验）——必出
     c) 业务流程图——仅复杂产品（多角色/多实体）按澄清复杂度判断
        是否产出及粒度
     d) 模块-页面映射表（模块 → 原型页面清单）——必出，
        ③④ 环的锚点（③ 取页面清单、④ 锚定功能需求）
     停在用户确认闸口：确认前可基于反馈修改，用户确认后写入
     confirmed.json 并放行进入 ③。
约束：Mermaid 源码写在 .md 中（开放格式，Finder 手改合法，
     Git 可 diff）；图与澄清要点一致，不引入输入外新假设；
     粒度自适应——简单产品出轻量图（可合并架构+流程为一张），
     复杂产品出全套；修改交互三层：对话式改（AI 改图并展示
     变更差异）/ App 内改源码+实时预览 / Finder 直接改 .md
     （索引兼容）。
```

**③ 原型生成（Prototyper）——代码生成型 Agent，以已确认结构为基准**
```
角色：资深交互设计师 + 前端工程师。
输入：02-structure/ 已确认结构产物（module-page-map.md 页面清单
     + core-flows.md 流程基准——confirmed.json 缺失则拒绝进入
     本阶段）+ 命中的交互模式 skill（若有）。
任务：把模块-页面映射表的 P0 页面（3-5 个）转成单文件 HTML/CSS
     原型，页面间跳转按核心流程图连通，落盘到 03-prototypes/，
     WKWebView 内嵌预览，停在用户确认闸口：确认前可基于反馈
     重新生成/局部调整，用户确认后写入 confirmed.json 并放行
     进入 ④。
约束：单文件、零外部依赖（无 CDN / 无网络请求，断网可渲染）；
     页面与模块-页面映射表一一对应，不凭空增删页面；
     灰盒线框风即可，不追求视觉稿；
     只做流程演示，不实现业务逻辑。
```

**④ PRD 撰写（Writer）——以已确认结构与原型为双重基准**
```
角色：严谨的产品经理。
输入：03-prototypes/ 已确认原型（页面清单、核心流程——
     confirmed.json 缺失则拒绝进入本阶段）
     + 02-structure/ 已确认结构（模块-页面映射表、核心流程图）
     + 01-requirements/clarification.md
     + 05-analysis/*.md（可选，分支产物）
     + 横切层注入（记忆 + top-k 方法论卡 + 命中技能）
     + 方法论主动推荐（进入本环时 Agent 扫描卡片库+技能库，
       按项目特征推荐 1-3 个适用方法论，含推荐理由，
       可拒绝、trace 可观测——机制见 §6.3）
     + 记忆校准注入（使用某方法论时，同步注入记忆层中该用户的
       历史使用倾向——「上次用 KANO 把所有需求都标成基本型」）
模板来源：规范路由器（确定性读取 templates/prd/ 分级模板集最新版，
     不走向量检索——过期规范比没有规范更危险）。
     分级：lean 精简版（1-2 页，简单迭代 / 低风险小功能）/
     standard 标准版（3-5 页，默认兜底）/
     full 完整版（5-10 页，高风险 / 大范围 / 涉资金权限数据）。
     三档共享章节骨架，差异在深度：lean 允许章节合并
     （如「背景与目标 / 功能需求 / 指标与边界」三章），
     full 细化各章（数据指标含基线表、非功能需求分维展开）。
     选择：进入本环时按三维度评分自动选档——技术复杂度 /
     风险影响 / 范围广度（各 0-10），评分依据锚定上游已确认
     产物（澄清要点表的复杂度与约束、结构产物模块数与集成点、
     原型页面数；迭代版本以版本语境信号参与判断），不凭空打分。
     阈值：均值 <4 → lean；均值 ≥7 或风险维 ≥8 → full；
     其余 → standard（默认）。评分卡（三维度分数 + 各一句理由 +
     所选档）在生成 PRD 前的对话流展示，用户可一键切换三档
     任一——模板选择本身是可解释决策，不是黑盒。
模板章节：背景与目标 / 用户画像与场景 / 功能需求（含优先级，
     若命中 RICE 方法论则按 RICE 排序说明）/ 非功能需求 /
     数据指标 / 边界与风险。
数据指标（四步对话式定义，生成到该章时触发，沿用 ① 环
     交互回路——选项式 + 自由作答 + 可跳过）：
     ① 期望结果（开放式：做成什么样算成功）
     ② 有无基线（有 → 用户提供当前值；无 → 该指标走
        「实施期建立基线」格式，标注基线待建立）
     ③ 目标值（用户给数字）
     ④ 测量时间点（点选或自填，如上线后 30/60/90 天）。
     硬约束：数字 100% 来自用户，Agent 只引导对话、不建议
     不编造任何具体数值——PRD 是下游实施的依据，编造的
     「漂亮数」会直接变成错误的验收标准；任一步跳过按
     待定问题处理（写入 open_questions、该指标标待定），
     不阻塞生成。
约束：引用调研事实时标注出处文件路径；功能需求与已确认结构
     （模块-页面映射表）及原型的页面/流程一一对应——结构与
     原型中不存在的功能不得写入 PRD；
     不引入上游输入之外的新假设。
```

**内建自评审（每个主线 Agent 的通用约束，写进各自 system prompt）**
```
时机：每轮产出后（含多轮对话的每一轮），在输出给用户 / 落盘前。
流程：产出草稿 → 对照本阶段自检清单逐项检查 → 发现问题当轮修正
     → 输出时附自评审摘要（发现的问题 / 已修正项 / 遗留问题），
       摘要随产物落盘（02-structure/、03-prototypes/、04-prd/
       等对应目录，UI 可见可追溯）。

━━ 漏项雷达四档（每轮输出末尾的漏项声明，四档缺一不可声明，档内可为空）━━
  判断依据（每个点怎么判）：不是模型自由发挥，而是两项输入的逐项
  对账——① 本阶段自检清单（维度固定不可自选，见下）；
  ② 上游已确认产物（结构对澄清要点、原型对映射表、PRD 对
  结构+原型——每阶段清单本质都是「和上游对账」+ 本阶段自身
  一致性/闭环检查）。
  判据定位：清单与判据是给 LLM 的「判断脚手架」——把「好好
  检查一下」的空泛指令变成可枚举、可对账的逐项检查；质量上限
  取决于清单特化程度，下限由「维度固定 + 连续 3 轮零修正告警」
  兜住（判断终归是 LLM 判断，脚手架只保证下限与可审计性）。
  ✅ 已覆盖：对照清单维度，本次实际展开了哪些——本质是「对账结果」；
     须列具体名称，禁「均已覆盖」式空话
  ❓ 可能遗漏：信息不足导致的待定方向 + 需要用户补充的具体问题清单
     ——❓ 的本质是「等输入」：有外部信息就能消除，回写 open_questions
  ⏭️ 刻意不展开：识别到但主动跳过的点 + 原因——本质是「边界声明」
     （超出 scope / 需单独展开，必须给原因）
  💀 致命漏洞假设（红队视角）：切换为批评者，假设本次结论是错的，
     揪出「哪个核心假设一旦不成立、整个结论就崩塌」
     ——💀 的本质是「证伪」：不需要新输入，现有信息下就能推翻
  分工硬规则：❓ 与 💀 不得互相挪用——需要用户回答的进 ❓，
     现有信息可证伪的进 💀；同一条两边都写的，归 💀。
  💀 可为空是明文合法输出：没有真实致命假设时，只允许写一句
     「未发现致命漏洞，最脆弱的假设是 X」。禁止为填而填凑数——
     凑出来的💀会污染风险台账并挤占软上限名额。
  审计归宿（v0.10）：四档声明全量落 self-review.jsonl，「每轮都在自检」
     的可见性由右栏「风险」Tab 顶部的「N 轮对账 · 累计修正 M 处」
     计数器承载（对话流只报本轮新增——事件驱动，防刷屏）。

各阶段自检清单（漏项雷达 + 六视角，按阶段特化，维度固定不可自选）：
  · ① 澄清：五字段盲区覆盖度；追问是否流于形式；
    是否未经澄清直接给方案。
  · ② 结构：核心场景覆盖度（每个核心场景在流程图中有路径）；
    流程闭环（无断头路、无死循环）；模块边界清晰（无重叠、
    无孤儿模块）；映射表完备（每个 P0 模块都映射到页面）；
    与澄清要点一致。
  · ③ 原型：核心场景覆盖；页面跳转闭环；零外部依赖；
    页面与模块-页面映射表一一对应；与澄清要点一致。
  · ④ PRD：功能需求与已确认结构及原型一一对应；引用出处齐全；
    无上游输入外新假设；模板章节完整。
  · 清单实例化（v0.9.10）：以上为通用兜底清单；命中的方法论卡/技能
    若带 checklist，其检查项**实例化**进本阶段自检清单——雷达个人化：
    检查项来自自己的方法论资产（注记里「上次漏了 XX」直接变成
    本轮检查项），实例化项与通用项并存、trace 可辨来源。
  · pitfalls 确定性路由（v0.9.11）：技能 front-matter 的 `pitfalls`
    列表按「当前阶段」规则读取该阶段相关技能**全部**条目，实例化
    进本阶段自检清单，**不依赖语义命中**——反模式防的就是「没想到
    要检查这个」（skill 没被检索命中时其反模式恰恰最该在岗）；
    与命中卡 checklist 实例化并存，trace 标注信号源 skill 名。
回补：自评发现的澄清类漏项 → 回写澄清要点表（open_questions），
     形成「自评审 → 澄清」回路；❓ 档条目同走此回路。

━━ 决策 WHY 记录（与自评审并行，见 §5.2 决策日志）━━
关键决策判据（三条，满足任一即须记全五要素）：
  a) 跨阶段影响（决策会改变下游阶段产物）
  b) 不可逆（落盘/封板后撤销成本高）
  c) 备选方案存在性——生成过程中真实写出过 B 方案（有实质内容的
     备选，不是「综合考虑了其他方案」一笔带过）。犹豫声明不算：
     模型没有 token 层面的犹豫信号，事后自评「我犹豫过」不可观测；
     备选方案有落盘痕迹，编造的备选会在具体性上露馅。
  兜底（回看检验）：三个月后有人问「为什么这么做」若答不上来，
  该决策漏记了五要素，补记。
  非关键决策一句话 WHY 即可（🟢 轻量档，执行规则见附录 B）。

━━ 风险台账四态闭环（红队从烟花变哨兵；v0.10 由触发信号状态机升级）━━
  1. 登记带后果与建议：每条写入 risks.jsonl 时必须带 impact（后果——
     对用户/进度的具体影响）与 plan（一句可执行的建议应对方案）；
     说不清后果的不允许登记（旧版「必带触发信号」约束在 v0.10 退场）。
  2. 四态流转：open（待处理）→ mitigating（已挂方案·等验证）
     → resolved（已解除·验证通过，回写决策日志）；或 → accepted
     （已接受·风险自留，封板时带入 PRD 已知风险）。
     核心纪律：**采纳 ≠ 解除**——采纳只是「挂了方案」，
     方案落地并确认没出事才算数。
  3. 结算点在阶段确认门：跨门时批量核 mitigating 条目——
     不再由模型每轮轮询扫描（否则第 20 轮核 20 条，
     结局必然是机械勾「未触发」）。
  4. 采纳落实闭环（v0.10）：用户点「采纳方案」→ 系统代发受理注记
     → AI 生成实施执行包（落 05-artifacts/risk-plans/，可预览、
     可注入下一轮）→ 流完成后状态转 mitigating + 落一条带证据指针的
     决策日志；中途中断保持 open 可重试；降级为「仅记账」的
     普通采纳路径必须显式告知用户（无声降级 = 事故）。
  5. 排队逃生门：同版本排队期间提供「不等了，只记账」——
     立即按普通采纳处理并钉住排队坐标，不阻塞用户。
  6. 软上限 + 收敛动作：活跃条目（open + mitigating）上限 3 条
     （工作集限制，类比 WIP limit）。超过时必须先执行收敛动作之一：
     关闭（已证伪）/ 降级为 open_question（实为❓非💀）/ 合并（同源），
     而非禁止登记——同时出现 4 条真实致命风险时，丢风险比
     破坏上限更糟。
  7. 终态闭合：封板时所有未处置条目统一结算——每条有始有终。
  8. 跨门重开：上游产物重做撞上未处置条目 → 状态回 open +
     决策日志留 risk_hit 对照（「当初预测 vs 实际发生」——
     预测命中记录是复盘时最值钱的资产，也是防「放空炮」的硬证据）。

自评审对账 trace（v0.10，对话流只报新增）：四档声明的完整审计归宿
     在右栏「风险」Tab 顶部——「N 轮对账 · 累计修正 M 处」计数器，
     逐轮可展开看 ✅ 覆盖清单与 ❓/⏭️/💀 计数（self-review.jsonl 投影）；
     对话流只播报本轮新增（事件驱动），不再每轮重复刷四档全文。

阶段推进确认（通用约束；v0.10 改为停靠卡确认段 + 分档路径）：
     本阶段产物落盘且自评完成后，在输入框上方统一停靠卡的确认段
     主动询问是否进入下一阶段；选项按闸口分档（§6.1 表：①② 含跳步档、
     ③ 含「到此为止」）；**单页直接确认**（不做第二页摘要）；
     用户不确认不推进；用户主动说「进入下一个阶段」等价于确认。
     弹出纪律（每版本每阶段弹一次）：产物落盘弹坞一次，
     「稍后再说 / 继续修改」即该阶段转静默（confirm-silence.json
     版本级持久、跨启动跨会话，修订落盘也不重弹）；
     之后推进由用户发起；静默中的修订生成轮由 prompt 在 AI 回复末尾
     融一句推进邀请（不弹卡）；确认推进后清除静默——回退重做回到
     该阶段视为新一轮提醒。

系统行静默化与开场承接（v0.10）：⚡快速通道受理 / ⏳跨门风险提醒 /
     ✅阶段推进三类系统行标 silent（落盘但不渲染 UI），其语义由
     当轮 AI 回答的开场承接句承载（【开场承接】注入段：把系统交办的
     事实转成大白话第一句，只依据给定事实、不照抄、不编造其他进展）；
     静默行仍作链标记（合并行 / 快速通道链判定 / 值班单时间轴节点）
     与 self-review 对账节点。无开场承接注入时，AI 不得自行播报
     状态变化或进度。
```

**分支：独立毒舌评审（可选技能/插件，不在主线上）**
```
定位：用户想要独立第二意见时按需触发（默认不开）；
     保留原「评审独立性」价值——不注入主线生成阶段上下文。
触发：a) 技能库「用这个」手动运行；b) MCP review_doc 对任意文档。
角色：以苛刻著称的评审官。找出问题比夸奖更有价值。
输出（JSON Schema）：六维打分（需求完整性 / 方案合理性 / 用户价值 /
     技术可行性 / 文档质量 / 原型一致性，各 0-10）
     + 每个扣分点的理由与修改建议。
约束：意见供用户参考落盘归档，不阻塞主线、不自动打回——
     是否修改由用户决定。
```

**分支：竞品分析（Researcher）——技能/插件，不在主线上**
```
触发：a) 技能库「用这个」手动运行；b) 对话意图自动命中
     （"帮我调研下竞品"）——命中记录写入检查器 trace
     （开发者模式 · 命中了哪个技能、为何命中）。
工具：web_search(query), web_fetch(url)
任务：基于澄清要点，找 3-5 个直接竞品，输出结构化分析包：
     每个竞品（定位 / 核心功能 / 差异点 / 可借鉴 / 出处链接），
     落盘到 05-analysis/。
约束：所有事实必须带出处 URL；找不到的信息标注"未找到"而非编造。
定位：④ PRD 环的可选输入（存在则引用并标注出处，不存在不阻塞）；
     本分支是 V2 增值技能/插件的扩展模板——主线精简、增值分支化。
```

### 6.3 横切层：注入与沉淀（读写双向 RAG）

**注入（Agent 调用前，全部经 Context Builder 收口）**

```
query = 当前阶段上下文摘要
  → 记忆层：按 scope（版本 > 项目 > 全局）取有效条目（失效条目不注入）
  → 知识与技能层（同层双库，各自检索）：
      · 卡片库：内存余弦检索 top-k 方法论卡（强制 scope 过滤，
        仅当前项目 + 全局；「怎么做事」跨项目直接用，不降级）
      · 技能库：when_to_use 语义匹配命中 → 仅加载命中 skill 的正文
        （未命中 skill 的正文不进上下文，token 可测）
  → 规范路由：模板类文档走确定性路径，不走向量检索
  → 方法论主动推荐（阶段开始时，v0.9.10 新增）：Agent 扫描卡片库
    + 技能库，按项目特征（澄清要点/结构产物/原型规模等）推荐 1-3
    个适用方法论——推荐含理由（引用项目特征与卡片注记）、用户可
    拒绝、推荐/采纳/拒绝全程写 trace（同阶段不重复推荐已拒项，
    防推荐变骚扰）；技能库从被动命中升级为主动调度
  → 记忆校准注入（使用某方法论时，v0.9.10 新增）：从记忆层取该用户
    对同类方法论的历史使用倾向（「上次用 KANO 把所有需求都标成
    基本型」类「经验」条目，按假设态注入），随方法论一并注入——
    方法校准不了使用者，记忆补位
  → 全部结果带上来源与 scope 标签，写入本次运行的 trace 日志
```

**沉淀（阶段结束后）**

```
结构化纪要生成
  → 结论/约束/否决项 → 记忆层（新结论覆盖旧结论，标 supersededBy）
     两档作用域（v0.10）：项目池 > 全局池；version 降为溯源标签；
     ⚑ 硬边界（约束/否决项）裁剪时永不丢；失效条目走碑文回链
  → LLM 抽取（schema 约束，非自由生成）+ 归属分流（v0.9.10）：
      · 「怎么做事」（how）→ 方法论卡片：去重合并（与现有库相似度
        >0.92 → 合并而非新建；同主题冲突 → 旧卡标失效）；
        写时富化（v0.10）：正文强制三段（定义/做法/适用边界）
        + 「为什么有效」原理段（principle）
      · 「发生过什么/为什么」（用户偏好、领域事实）→ 记忆层
        「经验」条目（带 source_ref + confidence，覆盖语义）
  → 方法论被实际使用（含推荐被采纳）→ 该卡「实战注记」区 append
    一条带项目出处的心得（只增不覆盖；抽取器只起草注记草稿，
    write-then-verify 后落盘）；**AI 代写案例**（v0.10）——先落确定性
    草稿（项目/版本/阶段 + 推荐时机线索），再依阶段上下文代写一条
    更具体的「在哪个环节、怎么用的、预期结果」（append-only 第二条，
    失败静默——确定性草稿兜底）
  → 决策项 → decisions.jsonl（5 字段 append-only + **话题结束才写**的
    富化字段：userAsk / ownership / 转折点，v0.10）→ 确定性渲染为
    HTML 档案（§6.9）
  → 推荐采纳 / 跳过 → events.jsonl 记 methodAdopt / methodSkip
    （v0.10，供封板复盘的方法论使用报告消费）
```

**推荐展示（v0.10 收口）**：方法论推荐不在右栏（知识点 Tab 已移除）而在**左栏「知识库」整页**——**本阶段聚光灯**：一次主推一张（匹配度 + 为什么现在），处置完翻下一张；底部**处置结算条**（采纳 N 张 · 跳过 M 张，案例已记进各自卡片——让用户看到「我处置过什么」）；左栏导航带**待处置数字胶囊**（不错过、不打扰）。**对话知识引用条**（`KnowledgeCiteBar`）：AI 回答底部列本轮注入命中的卡（确定性数据、注入即显示），chip 点开卡片全文与案例——回答的「用了什么」在对话内可见，不只藏在开发者模式。

**手动入口**：对话中随时点「这条记下来」→ 弹窗先做**归属分流**（默认判定可改）：「怎么做事」→ 方法论卡片（走卡片抽取 + 相似卡合并弹窗）；「发生过什么/为什么」→ 经验记忆（入记忆层，带 source_ref）。写入型 RAG 不能只靠阶段结束自动抽取。

**write-then-verify**：所有落盘操作（产物、原型、纪要、卡片）写入后必有一次回读校验，不一致则报错——防「返回成功但语义未生效」。**并发增强（v0.10）**：写入走 `writeMeasured(expectedSHA:)` 乐观校验，被外部改写时抛 `ArtifactConflict` 不落盘（见 §6.8）。

### 6.4 Context Builder 与可观测性

- **token 预算分配**：规则 / 记忆 / 技能正文 / 检索 / 历史各占配额，超预算按优先级裁剪；**保护层（v0.10）**：记忆中的约束与否决项（硬边界）永不裁，假设类经验最先裁
- **用户侧右栏 Inspector（v0.10 收敛为三 Tab：文件 / 决策日志 / 风险）**：① **文件**（分区台账，v0.10 改版——上半区扫 01~07 编号目录产物按四类归档：文档 / 原型 / 图表 / 报告 + 类型筛选 chips + 整行点击预览 + 右键菜单「预览 / 添加到对话 / 设为主原型」；下半区为版本目录全量内容的**工作空间文件**档案树，面包屑标注磁盘目录、与 Finder 口径一致；两区之间可拖分隔条调高、比例持久化、双击复位）② **决策日志**（紧凑卡列表 + 点击展开五要素）③ **风险**（v0.10 四态台账：顶部「N 轮对账 · 累计修正 M 处」自评审计数器 + 三分区 **待处理 / 已挂方案·等验证 / 已处理·留痕**；每条卡 = 一句话假设 + 「后果」警示标签 + 建议方案块 + 状态徽章 **待处理 / 已挂方案 / 已解除 / 已接受**四态人话；采纳动作走落实闭环，见 §6.2）。**知识点 Tab 已移除（2026-09-17）**——语义检索与推荐收口左栏知识库整页，右栏回归「会话伴随上下文」。**新建任务页同常驻此面板**（「面板」入口与对话页同位同构）：内容随输入框**关联项目**驱动——关联 → 文件 Tab 预览该项目文件树；未关联或任务未开始 → 各 Tab 空态（空态即真实状态，见 §5.1.1）
- **完整检查器（开发者模式 ⌘D 切换 ★）**：token 构成条（规则/记忆/skill 正文/检索/历史）+ 检索 trace（query → 命中条目 + scope 标签 + 相似度）+ 未命中 skill 列表（证明没被注入）+ 分支技能触发记录 + **主动推荐 trace**（推荐了什么 / 理由 / 采纳或拒绝 / 同阶段防重推荐）+ **记忆校准注入 trace**（注入了哪条「经验」· 假设态标签）+ **风险台账工程口径**（状态枚举 · 软上限与收敛动作 · 结算记录）
- 「某东西没发生」类验证（未命中 skill 不入上下文、跨项目 0 泄漏）没有检查器只能口头声称，有它就能当场指着屏幕证明——**这是绝大多数竞品 demo 没有的面板，最低成本的差异化**（演示时刻意切开发者模式展示，日常不打扰用户）

### 6.4.1 对话思考过程展示（ThinkingCard）

Agent 每轮回复在正文之上可携带**思考卡**（默认折叠，不挤占阅读动线），与右栏 Inspector 互补：Inspector 管「这次注入了什么」，思考卡管「这次怎么想的」。两态展示：

| 状态 | 展示 | 触发 |
|---|---|---|
| **思考中（流式）** | 旋转 spinner + 「正在思考…」流光文字 | 流式生成期间持续可见——长静默期（首轮 token 前的检索/规划）防「假死感」 |
| **已完成（默认折叠）** | 摘要行「思考了 Ns · M 步 · 技能 ×K」，点击展开步骤列表 | 流式结束后折叠为摘要，想看才展开 |

**阶段时间线（v0.10，`phaseTrail`）**：一次用户消息触发多跳（判断 → 跳步/回退 → 自动重做）时，思考卡展开区顶部显示**阶段时间线**——已完成的跳打勾（done）、当前跳流光，用户能看清「现在跑到哪一步了」。数据源 `StreamState.phaseTrail`（AppModel 确认链经 `setStreamPhase` 逐跳入轨）；无多跳时该区不渲染。

**steps 结构**（推理步骤与技能调用行混排，消息 schema 新增可选 `think` 字段）：

```json
{
  "think": {
    "dur": 4,
    "steps": [
      { "text": "对照评审六维与自评：扣分点同源——非功能需求无验收标准。" },
      { "skill": "内部埋点字典", "detail": "命中 · 触发词「埋点口径」· 注入 12 个事件定义", "dur": "0.6s" }
    ]
  }
}
```

- **普通步骤**（`text`）：一句推理要点——**面向用户可读的推理摘要**，不是原始思维链：保留「为什么这么做」的可解释性，同时不泄露原始 CoT 全文
- **技能调用行**（`skill` + `detail` + `dur`）：命中了哪个技能 / 触发词 / 注入了什么 / 耗时——v0.9.1 从右栏面板移出的「技能调用在对话思考过程中展示」在此落地（E20 判定来源）
- **折叠即默认**：思考内容是可解释性的证据不是正文；摘要行常驻（耗时 + 步数 + 技能数），完整推理一击展开
- **落盘**：`think` 随对话消息存 session 记录（不新增文件）；完整推理 trace 仍在开发者模式检查器（⌘D）——两者分工：思考卡给结论级信任，检查器给 token 级证明
- **性能约束（v0.10，bugs.md B001）**：展开态思考流只渲染**尾部窗口**（≈十几行可视量），完整内容留在全量态——流式渲染量必须常数化，否则长思考轮会出现「模型早输出完了、界面还在慢慢显示」（实测 262s → 预期 ~75s）。显示与识别解耦：进度/块判定等**识别事实在发布点用全量文本计算**、结构化下发，视图只渲染不解析

**值班单交接条（v0.10，`DutyHandover`）**：一次用户消息触发多次 LLM 调用时（判断段 → 回退/跳步 → 自动重做段），每个**续段**内容顶部渲染一行**段级计量**——进行态给「计时 + 已生成字数」，完成态定格为「耗时 + 步数 + 阶段」；点击展开链内**交接明细时间轴**。数据全部由已有条目推导（`MessageBubble.dutyHandover`，不新增持久化），历史会话重开同样可见。**铁律：交接条数据不得含 `Date()`/`now` 派生字段**——消息气泡走 Equatable 短路（流式期间每秒十次重渲染），含时间派生字段会让短路永远失效。

### 6.5 初始内容资产迁移（产品第一天不是空壳）

| 现有资产 | 迁移方式 | 落点 |
|---|---|---|
| 产品之神 17 条约束 | 拆成「全局约束层」+「PM 角色层」 | Context Builder 规则层（rules/global.md） |
| PRD 标准模板 | 扩为**分级模板集**（lean/standard/full 三档，正文自写）进**规范路由器**（确定性检索，不进向量索引） | ④ 环输出格式来源（三维度评分选档，见 §6.2）；「为什么不用 RAG」的实证对象 |
| 苏格拉底式追问教练 | 直接复用 | ① 澄清 Agent 的角色人格 |
| 漏项雷达 + 六视角 | 改造为阶段特化的扣分维度 | 每个主线 Agent 的自评审 checklist（内建化：四档雷达声明 + 阶段清单）+ 独立毒舌评审分支的六维打分 |
| 20 次迭代日志 | **不直接搬**——只抽取其结构 | 决策日志数据模型（5 字段同构记录 + 💀 命中回写条目，见 §5.2）；复盘心得 → 对应方法论卡的初始实战注记 |
| 永久笔记卡片生成教练 | 直接复用 prompt 与卡片 schema | 知识与技能层·卡片库抽取器（省掉最难的 schema 设计；抽取目标收窄为方法论卡，讨论类萃取分流至记忆层「经验」） |
| 10 个角色指令 + 8 篇产品复盘 + 方法论笔记 | 灌入技能库与卡片库；**复盘中的方法论心得 → 对应卡片的初始实战注记**（day-1 就示范「注记才是资产」） | 30+ 条真实 skill 与卡片，**初始即非空壳** |
| 数据报告生成能力 | 输出模板与归因框架 | V2 ⑤ 数据分析分支 |
| 表达力教练、求职陪跑等其余角色 | **本期不迁** | MVP 只有 PM 一个角色（防范围蔓延） |

**这是唯一真稀缺的供给**：别人 6 周能写出同样的代码，写不出这些内容资产。M2-M4 优先灌入，让产品第一天就不是空壳 demo。

### 6.6 快速通道（v0.10，主线是建议节奏而非硬向导）

**设计立场**：四阶段是**默认节奏**，不是硬向导。用户已经很清楚要什么时，让流程完整性凌驾于用户意图之上就是产品在为难人。快速通道省掉的是「逐步确认」，**不是质量把关**——到手的产物照走自评审、模板选档与确认坞。

**协议与裁决边界（与变更提案同构的重要原则）**：LLM 只**请求**，App **裁决**——LLM 输出 `artifact:fast-forward` 块（`{"target": …, "instruction": …}`），`AppModel.fastForwardTarget` 白名单校验后执行。**权限边界在 App 侧**，prompt 只描述协议，不是安全边界（LLM 幻觉出的非法目标被白名单挡掉）。

| 起始阶段 | 合法跳步目标 | 效果 |
|---|---|---|
| ① 澄清后 | `prototype` | 跳过 ②，直达原型 |
| ① 澄清后 | `prd` | 跳过 ②③，直达 PRD |
| ② 结构后 | `prd` | 跳过 ③，直达 PRD |
| ③ 原型后 | —（无下游；「到此为止」走确认坞 `stopHere`，不是 fast-forward） | — |

**触发判定纪律**（写进 prompt，防误触发）：
- 必须是用户**明确**要求跳步——措辞含「直接出 / 别问 / 不用确认 / 跳过确认」一类，或明确表达不想逐项确认
- 只说「生成 PRD / 做个原型」而无跳步措辞**不算**跳步（目标为 prd 时走下方的前置确认分诊）
- 意图拿不准 → 照常澄清提问，不输出块
- **禁止 AI 主动建议跳步**——只转述用户明确意图
- 跳步请求信息不足时**不得反问拦截**：缺失项由 App 侧按合理假设补齐并在产物中标注（这正是快速通道的语义——用「直接出」换速度）

**跳过即留痕（`skipped.json`）**：
- `PipelineEngine.skipStage` / `advanceFromClarify(skipping:)` 写跳过标记（`{reason: "route_selection", skippedAt}`），与 `confirmed.json` / `amend.json` 同构
- 跳过阶段**不生成产物**（不伪造空产物充数），阶段位置由 `deriveStage` 按闭环链重推导
- 事件流记 `stageSkip`（`reason: route_selection`详列跳过阶段），`stageAdvance` 事件带 `outcome: fast_track`
- **可事后补做**：保留补做入口（`requestBackfill`），补做时删跳过标记、正常生成产物

**PRD 前置确认分诊（目标为 prd 且用户无跳步措辞时）**：先盘点影响 PRD 内容走向的未决项（用户未表态、只能按假设起草的关键取舍，如提醒时机、商业化口径、数值基线）——
1. 未决项 1-3 个 → **不出 fast-forward**，改出 `{"purpose": "prd_preflight"}` 的 `artifact:question-card` 前置确认卡；每题 2-4 选项、**首选项 = AI 建议默认值**、detail 一句话说明默认依据或影响哪章；用户选完 **App 自动串链**生成 PRD（不让用户再点一次「生成」）
2. 未决项 ≥4 个 → 不出卡也不出 fast-forward，照常执行本阶段任务，末尾列待确定清单并提示「确定完这些问题再生成 PRD 就好」
3. 用户随后表态「你定 / 直接出」→ 转快速通道

**PRD 产物块硬禁令（协议层的兜底）**：任何阶段**禁止 LLM 直接输出 `artifact:prd` 产物块**——PRD 落盘只能由系统执行（前置确认卡提交或 fast-forward 受理后自动生成，才会走模板与评分卡协议）。理由：PRD 落盘链路挂了模板选档 / 评分卡 / 确认坞三个机制，若允许 LLM 直接产块，这三个机制全部可被绕过（且模型容易只写半份）。错写的 PRD 全文系统不会落盘，用户拿不到文档——这是**用「产出为空」换机制不可绕过**。

### 6.7 变更治理（v0.10，候选变更池 + 草稿预演）

**要解决的张力**：产品推进中一定会冒出新想法。既不能让一句话把已确认的结构原型全推翻（打断收敛），也不能让想法丢掉（灵感流失）。解法是**把「想改」和「真改」拆开**：想改 → 进池（零成本）；真改 → 提案 → 用户裁决 → 影响清单 → 回退重生成（显式代价）。

**变更提案协议（`artifact:backtrack`，LLM 只分诊不裁决）**：
```json
{"suggestion": "now | pool", "target": "clarify|structure|prototype",
 "idea": "新想法一句话概述", "category": "局部修订|页面流程|模块核心|目标范围|需验证",
 "impacts": ["02-structure/核心流程图.md · 支付模块"],
 "instruction": "用户具体诉求", "mode": "revise | redo"}
```
- **禁止 AI 替用户宣布「将执行回退」**——LLM 给一两句分诊意见 + 提案块，App 转成提案卡交用户裁决
- **默认进池（关键校准）**：全新功能 / 范围变化类诉求（超出既有澄清要点表范围）→ 默认 `suggestion: "pool"`——保住灵感、不打断当前版本收敛；仅当用户明确说「这版必须有」才 `"now"` + `target: "clarify"`（新需求先回澄清判断能不能做，**禁止静默并入本阶段产物**）
- **impacts 纪律**：`"now"` 时必填且必须引用具体产物文件与部位（文件名 · 模块/页面/章节）——**列不出具体影响的，说明这不是回退级变更**，应走本阶段正常修订（防「轻描淡写说小改动」）；`"pool"` 可省略；**App 侧按产物依赖图解析核验，引用不到具体产物的回退建议不采信（见 §6.10）**
- **mode 判定**：structure / prototype 目标——在上一版基础上改 → `revise`（App 把上一版注入作修订基底）；明确推翻重来 → `redo`（不带上一版、从零重做）
- 校准原则（写进 prompt）：把微调误判成新需求的代价小（多答一问），把新需求吞进修订的代价大（下游契约漂移）——**宁可多问**；但纯样式 / 文案 / 参数调整不是新需求，走正常修订
- 意图拿不准 → 反问，不输出提案块；只在用户消息引出新想法 / 新诉求时输出

**候选变更池与毕业仪式**：左栏「决策日志」整页设 **决策 / 变更池** 两分段；池内提案的处置现场即毕业仪式现场——**封板前池内每条提案必须逐条毕业处置**（纳入后续版本 / 放弃 / 顺延），否则封板被拦（`ProjectHomeView` 的 `pooledCounts` 拦截 + 引导先去处置）。动机：不留「说不清」的尾巴——封板意味着「这一版就这样了」，池里的悬案会污染下一版的起点。

**草稿预演（先试再定，`kind: "stage_draft"`）**：
- 会话可标记为草稿 → 该会话的产物**镜像到 `05-artifacts/proposals/<sessionId>/`**（`artifactRoot(proposalSessionId:)` 重定向槽位），**主线零写入**——试做不污染主线
- 「合并入主线」（`DraftMergeCard`）：对拷产物入主线 + 读活引擎推进主线状态机 + 提案置 `adopted` + 退出草稿 + 决策留痕；要求当前选中即目标版本（否则按钮禁用并引导切换——合并是版本级结构写）
- 「放弃草稿」→ 提案置 `dropped`，主线完全不受影响
- 草稿原型槽位扫描与主线一致（草稿也能生成原型、能预演完整链路）；两侧路径统一 `resolvingSymlinksInPath` 防 `/var → /private/var` 前缀错位

### 6.8 多会话并行与后台完成（v0.10）

**问题**：一个版本下常有多个会话在跑（主线推进、追问、单独调用某能力）。旧设计用一把全局单流闸把同版本的所有会话串成一队——用户在一个会话里等长文档生成时，另一个会话连问个问题都被卡住，这是为了防竞态把成本转嫁给了用户。

**忙判据分级（`SessionStore`）**：

| 判据 | 范围 | 拦截什么 |
|---|---|---|
| `isSessionBusy(sid)` | 会话级 | 本会话流式 / 占位中时，本会话的发送与问题卡动作 |
| `isVersionBusy(project, version)` | 版本级 | **仅该版本**正在生成时，改动版本级状态的动作（采纳、阶段推进、合并） |
| `isProjectBusy(project)` | 项目级 | 结构性操作（重命名 / 删除 / 切版本）防跨版本竞态 |

关键放宽：**他会话生成中不再阻塞本次采纳 / 发送**——只有「发起动作的会话自己正忙」与「同版本结构闸」才拦。版本级状态写入仍串行（按版本归属排队），但排队期间给可见状态 + **逃生门**（「不等了，只记账」立即按普通采纳处理并钉住排队坐标）。

**后台完成（`ReplyOrigin`）**：
- 用户切走后任务继续跑；流结束时回复按 **origin 会话**钉回并落盘（`saveCompactionPinsOriginSessionNotLiveSession`）——**不串到当前会话**
- 历史组装按 origin 会话组（`buildHistoryReadsOriginSessionEntriesFromDisk`），压缩缓存按会话分键
- 同版本内后台完成**照常触发闸口**；**跨版本**后台完成**不推进闸口**，落一条「需回发起会话处理」挂起行（`CrossVersionBackgroundCompletionDefersGateWithPendingLine`）——防「用户在 A 版本，B 版本悄悄推进了阶段」
- 并行会话的流态、停止生成、队列路由按会话隔离（`StreamBox` per-session）；`stopGeneration` 只停目标会话；历史投影按 sessionId 过滤

**存储层并发安全（`PMAgentStore`）**：
- **进程级写锁**：`ioLock` 串行化 jsonl 追加（`appendLineLocked`），并发追加不丢行
- **乐观并发校验**：`writeMeasured(expectedSHA:)` 写前校验内容指纹，失配抛 `ArtifactConflict` 且**不落盘**（防静默覆盖用户在 Finder 里的手改）；缺失文件带期望 SHA 同样报冲突
- **原型冲突分槽**：生成原型前比对磁盘现有原型指纹 → 被外部改写时新原型落「原型-修订-MMdd-HHmm」（同分钟自动 `-2` 递增防覆盖），**主槽位不动**，落用户可见说明行；用户可 `setAsMasterPrototype` 把修订版提升为基准（版本忙时拦截并提示）

### 6.9 决策档案（v0.10，decisions.jsonl 的确定性 HTML 投影）

**动机**：决策日志的原始 jsonl 是给程序读的（append-only、字段全、无排版）；用户要的是**能读、能翻、能给别人看**的东西。中间层用 LLM 转述是最差方案（每次生成结果不同、贵、可能失真）——正确解法是**确定性渲染**。

**机制**：
- 渲染 = **纯函数**（`DecisionArchive`）：零 LLM、零 token、毫秒级
- 产物两层：**按天档案** `decisions/YYYY-MM-DD.html` + **版本总览** `decisions/版本总览.html`（中文名——档案是「给人看的产物」，对齐产物命名规范）
- 写盘 = **内容比对幂等**：内容相同跳过（mtime 不动），变了才写；点「生成」则**无条件全量重建**（保证所有文件与 jsonl 当前状态严格一致）
- **HTML 不嵌生成时刻**：新鲜度由数据本身表达——否则内容比对永不相等，幂等失效
- 按天归组：决策按 `createdAt` 解析；`risk_hit` 历史行（无 createdAt）按文件序回退归日（上方最近决策的天），全无前驱 → 兜底天「未知」
- 渲染分流：**富话题卡**（有 `topicId`，含用户诉求 / 转折点 / 认知所有权）/ **记账卡**（风险应对·解除·池毕业——系统确定性写入，无话题字段，出专属形态）/ **简单卡**（真·历史记录，顶部挂降级说明）

**不变量**：档案是**派生产物，删了可随时重建**——`decisions.jsonl` 仍是唯一事实源。这是「删库可重建」向派生产物的同级推广（同索引、同 release-notes 复盘段的地位）。

**与写入时富化的配合**（见 §5.2）：富化字段（`userAsk` / `ownership` / 转折点）必须**在会话中采集**——事后 LLM 分析猜不出「这是用户原创还是 AI 建议被采纳」；所以采用「话题结束才写」（讨论过程中不写，累计到话题闭合一次性写全），上下文作缓冲区 + 多重兜底降丢风险。

### 6.10 产物依赖图与重规划循环（v0.10，自适应主线的「任务图」肌肉）

**动机**：自适应主线（四阶段骨架 + 任务图 / 变更影响分析 / 重规划循环）在 09-16 立项时识别出五个缺口，变更池与分诊层已落地（§6.7），剩下两块——**影响分析没有推理底座**（`impacts` 是 LLM 自由文本，与状态机硬编码的传播链互不相干）与**重规划不可见**（采纳即静默回退，用户看不到「将重做哪些产物」）。本节用一张确定性依赖图同时补上两者。

**图（`ArtifactDependencyGraph`，2-Orchestration，全纯函数零磁盘读取）**：
- **节点** = 真实产物：`ArtifactPath` 中文路径事实源（澄清要点表 / 功能架构图 / 核心流程图 / 业务流程图 / 模块-页面映射表 / 产品需求文档 / 竞品分析报告）+ **原型家族节点**（多槽位——默认槽 / 分端 / 方案槽——按 `03-prototypes/` 目录前缀归并，具体槽位文件 normalize 到家族）。
- **边 = 严格锚点，不是信息性参考**：`映射表 → 原型`（页面清单）、`核心流程图 → 原型`（流程基准）、`映射表/核心流程图/原型/竞品分析 → PRD`、`要点表 → 结构三件`。**功能架构图与业务流程图无下游**——它们启发生成但没有 1:1 锚定关系；② 阶段回退会整段重推它们，不需要产物级展开。这条取舍让「改了 X 会影响什么」的答案**宁可少报不虚报**。
- **下游闭包**：`downstream(ofSeeds:)` BFS——任一产物变更的连带失效集合，确定性可测。

**引用解析（impacts 自由文本 → 已知产物）**：具体引用优先（带路径全名 / 展示名词干如「核心流程图」「要点表」「原型」/ PRD 大小写不敏感），**目录级引用仅作回退**（「02-structure 整体重排」→ 整组；但「02-structure/模块-页面映射表.md · 登录」只命中映射表，路径里的目录前缀不污染）；模糊文本（「结构不太对」「整体体验有点乱」）解析为**空集——不猜**（宁可多问校准，§6.7）。

**消费点一：重规划循环（计划 → 执行 → 重生成 → 过期清除）**：
- 采纳变更提案（`adoptChangeProposal`）与回退坞（`requestBacktrack`，redo 语义）执行回退前，`replanSuffix(target:mode:)` 生成确定性计划——② 回退 →「功能架构图 → 核心流程图 → 模块-页面映射表 → 交互原型 → 产品需求文档」（revise：「依次修订重做」；redo：「推翻后依次重做」）；① 增补澄清链首为要点表（「依次更新」）；③ 回退仅「交互原型 → 产品需求文档」。
- **折叠进「🔄 已回到」行，不新发独立系统行**——新前缀会切断快速通道链游走判定（`isFastForwardChainedBefore` 按「🔄 已回到」前缀匹配，续段合并依赖它；bugs.md B004 同教训）。行前缀保真由回归测试钉住。
- **backfill（补做）不产计划**——被补做的阶段本无产物，无「失效重做」可言。
- **文案原则**（§9 v0.9.12）：计划只含产物中文名（与产物 chip 同词汇表），零内部路径——测试断言无 `/`、无 `.md`、无 `.html`。
- **与状态机的一致性**：引擎 `invalidateStructure/Prototype/Clarify` 的传播链是硬编码的，图是它的**产物级视图**——`replanSteps` 顺序与传播语义由测试互相钉住（② 连带原型+PRD、③ 仅 PRD），两处改一处漏一处即测试红。

**消费点二：impacts 纪律 App 侧落地（防轻描淡写）**：
- `verifiedTarget(target:impacts:)`：②/③ 回退建议必须至少引用一个可解析的具体产物；解析为零 → **不采信 target**（`presentChangeProposal` 降级为仅登记，想法级纳入走 ① 增补澄清兜底）；① 澄清目标豁免（增补澄清是对话式判断，不依赖产物级影响清单）。
- 这补上了 §6.7 提案协议注释里声明却未落地的校验——「无引用不得建议回退」从 prompt 纪律升格为 **App 侧强制**（prompt 只描述协议，不是安全边界——§4 刻意设计 8 的又一次落地）。

**不变量**：全纯函数、零磁盘读取、零 LLM——依赖图是确定性计算（§4 刻意设计 9），同一输入永远同一输出；影响分析的质量从此有下限（图覆盖），上限随产物目录扩展而增长。

### 6.11 内置 Function Calling——模型自主工具调用（v0.10.1，PRD §11 V2 路线首项提前落地；v0.10.2 扩至四工具）

**动机**：定位审计（2026-09-20）发现「工具调用决策权在编排层」与「产品经理全流程 AI Agent」定位有落差——模型本身无工具调用通道，工具/技能全部由系统侧语义命中与规则调度触发。本节给 LLMClient 接入 OpenAI 兼容 tools 协议，模型在对话中可自主发起工具调用。

**四工具（`2-Orchestration/AgentTools/`，`AgentTool` 协议 + 注册表）**：

| 工具 | 参数 | 执行 | 红线 |
|---|---|---|---|
| `load_skill` | `query` | 语义检索（与系统注入同源，`ContextBuilder.skillCatalog` + Retriever）→ `SkillLoader` 读正文回灌；命中计数同注入口径 | 本地只读 |
| `web_search` | `query` | `WebTool.search`（Tavily 兼容，与竞品分支共用配置）；未配置 endpoint/Key 返回人话提示让模型转告用户 | 只读；结果引用必须带出处 |
| `propose_competitive_analysis` | `query` | 提交既有分支确认卡（`submitBranchConfirmation`）即返回，不被用户裁决阻塞 | 用户裁决后才执行；封板版本拒绝 |
| `query_impact`（v0.10.2） | `seeds: string[]`（路径 / 中文名 / 带部位引用） | `ArtifactDependencyGraph` 纯函数（§6.10）：逐 seed 引用解析 + 下游闭包 BFS，按重做顺序回灌（含产物路径，供 impacts 按「路径 · 部位」形态编制）；未识别引用回菜单纠偏 | 本地只读、零磁盘零网络；不触碰确认 / 产物写入路径 |

**循环（`SessionStore.streamReply` 既有轮循环内）**：流末 tool_calls 分片累积非空（`LLMClient.accumulateToolFragments` 纯函数，兼容增量/全量两种端点形态）→ 逐调用执行（执行前后查 `Task.isCancelled` 保停止语义）→ assistant(tool_calls) + role=tool 结果消息 append 进局部 messages → continue 下一轮生成。**上限 4 轮**：超限后不再执行、以「已达上限」文本作工具结果回流并摘除 tools，强制模型直答（防失控兜底）。

**关键取舍**：
1. **工具消息循环局部不持久化**——不入盘、不进 HistoryProjection（对齐「动态材料」先例：discussions.jsonl 是闸门事实源，工具轮是生成过程而非用户裁决事件）；可追溯由思考卡工具行（`ThinkData.Step.tool`，随 assistant 条目落盘）承担。
2. **执行权在系统侧**（延续 §4 刻意设计 8「LLM 只请求、App 只裁决」）：模型只决定调不调 / 调哪个 / 传什么参数；工具路径禁写 confirmed.json / skipped.json / stale.json 与正式产物——闸口不变量不因 Function Calling 而破。
3. **anthropic-compat 优雅降级**：content blocks 协议与 OpenAI tools 形态不同，v1 调用侧单点判断不发 tools，行为与旧版一致。
4. **总开关**：`LLMSettings.agentToolsEnabled`（默认开），设置页「Agent 工具调用」一行；false = 请求不带 tools = 代码路径即旧版（一键回滚点）。
5. **失败自纠**：工具失败一律 ok=false 人话文本回流（不计抛错、不触发外层 ⚠️ 路径），模型读提示调整查询，同一查询不原样重试超过一次（prompt 纪律）。
6. **结果预算**：单结果回灌 4k 字符截断（`agentToolResultBudget`）+ 回合上限 4——工具轮不拉爆上下文。

### 6.12 阶段计划卡——plan-act-reflect（v0.10.2，阶段内模型自主规划）

**动机**：定位审计指出「规划、排序、影响分析全是确定性代码做的，模型只负责写内容」——阶段内生成是单次大生成，模型没有机会先规划再执行。本节给 ②③④ 生成/修订回合引入 plan-act-reflect 三段：**plan** = 回复最开头的 `artifact:plan` 块（对用户可见）；**act** = 既有产物块生成本体（产物与计划步骤对应）；**reflect** = 既有内建自评审（§6.2，对照计划逐项自查、fixed 记录实际修正）。协议文本进冻结段（静态文本，前缀缓存安全），不新增 LLM 调用轮（B001 教训：不加延迟）。

**协议（`AgentPrompts.planSection()`，注入 structure / prototype / prd；① 澄清是对话阶段不注入）**：

````text
```artifact:plan
{"mission": "本轮任务的一句话目标",
 "steps": [{"do": "步骤：做什么、产出什么", "basis": "依据（可省略）"}]}
```
````

**硬规则**：① 计划块必须是回复第一个产物块，产物块与计划步骤对应、计划外不凭空产出；② 常规 3-6 步、修订轮可缩 1-3 步，步骤具体可验收；③ **豁免明文化**——歧义反问 / 输出变更提案块 / 输出快速通道块 / 纯问答讨论轮一律不输出计划块（防仪式化，对齐「不硬凑」的反呓语立场）；④ 全部产物输出后按自评审对照计划自查。

**解析与渲染**：
- `ArtifactParser.parsePlan(blocks:)`：LenientJSON 容错解码 + 归一化（步骤 clamp ≤8、空步骤丢弃、mission 空白置 nil；无有效步骤 → nil）。**零新持久化**——计划块随回复原文落盘（discussions.jsonl 事实源），不另设台账。
- `PlanCardView`（ConversationView）：零容器降噪基调（DutyHandoverBar 同族：图标行 + 编号步骤 + 依据淡字），条目可拖选、不挂 compositing 修饰符（气泡拖选铁律）；**流式（StreamingContentBody）与落盘（assistantContent）双路径同位渲染**——先于正文出现（计划块在回复首部，展示顺序对齐「先计划后执行」语义）。进行中的 plan 块走既有 incomplete 裁剪通道（不闪 JSON 源码）。

**不变量**：计划是模型的自主决策输出、用户可见可核对（上下文透明），但**不改变闸口与产物闭环**——产物落盘仍走 write-then-verify + 机器门 + 确认坞；计划缺失/不合法时回复照常解析（计划是增强不是依赖，解析失败静默降级）。

### 6.13 Agent 主动简报轮（v0.10.2，Agent 主动发起回合）

**动机**：此前 Agent 是「被说一句话动一下」——风险只落台账行、池子只拦封板，都不开口。本节给 Agent 两个**主动触发点**：新风险登记后主动讲「为什么值得关注」、封板被池拦截时主动给处置建议。复用既有 `sendSystemTurn` 合成指令通道（note: nil 零新增系统行——B004 教训前置规避；合成指令不落盘为用户消息）。

**触发点一：主动风险简报（`scheduleProactiveRiskBriefing`）**：
- **触发**：`processCrossCutting` 漏项雷达处理中 `registerFatalRisks` 实际新登记 > 0（返回值由 Int 升为 `[RiskRecord]`——新登记记录即简报数据源；既有去重保证一条风险至多简报一次）。
- **内容**：`AgentPrompts.riskBriefingSystem/Task`——产品合伙人口吻，逐条点评（后果 + 建议处理）、指出最该先处理的一条、结尾引导右栏「风险」面板；≤6 句；**禁止输出任何 artifact 协议块**（简报轮刻意不带产物协议，违规输出由 `handleAssistantReply` 兜底横切处理）。

**触发点二：变更池催办（`schedulePoolGraduationBriefing`，ProjectHomeView 封板拦截分支接线）**：
- **触发**：封板按钮被「池内 pooled>0」闸拦下的同时。
- **内容**：`AgentPrompts.poolGraduationSystem/Task`——逐条给明确处置建议（纳入后续版本 / 放弃 / 顺延 + 一句理由，不和稀泥）、引导左栏「决策日志」裁决；≤8 句。
- **去重**：`poolBriefingSignatures`（project|version|池内提案 id 集）——同池内容不重复催办，池变化后再拦可再催。
- **会话锚点**：版本最近一条 assistant 回合所在会话（`gateOwnerSession` ① 口径——项目页发起、会话不在当前选中上下文）。

**共同纪律（防打扰优先）**：
1. **闸门即放弃，不排队不重试**：版本已封板 / 版本级忙（`isVersionBusy`）/ 快速通道进行中 / 采纳落实在途 / 发起会话忙（Task 起跑前再核一次，竞态兜底）——任一命中静默跳过。简报是增值不是必需：台账行与拦截弹窗已承载事实，丢一次简报无信息损失。
2. **走 sendSystemTurn 标准通道**：思考卡、流态、origin 钉回、followUp 消费全部复用既有语义；不带工具不带原型快照（`assembleSystemPrompt` 注入记忆，toolsSection 不开）。
3. **元信息禁令适用**：承接句用用户视角说法（「我在检查时发现/标记了」），不出现「系统/登记/自评审/拦截」等内部词。

---

## 7. MCP Server 与开放通道

### 7.1 MCP Server（stdio，异步任务模式参照墨刀）

| 工具 | 入参 | 出参 | 说明 |
|---|---|---|---|
| `analyze_requirement` | `idea: string` | `clarification: object` | 单独调用澄清能力 |
| `generate_structure` | `project, version` | `task_id` + 轮询 `get_task(task_id)` → 结构图路径 | 基于该版本澄清要点生成架构/流程图与映射表（须已有 clarification.md；生成后同样需过用户确认闸口才可被 `generate_prototype` 引用）；长任务异步 |
| `generate_prd` | `project, version` | `task_id` + 轮询 `get_task(task_id)` → PRD 路径 | 跑③④（该版本须已有**已确认原型**——03-prototypes/confirmed.json，否则报错并引导先完成③及确认闸口）；长任务异步 |
| `generate_prototype` | `project, version` | `task_id` + 轮询 `get_task(task_id)` → 原型路径 | 基于该版本**已确认结构**生成原型（须已有 02-structure/confirmed.json，否则报错并引导先完成②及确认闸口；生成后同样需过用户确认闸口才可被 `generate_prd` 引用）；长任务异步 |
| `review_doc` | `doc: string` | `review: object` | 独立毒舌评审任意文档（分支能力按需入口，评审意见落盘不阻塞主线） |
| `get_task` | `task_id` | `status / result` | 异步任务查询 |

- **入参缺省兜底（落盘规则，见 §5.1.1）**：`project` 未指定 → 落「默认」项目（`Projects/默认/`）；`project` 指定但 `version` 未指定 → 落该项目的 `unversioned/`（默认无版本号）目录。两条兜底保证 MCP 调用永不因缺参报错丢数据，产物事后可迁移归位
- **前置校验与跳步的交互（v0.10 待定，见 §15）**：`generate_prototype` / `generate_prd` 按主线顺序校验前置产物；而 UI 侧快速通道会让某阶段没有产物（只有 `skipped.json` 标记）——被跳过的阶段是否视为「已闭环放行」尚未裁决；当前实现以 UI 侧闸口为准，MCP 侧保持保守校验

- **传输：stdio**（本地进程）——Claude Desktop / Cursor 配置里直接指向 App 可执行文件；本地 stdio 下无远程鉴权面，App 内提供「复制 Claude Desktop 配置」按钮
- **实现**：modelcontextprotocol/swift-sdk（官方，已跟进最新协议）
- 异步任务持久化于 `mcp_tasks`；App 重启后 running 状态任务标记 failed，由调用方重试
- `generate_prd` / `generate_prototype` 执行时创建/复用对应版本的 `pipeline_runs`——MCP 与 UI 共享同一条流水线运行态，界面上能看到 MCP 触发的运行
- **演示胜负手**：你的 App 在后台当 server，Claude Desktop 在前台调用它——这一幕比任何口头解释都有说服力

### 7.2 导出通道（V2）

- **飞书**：Open API「导入 Markdown」接口（docx）
- **Notion**：官方 API，Markdown → blocks 逐段转换
- 触发：产物完成页手动按钮 / 封板时自动（可配置）

### 7.3 BYOK 模型配置

```yaml
# 存于 Application Support（Key 走 Keychain，不入库不入 Git）
models:
  classify:  { provider: "deepseek", model: "deepseek-chat" }   # 意图分类/路由（便宜）
  clarify:   { provider: "deepseek", model: "deepseek-chat" }
  structure: { provider: "deepseek", model: "deepseek-chat" }   # ② 结构图生成（Mermaid 源码）
  research:  { provider: "zhipu",    model: "glm-4.6" }          # 竞品分析分支
  prd:       { provider: "anthropic-compat", model: "claude-sonnet-4" }
  prototype: { provider: "anthropic-compat", model: "claude-sonnet-4" }
  review:    { provider: "openai",   model: "gpt-4o" }           # 仅可选独立毒舌评审分支用
embedding:   { provider: "zhipu", model: "embedding-3" }
```
全部走 OpenAI 兼容端点（`baseURL + apiKey + model`），支持 Ollama 本地。小模型做意图分类与 skill 路由，顺手吃掉「分层路由降本」考点 + 成本统计（每次/每月）。

---

## 8. 技术选型

| 维度 | 选型 | 一句话理由 |
|---|---|---|
| 平台 | macOS 14+，Apple Silicon 优先 | 平台已定死；本地优先让文件读写、skill 目录、MCP stdio、代码执行全部变自然 |
| 语言 / UI | Swift 5.9+ / SwiftUI 为主，AppKit 兜底 | 唯一已验证的栈；只用系统原生控件，白拿暗黑模式/动态字体/键盘操作 |
| 模型调用 | **先手写 URLSession.bytes + SSE 解析**；兜底 MacPaw/OpenAI（手写卡住超 1 天才启用） | 手写一次流式与 tool-calling 循环是本项目最值的两天；兜底分层限时，不许硬扛 |
| 编排 | **手写状态机**（Swift actor 承载状态） | 目标是干中学，用框架 = 把要学的东西外包出去；能画出自己的编排图才是面试答案 |
| 存储 | 文件系统（唯一真相源）+ SQLite（GRDB，仅索引） | 本地单文件，零部署零账号；索引可丢弃重建 |
| 向量检索 | MVP 内存暴力余弦 → 阈值（~2000 chunk）换 SQLiteVec | 量小不上向量库，本身是可讲的取舍；留接口到点即换 |
| 原型渲染 | 单文件 HTML + WKWebView 内嵌预览 | 零外部依赖、断网可用；系统原生组件，不引入浏览器内核之外的任何东西 |
| MCP | modelcontextprotocol/swift-sdk（官方），stdio | 本地 App + stdio 是 MCP 的默认原生形态 |
| 代码执行 | 本地 Python 子进程 + 超时控制（V2 ⑤ 数据分析分支） | Mac 端不需要云沙箱 |
| Git | 后台自动 commit 快照，操作串行化（互斥队列） | 同一版本同时只允许一条流水线运行，防并发写冲突 |
| 密钥 | Keychain | 已验证能力；不进 git |
| 分发 | GitHub 开源（clone 自构建）+ 录屏；签名公证（$99/年）后置为可选项 | 见 §10 |

**R1 分层兜底（重要）**：手写 SSE 卡住 → 换 MacPaw/OpenAI；MCP server 侧卡住 → 先只做 client 侧；向量绑定卡住 → 退回内存余弦。**任何一层不许连续硬扛超过 1 天。** Swift AI 生态的坑要在 W2 前抽半天提前踩（尤其 MCP server 能否被 Claude Desktop 发现——全案最晚暴露又最难补救的风险点）。

**非功能需求**：

| 项 | 要求 |
|---|---|
| 响应 | 首 token < 3s；检索 < 200ms（内存余弦毫秒级，余量充足）；**流式渲染量常数化**——界面每秒重渲染字符量不随输出长度增长（长文档不得越写越慢；bugs.md B001） |
| 离线 | 除模型调用与竞品分析分支联网外，主线四阶段全部可离线（结构图与原型断网可渲染——mermaid.js 打包进 App bundle，见 E15/E17） |
| 安全 | API key 存 Keychain、不进 git；无遥测；web_fetch 做 SSRF 防护；本地文件引用做路径越界拒绝 |
| 成本 | 单次全流程调用设 token 上限并记录实际花费（UI 成本统计：本次/本月）；**思考力度档位**（Low/Medium/High/Max）按调用类型自动适配——classify 等内部调用压 low |
| 可观测 | **每次检索打出 scope 与命中来源**（否则 E12 无法验证）；Context Builder 每次组装可回放（开发者模式检查器）；自评审对账逐轮可查（风险 Tab 计数器）；**识别事实与展示文本解耦**（块判定等在下发前用全量算好） |
| 数据完整性 | write-then-verify + **乐观并发校验**（`expectedSHA` 失配抛冲突、不落盘）+ 并发 jsonl 追加进程级写锁；派生产物（索引 / 决策档案 / 复盘段）**删了可重建**；长输出撞上限自动续写合并为一条回复 |
| 失败处理 | 限流 / 超时按退避重试并以人话说明（不暴露原始错误码）；流式连接有总时长上限保护；停止生成只影响目标会话且不残留错误行 |
| 并发 | 同版本多会话并行回合互不污染（会话 / 版本 / 项目三级 busy 判据）；切换上下文不丢进行中的任务（回复钉回 origin 会话）；跨版本后台完成不推进闸口 |

---

## 9. 界面设计

**形态**：单窗口三栏（`NavigationSplitView`）。左栏导航、中栏工作区、右栏 Inspector。不做多窗口、不做自绘控件。

| 界面 | 功能点 | 里程碑 |
|---|---|---|
| **左栏：项目/版本树 + 空间导航** | **任务 / 空间两模块分段胶囊**（v0.10：选中项驱动落段）——任务段 = 项目/版本/会话三级树（新建项目・项目切换即切 scope・版本切换（已封版带 🔒）・右键「在 Finder 中显示」）；空间段 = 功能导航整页（知识库 / 决策日志 / 技能库），**知识库带待处置数字胶囊**（聚光灯推荐数，v0.10） | M1 |
| **项目主页（版本驾驶舱）** | 目标收敛句卡片（可编辑）・长期约束列表・**否决项列表（红色）**・版本时间线・四阶段状态点 + 分支运行状态・**封板黄条置顶（存在已封板版本时，v0.10）**・**封板两段式确认**（第一段说明后果 + 一行复盘摘要，第二段最终确认）・**变更池毕业闸拦截弹窗**（池内有未处置条目时引导先去处置）・**版本对比入口** | M1 骨架 |
| **新建任务页（首页 / 开工台）** | 一句话输入框（大输入框 + 附件 + 场景胶囊）・**关联项目选择器**（不关联 = 落「默认」兜底 / 关联已有项目 → 落其 `unversioned/`）・**按时间问候 + 四意图建议入口**（写需求 / 看竞品 / 找方法论 / 继续上次，一键带入起点，v0.10）・**「面板」入口常驻**（与对话页同位同构）：右栏三 Tab 随关联驱动——文件 Tab 预览关联项目文件树（无产物空态）；未关联或任务未开始 → 各 Tab 空态（决策日志恒空：执行中才写入） | M1 骨架 |
| **工作区 ★** | 对话态（①澄清：流式吐字、**思考卡（思考中 spinner + 流光文字 → 完成后默认折叠「思考了 Ns · M 步 · 技能 ×K」，展开见推理步骤 + 技能调用行 + **多跳阶段时间线**，见 §6.4.1）**、**统一停靠卡（作答段 → 分支确认段 → 确认段同卡切换，v0.10，见下）**、**问题卡向导（多题逐答/回看/提交）**、**值班单交接条（续段段级计量 + 展开时间轴）**、**知识引用条（回答底部「参考知识卡」chip）** / 流水线进度态（②③④：阶段进度 + 单阶段重试 + **执行中提问暂停点**）/ 文档预览态（PRD、分析包 + **各阶段自评审摘要：发现/已修正/遗留**）/ **结构态（Mermaid 本地渲染 + 源码/预览双视图 + 「确认结构并进入原型」推进按钮，对话式改图展示变更差异）**・**原型态（WKWebView 内嵌渲染 + 「在浏览器打开」 + 「确认原型」闸口按钮）**・**回退坞（④ 阶段收起态虚线胶囊 → 展开「重做原型 / 重做结构」，v0.10）**・底：输入框 + **本地文件引用**（可把文件带进对话，v0.10）+ **「这条记下来」按钮** | M1 起 |
| **统一停靠卡（v0.10 ★）** | 输入框正上方**唯一一张卡**（同宽 720 居中列、brand200 描边、浮起影、xxl 圆角），按优先级切换段：**作答段**（待答问题，最高优先——多题问题卡向导 / 单题选项行）→ **分支确认段**（分支执行待用户裁决，防误触发）→ **确认段**（阶段闸口就绪）。段切换时卡体不重建、内容弹簧过渡；待答问题自动弹出一次，显式关闭后不再自动弹（触发 chip 兜底重开） | M2 |
| **记忆抽屉** | 当前注入的记忆列表（按**两档作用域**分组：项目 / 全局，含「经验」条目——带出处与置信度、假设态标签，v0.10）・⚑ 硬边界 / 假设标记・已失效记忆折叠・点「被谁覆盖」跳转高亮・记忆管理页（添加 / 编辑 / 整理 / 交接） | M4 |
| **技能库 ★** | 列表（name + when_to_use + tags）・搜索・正文预览（含实战注记时间线，与方法论卡同构）・**「学这个」/「用这个」双按钮**（学模式 V2）・命中历史计数・**竞品分析为首个分支技能（「用这个」即手动运行）** | M4 |
| **知识库（卡片库，独立整页）** | 列表 + 方法论名搜索（**语义检索收口于此，v0.10**——右栏知识点 Tab 已移除）・**卡片详情 = 正文（三段 + 为什么有效）+ 实战注记时间线（只增不覆盖）**・**相似卡片合并弹窗**（新建时触发）・来源跳转・**本阶段聚光灯推荐区（一次主推一张 + 处置结算条，v0.10）** | M4 |
| **版本回看** | 切到已封版版本后**全界面只读 + 顶部黄条提示**・同名产物并排 diff（v1.0 vs v1.1）・「封板」按钮（手动触发） | M3 |
| **决策日志（独立整页）** | **决策 / 变更池两分段（v0.10）**——决策段 = 档案生成区（**生成决策日志**按钮 → 按天 HTML + 版本总览，原始 jsonl 不再在 UI 投影）；变更池段 = **池内提案毕业仪式处置现场**（纳入后续 / 放弃 / 顺延）+ 草稿预演合并卡。数据源仍为 append-only 记录（**不可删除**）・按版本筛选・「待验证」未闭环高亮・另两处入口：产物「按阶段」视图弹窗 / 右栏 Tab（紧凑卡 + 展开五要素） | M3 |
| **风险台账（右栏「风险」Tab）** | **四态台账（v0.10）**：顶部**「N 轮对账 · 累计修正 M 处」自评审计数器**（逐轮可展开 ✅/❓/⏭️/💀 计数，对话流只报新增）＋ 三分区 **待处理 / 已挂方案·等验证 / 已处理·留痕**；每条卡 = 一句话假设 + **「后果」警示微标签** + 建议方案块 + 状态徽章（**待处理 / 已挂方案 / 已解除 / 已接受**四态人话）；动作 = **采纳方案（落实闭环：代发受理 → 生成执行包 → 转已挂方案）** / 接受风险 / 已解除 / 没解决（重开）；**执行包预览 + 「注入下一轮」**；**排队逃生门「不等了，只记账」**；操作失败落可见反馈行。工程口径（状态枚举 / 软上限 / 收敛）在开发者模式 ⌘D | M3 最简 |
| **右栏 Inspector（三 Tab，v0.10）** | ① **文件**（**分区台账**：上半区扫编号目录产物按四类归档——文档/原型/图表/报告 + 类型筛选 chips + 整行点击预览 + 右键「预览 / 添加到对话 / 设为主原型」；下半区**工作空间文件**档案树：版本目录全量内容、面包屑标注磁盘目录、与 Finder 口径一致；两区可拖分隔条调高、双击复位。「默认」项目与无产物项目为空态，不回退显示其他项目文件）② **决策日志**（紧凑卡 + 点击展开五要素；新建任务页恒空——执行中才写入）③ **风险**（四态台账，见上）。**知识点 Tab 已移除**——语义检索与推荐收口左栏知识库整页 ・**新建任务页亦常驻**（内容随关联项目驱动） | M4 第一版 |
| **检查器（开发者模式 ⌘D）★★** | **token 构成条**（规则/记忆/skill 正文/检索/历史）・**检索 trace**（query → 命中条目 + scope 标签 + 相似度）・**未命中 skill 列表**・**分支技能触发记录**（意图自动命中：命中了哪个技能、为何命中）・**主动推荐 trace**（推荐了什么/理由/采纳或拒绝/同阶段防重推荐）・**记忆校准注入 trace**（注入了哪条「经验」· 假设态标签）・**风险台账工程口径**（状态枚举 / 软上限与收敛 / 结算记录） | M4 第一版 |
| **设置** | API key（Keychain）・模型选择（每阶段）・**思考力度档位（Low / Medium / High / Max，默认 High，v0.10）**・token 上限・**成本统计（本次/本月）**・数据目录位置 + 「重建索引」 | M1 最简 |
| **MCP 状态** | server 开关・传输方式 stdio・已暴露 tool 列表・最近调用日志・**「复制 Claude Desktop 配置」按钮** | M5 |

**三条 UI 决策**：
1. **不做强制向导（wizard）**：用户随时想回到澄清、结构、原型阶段修改（回退后相关下游产物分级标记过期：结构改→原型/PRD 过期，原型改→PRD 过期）——顺序是默认建议不是硬约束，用「阶段切换器 + 状态点」；**v0.10 进一步**：用户**明确要求跳步**时可走快速通道直达下游（§6.6），确认坞本身也带跳步路径档
2. **只用系统原生控件**：自绘 UI 会吃掉本该花在 Agent 上的时间
3. **每个 AI 输出必须有来源入口**：卡片跳回出处、检索结果点开原文——信任的唯一实现方式；**v0.10 补充**：回答底部「参考知识卡」引用条把「本轮用了什么」提到对话内可见（不必开 ⌘D）

---

## 10. Eval 验收标准（42 条）

| # | 场景输入 | 期望输出 | 判定标准 | 失败模式 |
|---|---|---|---|---|
| E1 | 输入一句模糊需求「想做个签到功能」 | 澄清要点表（JSON Schema） | 5 字段齐全；5 轮耗尽走强制收束且 open_questions 非空 | 直接给方案；追问流于形式 |
| E2 | 项目内有历史文档后检索提问 | 命中相关段落并标注来源 | Top3 命中率 ≥ 70%（30 条人工标注样本） | 召回无关内容；不标来源 |
| E3 | 问「PRD 该包含哪些模块」 | 走**确定性路由**返回模板最新版 | 100% 命中当前版本，0 次召回历史版本 | 向量检索召回过期版本 |
| E4 | 技能库手动运行「竞品分析」分支 | 调用 web_search/web_fetch | 五要素齐全；工具被实际调用；返回含出处 URL；找不到标「未找到」 | 凭记忆编造 |
| E4a | 对话说「帮我调研下竞品」 | 意图自动命中分支技能并执行 | 命中记录可在检查器 trace（开发者模式）追溯（命中了哪个技能、为何命中）；主线状态未被阻塞或重置 | 意图误判（闲聊被当调研请求）；主线被破坏 |
| E5 | 产物落盘后立即回读 | write-then-verify 通过 | 写入后必有一次回读校验；不一致则报错 | 返回成功但内容未生效 |
| E6 | ④ 环生成 PRD 后 | 自动生成 5-10 条 eval | 每条 4 要素（场景/输入/判定标准/失败模式）齐全 | 只有场景描述 |
| E7 | Claude Desktop 通过本地 stdio MCP 调用 `generate_prd` | 正常返回 task_id 并可轮询到结果 | 外部 client 能发现并成功调用 | tool 描述不清选不中；stdio 握手失败 |
| E8 | ④ 环中途退出 App 再打开 | 恢复到中断前状态 | 运行态持久化生效 | 从头开始 |
| E9 | ① 环定下「不做多用户」 | 后续产物（原型/PRD）不包含多用户；用户改口则以新结论为准 | 记忆正确注入；**旧结论被覆盖而非并存** | 忘记早期约束；新旧结论同时生效自相矛盾 |
| E10 | 对话说「这条记下来」 | 弹窗**归属分流**后落对应层 | 弹窗含「方法论卡片 / 经验记忆」分流（默认判定可改）；方法论卡 4 字段齐全、**同一概念第二次触发时合并而非新建**；偏好/事实类默认分流至记忆层「经验」（带 source_ref + confidence） | 卡片是自由文本；重复建卡；「怎么做事」被错分进记忆层 |
| E11 | 提问命中「Kano 模型」skill | 只加载该 skill 正文 | 上下文中**未命中 skill 的正文不出现**（token 可测，开发者模式检查器可证） | 全量注入；命中错误 skill |
| E12 | 切到项目 B 提问 | 不提及项目 A 的约束/否决项 | 检索结果中 **0 条**来自项目 A（检索 trace 日志可验证） | 跨项目串味 |
| E13 | 在 v1.1 里回看 v1.0 的 PRD 与讨论 | 显示当时的原始内容 | 与封版快照**逐字一致**；决策日志能看到推翻记录 | 显示最新版本；历史被静默覆盖 |
| E14 | 决策写入 decisions.jsonl | 5 字段齐全的 append-only 记录 | 「待验证」条目在 UI 高亮；不可删除 | 自由文本；可编辑覆盖 |
| E15 | ② 结构确认后进入 ③ 环 | 生成单文件 HTML 原型并自动预览 | **零外部依赖（断网可渲染）**；P0 流程页面齐全且可点击跳转；**页面与模块-页面映射表一一对应**；覆盖澄清要点表的核心场景 | 引用 CDN 断网白屏；漏做核心流程；页面与映射表不符 |
| E15a | 未确认原型即触发 ④ PRD 环 | 拦截并引导回 ③ 确认闸口 | 确认后 PRD 功能需求与已确认结构（映射表）及原型的页面/流程一一对应，无凭空功能 | 绕过闸口；PRD 与结构、原型各说各话 |
| E16 | 任一主线阶段的产物生成 | 附自评审摘要并落盘 | 每轮产物附发现的问题/已修正项/遗留问题 + **漏项雷达四档声明**（四档缺一不可声明，💀可为空合法）；修正发生在产出当轮；连续 3 轮零修正触发自评审失效告警（pipeline_runs.self_review_fixes 可查） | 自评审空转（只出结论不修正）；漏项留给下游；💀凑数污染登记册 |
| E16a | 风险台账四态闭环运转 | 采纳与解除分离、结算点在确认门 | 登记必带 `impact`（后果）与 `plan`（建议方案），说不清后果不允许登记；状态只在 open / mitigating / resolved / accepted 四态流转，**采纳 ≠ 解除**（采纳后 mitigating，跨门核验通过才 resolved 并回写决策日志）；采纳走落实闭环（代发受理 → 生成执行包 → mitigating + 带证据指针决策日志；中断保持 open 可重试；降级「仅记账」必须显式告知）；排队期有「不等了，只记账」逃生门；活跃条目超 3 条执行收敛动作；封板时全部未处置条目统一结算；上游重做撞上未处置条目 → 重开回 open + risk_hit 对照；操作失败落可见反馈行（不许无声） | 💀每轮强制非空（为凑数而凑）；采纳即当解除（方案没落地就算数）；降级静默发生（用户以为方案已落实） |
| E17 | ① 澄清确认后进入 ② 环 | 产出结构产物（Mermaid 源码 .md）并落盘 | 功能架构图 + 核心流程图 + 模块-页面映射表齐全（复杂产品另含业务流程图）；图与澄清要点一致、无输入外新假设；App 内渲染断网可用（mermaid.js 本地打包）；Finder 手改 .md 后索引兼容 | 漏核心流程路径；引入澄清外新模块；只有图没有映射表 |
| E17a | 未确认结构即触发 ③ 原型环 | 拦截并引导回 ② 确认闸口 | 确认后原型页面与映射表一一对应；结构回退修改重新确认后，原型自动标记「已过期」且分级标注影响范围（局部/全部） | 绕过闸口；结构改了原型不联动 |
| E18 | 任一阶段产物落盘且自评完成 | 停靠卡确认段发起推进询问 | **单页直接确认**（选择处理方式 → 页脚按钮随所选选项直接生效，不做第二页摘要）；**选项按闸口分档**（① 进入结构 / 跳过结构直出原型 / 跳过中间阶段直出 PRD；② 进入原型 / 跳过原型直出 PRD；③ 进入 PRD / 到原型为止），跳步后对应阶段写 `skipped.json` 且不生成产物；不确认不推进；用户主动说「进入下一个阶段」等价确认；无强制弹窗；**每版本每阶段只自动弹出一次**（`confirm-silence.json` 版本级持久、跨启动跨会话）——稍后再说即该阶段永久静默（修订不重弹），推进由自由作答发起，静默修订轮 AI 回复末尾融一句邀请 | 阶段自动连跑用户无干预点；询问打断对话流；坞每回合弹回形成催办骚扰 |
| E19 | 新建任务页关联/切换项目（含不关联） | 右栏面板内容随关联实时切换 | 关联有产出的项目 → **文件 Tab** 显示**该项目**文件树；未关联或项目无产物 → 文件 Tab 空态；新建任务页决策日志 / 风险恒空态（执行中才产生数据，空态文案引导不填示例数据）；打开「默认」项目会话的文件 Tab 为空态，不回退显示其他项目文件；**开工台四意图建议入口**可用（写入输入框不自动发送） | 面板内容与关联项目不符；空态被硬塞示例数据冒充真实产物 |
| E20 | Agent 回复携带思考卡（含技能调用轮次） | 思考中：spinner + 「正在思考…」流光可见；完成：默认折叠为摘要行（思考了 Ns · M 步 · 技能 ×K），点击展开推理步骤与技能调用行 | 推理步骤为面向用户可读的摘要（非原始思维链）；技能调用行含命中技能名 + 触发词 + 注入内容 + 耗时，与开发者模式检查器 trace 一致（同一事实两个视图）；无思考的短轮次不强制显示空卡 | 思考卡常开挤占阅读动线；技能调用行与检查器 trace 对不上；原始 CoT 全文直出 |
| E21 | M1 骨架期 WKWebView 预搭验证 | 加载本地单文件 HTML（依赖全内联）成功渲染 | 断网加载无白屏、页面可交互；「在浏览器打开」兜底入口可用；本地文件沙箱/读取权限配置正确（E15 断网渲染的组件级前置） | WebView 接入问题（沙箱权限/断网白屏）拖到 M2 原型环才暴露；仅联机验证通过 |
| E22 | ④ 环进入时模板选档 + 生成到「数据指标」章 | 评分卡展示（三维度各 0-10 + 一句理由 + 所选档）→ 生成 PRD；数据指标章走四步对话式定义 | 评分依据锚定上游产物（理由须引用澄清要点/结构/原型的具体依据，非空话）；用户可一键切换三档任一且切换后按新模板生成；**「数据指标」章的数字 100% 来自用户输入（对话记录可溯），Agent 建议数值 = 失败**；无基线指标标「实施期建立基线」；跳过项入 open_questions 兜底；模板集 100% 确定性路由取最新版（与 E3 同口径） | 评分凭空（理由是「看起来复杂」类空话）；复杂产品被评成 lean / 简单功能被评成 full；Agent 编造「漂亮数」基线或目标值 |
| E23 | 阶段开始（如 ④ 环进入）+ 方法论被实际使用 | 聚光灯推荐；使用后卡片注记区新增一条 | 推荐 1-3 个含**理由**（引用项目特征或卡片注记，非空话）、可拒绝且**同阶段不再重复推荐被拒项**；推荐在**左栏知识库整页以聚光灯呈现**（一次主推一张 + 处置结算条 + 左栏待处置数字胶囊），推荐/采纳/拒绝在检查器 trace 可查；实战注记 append-only（只增不覆盖，带项目出处与日期，回读可验），AI 另代写一条具体案例草稿（失败静默）；使用方法论时记忆层该用户历史使用倾向被一并注入（trace 可证，假设态标签） | 无理由推荐刷存在感；被拒后反复推销；注记覆盖旧注记；方法论裸注入不做记忆校准 |
| E24 | 澄清阶段说「别问了，直接出 PRD」 | 受理跳步并直达 PRD | 不继续提问、不出问题卡、不生成当前阶段产物；被跳阶段写 `skipped.json` 且**不生成产物**（位置按闭环链重推导）；目标产物仍走模板选档 + 评分卡 + 确认坞；缺失信息按合理假设补齐并**在产物中标注为假设**；跳过阶段保留补做入口（补做时清跳过标记）；无跳步措辞的「生成 PRD」不出跳步（走前置确认分诊：未决项 1-3 → `prd_preflight` 卡，选完自动串链；≥4 → 照常回答 + 待确定清单）；**任何阶段 LLM 直接输出 `artifact:prd` 块不落盘**（只走系统两条路径）；AI 不得主动建议跳步 | 跳步被当成正常阶段任务执行；跳过阶段被伪造空产物；缺失信息被 AI 假装成已确认结论；LLM 绕过模板/评分卡直接产 PRD |
| E25 | 流程中途说「再加个消息通知」 | 出变更提案卡交用户裁决 | Agent 不直接改产物、不静默并入本阶段产物；先给分诊意见再出 `artifact:backtrack` 提案块；卡片含分类 / 建议回退目标 / **影响清单（具体到产物文件与部位）**；**建议回退但列不出具体影响即失败**（应改走本阶段正常修订）；新功能类诉求默认 `pool`，用户明确说「这版必须有」才 `now`（且先回澄清）；裁决 append-only 留痕（adopted / pooled / dropped / deferred） | AI 替用户宣布「将执行回退」；轻描淡写为小改动；新需求静默并入本阶段产物 |
| E26 | 候选池有未处置提案时点封板 | 封板被拦并引导先处置 | 池内提案可逐条毕业（纳入后续版本 / 放弃 / 顺延），处置完封板放行；草稿预演：草稿会话产物只落 `05-artifacts/proposals/<sessionId>/`、**主线零写入**，「合并入主线」执行对拷 + 推进主线阶段 + 提案 adopted + 退草稿 + 决策留痕，放弃草稿后主线产物与阶段状态完全不受影响；当前选中非目标版本时合并按钮禁用并引导切换 | 池内悬案被静默带过封板；草稿污染主线；合并后主线状态机没推进（产物与阶段脱节） |
| E27 | 磁盘上的原型被外部改写后再生成原型 | 冲突检出并降级落修订槽 | 指纹不一致 → 新原型落「原型-修订-MMdd-HHmm」修订槽，**主槽位不被覆盖**，并落用户可见说明行；同分钟多次生成自动 `-2` 递增防覆盖；可在文件 Tab 右键「设为主原型」（版本忙时拦截并提示）；**决策档案**：点「生成」产出按天 HTML + 版本总览，内容未变不重写（mtime 不动）、内容变了全量重建，删掉档案后可从 jsonl 完整重建 | 静默覆盖用户手改；修订槽相互覆盖；档案与 jsonl 状态不一致；幂等失效（每次生成都重写） |
| E28 | 封板时看本版复盘 | 一行摘要 + release-notes 复盘段 | 封板第一段确认弹窗给一行摘要（变更处置 + 回退次数与集中阶段 + 闸口 outcome）；release-notes 尾部持久化 `## 本版复盘（决策稳定性）` 段，含变更处置分布 / 回退分布 / 闸口确认 outcome 分布 / 方法论使用报告（采纳 N 张、被跳过 M 次、连续被跳过需校准的卡）；数据全部来自磁盘事实聚合（`changes.jsonl` + `events.jsonl` + `decisions.jsonl` 确定性 fold），**不经 LLM 转述、不虚构**；无复盘内容时不渲染该段 | 复盘由 LLM 转述导致数字漂移；空版本硬凑复盘段 |
| E29 | 同版本两会话，其一正在生成 | 另一会话不被阻塞、结果钉回原会话 | 另一会话的发送与风险采纳**不被阻塞**（仅发起会话自身忙与同版本结构闸拦截）；版本级状态写入按版本归属串行、不竞态；生成中切走到别的项目再回来，回复**钉回发起会话**（不串到当前会话）；同版本内后台完成照常触发闸口，跨版本后台完成不推进闸口并落「需回发起会话处理」挂起行；停止生成只停目标会话；并行会话历史投影按会话过滤、互不污染；并发 jsonl 追加不丢行 | 他会话生成中采纳被拒（旧全局闸回归）；回复串到当前会话；跨版本后台完成悄悄推进了闸口 |
| E30 | 输入框上方的停靠卡与问题卡 | 同卡分段切换、问题卡向导可回改 | 同一时刻只有**一张**卡，按优先级切换内容段（作答段 → 分支确认段 → 确认段），段切换时卡体不重建；问题卡向导支持多题逐答（单选 radio / 多选 checkbox + 自定义 + 跳过）→ 上一步回改（已答保留）→ 回顾页跳题修改 → 确认提交（拼装为一条用户消息进对话流）；单题选项行不限于澄清阶段；自动命中的分支在执行前由分支确认段请用户裁决，裁决完毕闸口状态自动回补不丢失 | 两张卡叠挂（旧设计的样式混乱）；答案回改丢失；分支误触发直接执行 |
| E31 | ④ 阶段点回退坞 | 回退 + 自动重生成 | 收起态为虚线小胶囊（不占空间、无文案），展开可选「重做原型 / 重做结构」（后果写在选项副标题，不用 tooltip）；执行即回退 + 自动重新生成，下游产物标记「已过期」；重做区分双模（revise 带上一版 / redo 从零重做）；跳过阶段可事后补做 | 回退入口不可发现；回退后要用户手动再点一次生成 |
| E32 | 一次消息触发多跳（判断 → 跳步/回退 → 重做） | 值班单交接条 + 开场承接 | 续段顶部渲染段级计量（进行态：计时 + 已生成字数；完成态：耗时 + 步数 + 阶段），点击展开链内交接明细时间轴，历史会话重开同样可见；⚡快速通道受理 / ⏳跨门风险提醒 / ✅阶段推进三类系统行**不单独渲染**，语义由 AI 当轮回答的开场承接句交代；静默行仍留痕并作链标记；无开场承接注入时 AI 不得自行播报状态变化；展开态思考流只渲染尾部窗口（渲染量常数化） | 系统行刷屏（状态播报淹没正文）；静默行语义丢失（用户看不到状态变化）；续段看不出自己在第几跳 |
| E33 | 记忆治理与知识卡富化 | 两档作用域 + 保护层 + 三段富化 | 记忆注入按**项目 > 全局**排序，version 只作溯源标签；约束与否决项（⚑ 硬边界）在预算裁剪时**永不丢**，假设类经验最先裁；失效条目不物理删除、留碑文可回链追溯；整理只允许白名单动作；**「提升为全局」是两池之间唯一流通通道**且需用户显式操作；新沉淀知识卡正文为三段结构（定义 / 做法 / 适用边界）+ `## 为什么有效` 原理段（旧卡兼容）；AI 回答底部「参考知识卡」引用行可点开卡片全文 | 版本作用域残留导致注入错序；约束被裁掉（后续产出违反硬边界）；整理动作改写内容；新旧卡格式不兼容 |
| E34 | 长回答撞输出长度上限 / 模型端限流 / 流挂死 | 续写合并、退避重试、挂死保护 | 撞上限自动续写并**合并为一条回复**（不出现半截回复或重复段落）；续写失败给明确截断提示与「重新发送」入口（点击找回原始消息）；限流 / 超时按退避重试并以人话说明（不暴露原始错误码）；流式连接有总时长上限保护；生成中可随时停止且只影响目标会话、不残留错误行；同一请求重复发送命中缓存快速返回、不重排 | 半截 PRD 交付；429 直接弹原始错误；挂死无限等待；停止后残留 ⚠️ 行 |
| E35 | 采纳引用了具体产物的变更提案（纳入当前版本） | 重规划计划随回退行可见化后执行 | 回退行内含**确定性重生成链**（② 回退 → 功能架构图/核心流程图/模块-页面映射表/交互原型/产品需求文档；redo 注明「推翻后依次重做」；① 增补链首为要点表），**文案只含产物中文名、零内部路径**；计划与引擎过期传播一致（② 连带原型+PRD、③ 仅 PRD，回归测试钉住）；计划**折叠进「🔄 已回到」行**、不新发独立系统行（链游走前缀保真）；backfill 补做不产计划；提案 ②/③ 建议引用不到任何具体产物（含模糊文本「结构不太对」）→ 不采信 target、提案卡降级仅登记（① 增补澄清豁免）；引用解析具体优先（「02-structure/模块-页面映射表.md · 登录」只命中映射表，目录前缀不污染整组）；任一产物变更下游闭包确定性可测（原型槽位归并家族节点） | 计划与实际传播脱节（图与状态机两张皮）；LLM 轻描淡写的影响清单被照单全收；计划行切断快速通道链游走（B004 复发）；文案露内部路径；模糊引用被猜成具体产物 |
| E36 | ②③④ 生成/修订产物的回合（v0.10.2 计划卡） | 回复最开头先出 `artifact:plan` 块再执行 | 计划卡先于正文渲染（流式与落盘双路径同位，PlanCardView）；步骤具体可验收（常规 3-6 步 / 修订 1-3 步）；产物块与计划步骤对应、无计划外凭空产出；歧义反问 / 变更提案 / 快速通道 / 纯讨论轮不出计划卡；① 澄清提示词不含计划协议；计划解析失败静默降级不阻塞产物落盘 | 计划块仪式化（每轮硬凑）；计划与产物两张皮（列了不做、做了没列）；流式中闪 JSON 源码；① 澄清轮也输出计划块 |
| E37 | 对话中问「改核心流程图会影响什么」/ 编制变更提案 impacts（v0.10.2 query_impact） | 模型自主调用 `query_impact` 拿确定性下游闭包 | 下游产物按重做顺序回灌（原型 → PRD）；未识别引用回菜单纠偏不猜；「功能架构图」等无下游产物如实报告无下游；impacts 编制提示落「路径 · 部位」形态；工具只读、不触碰确认 / 产物写入路径 | 模型凭印象列影响面（绕过工具）；未识别引用被编造成具体产物；工具结果截断丢关键下游 |
| E38 | 自评审新登记 💀 / 封板被池拦截（v0.10.2 主动简报） | Agent 主动发起简短简报回合 | 风险简报：逐条点评（后果 + 建议处理）+ 指出最该先处理的一条 + 引导右栏风险面板（≤6 句）；池催办：逐条给明确处置建议（纳入后续 / 放弃 / 顺延 + 理由，不和稀泥）+ 引导左栏决策日志（≤8 句）；零新增系统行前缀（复用 sendSystemTurn note:nil）；同池签名去重（池变化才再催）；闸门命中（封板 / 版本忙 / 快速通道 / 落实在途 / 会话忙）即静默放弃、不排队不重试；简报禁止输出任何 artifact 协议块 | 简报刷屏骚扰（重复触发 / 排队轰炸）；简报轮输出协议块触发跨切处理；漏内部术语（「系统登记了风险」）；闸门不满足仍强行发起打断生成 |

---

## 11. 里程碑（6 周）

| 阶段 | 周期 | 交付 | 验收（可演示） |
|---|---|---|---|
| M1 骨架 | 1 周 | SwiftUI 三栏骨架 + Keychain + 手写 SSE 流式对话 + 项目/版本容器（真实文件夹 + 目录结构定死）+ 新建任务页（关联项目选择器 + 面板入口，四 Tab 可空态占位）+ **WKWebView 预搭**（组件接入 + 本地单文件 HTML 加载渲染 + 「在浏览器打开」兜底，E15 断网白屏风险的组件级前移）+ 设置最简 | 一句模糊念头 → 流式澄清对话；产物落进 `Projects/项目名/v0.1/` 且 Finder 可打开；E19 通过（面板随项目关联切换）；**E21 通过（WKWebView 断网加载本地 HTML）** |
| M2 澄清+结构+原型 | 1.5 周 | 澄清 Agent（多轮+要点表+强制收束）+ **结构 Agent（Mermaid 生成 + mermaid.js 本地渲染 + 确认闸口）+ 阶段推进确认** + 原型 Agent（HTML 生成 + WKWebView 预览（组件复用 M1 预搭）+ 确认闸口，以映射表为页面清单）+ 记忆层最小版 | E1 / E15 / E17 / E17a / E18 通过；**第二轮对话不需要复述第一轮结论** |
| M3 PRD+版本+调研分支 | 1.5 周 | PRD Agent（**分级模板集确定性路由 + 三维度评分选档（评分卡透明可切换）**、以已确认结构与原型为双重基准、**数据指标四步对话式定义**）+ **各 Agent 内建自评审约束（漏项雷达四档 + 决策 WHY 记录）** + **风险登记闭环（risks.jsonl + 结算 + 封板终态）** + 状态机串联（含过期传播分级标记）+ 决策日志 + 封版快照 + 竞品分析分支技能（手动运行 + 意图自动命中） | E3 / E4 / E4a / E5 / E6 / E8 / E9 / E13 / E14 / E15a / E16 / E16a / E22 通过 + 1 份完整 PRD + 1 份已确认可点击原型 + 1 份已确认结构图 + 1 份竞品分析包 |
| M4 横切层 | 1 周 | 知识与技能层（同层双库：**方法论卡抽取 + 去重合并 + 「记下来」归属分流 + 实战注记 append**；技能渐进式披露+灌入方法论）+ **方法论主动推荐 + 记忆校准注入** + 检索（内存余弦 + scope 过滤）+ **右栏四 Tab 面板（产物/决策日志/漏项雷达/知识点——含推荐卡与注记条数）+ 开发者模式检查器第一版（含推荐/校准 trace）** | E2 / E10 / E11 / E12 / E23 通过 + 拿出「向量 vs 确定性路由」对照数字 |
| M5 MCP+收尾 | 1 周 | MCP server（stdio，6 工具，异步任务）+ 双语 README + 3 分钟录屏 + 5 分钟演示脚本 | E7 通过 + Claude Desktop 现场调用成功 |

**M5 期间实测迭代（2026-09-13 ~ 09-18，v0.10 回填）**：计划外的能力补强，全部有代码与测试证据——**快速通道**（§6.6）+ **变更治理与草稿预演**（§6.7）+ **多会话并行与后台完成**（§6.8）+ **决策档案**（§6.9）+ **产物依赖图与重规划循环**（§6.10）+ **封板复盘** + **风险四态台账与采纳闭环** + **统一停靠卡与问题卡向导** + **右栏三 Tab 收敛与文件分区台账** + **记忆两档治理** + **知识卡写时富化** + **流式性能三刀**（bugs.md B001）。Eval 相应扩至 39 条（E24 ~ E35）。这些改动没有扩大里程碑范围，而是把「实际做出来比计划更完整」的部分补回文档——**文档滞后于代码是本次回填的直接原因**。

**演示脚本（W6 必产出，5 分钟）**：模糊念头输入 → 澄清追问 → **结构一键生成（Mermaid 当场渲染，现场对话改一张图看变更差异——展示图是比原型便宜得多的干预点）→ 阶段推进确认** → **原型一键生成（WKWebView 当场渲染，页面与映射表一一对应，可现场断网演示）** → 用户确认闸口 → 调用一条方法论 skill（⌘D 切开发者模式检查器，展示只加载了这一条）→ PRD + eval 生成（先亮**模板选档评分卡**——三维度分数与依据、可一键切换，一个「上下文透明可解释」的实证点；再展示与已确认结构及原型一一对应 + **自评审摘要：当轮发现并修正了什么**）→ 说「帮我调研下竞品」**意图自动命中分支技能**（带出处）→ 说「这条记下来」当场演示**归属分流**并生成方法论卡（含实战注记条数） → **手动触发独立毒舌评审分支拿第二意见**（展示不阻塞主线）→ **切到 Claude Desktop 用本地 MCP 调用同一能力**（App 在后台当 server）。**每一步停 10 秒说一句技术决策**——演示的胜负手不是流程有多长，而是这 10 句技术决策说得有多准。

**深度分级（防环环都浅的唯一活路）**：横切层 + ②③④ 主线阶段 + 竞品分析分支必须有深度（技术决策可讲）；① 阶段对话体验与 ③ 原型视觉允许糙（灰盒线框风）。超时按门禁砍深度而非砍阶段。

**三条硬门禁（防延期纪律，是方案的一部分，不是建议）**：

1. **纸面封顶**：本页是唯一允许存在的设计文档。M1 跑通前不许再写第二份 PRD、路线图或调研页——想写就是逃避写代码的信号，当日必须提交一次可运行 commit
2. **时间盒**：任一里程碑超出预估 1.5 倍，**强制砍深度，不许延期**（v0.4 原型预算债已预先选定降级候选，见 §12——届时不需要重新做决定，只需执行）
3. **可演示验收**：每个里程碑的验收标准都是「能给别人看到的东西」，不是「代码写完了」

**M0 验收线**：M1 结束若最小闭环（模糊念头 → 流式澄清 → 产物落进真实文件夹）未跑通，正确动作不是调整计划，而是承认「现在不该做这件事」——**M1 是唯一真正重要的里程碑，这一步不通过，后面全部作废**。

---

## 12. 风险与对策

| 风险 | 对策 |
|---|---|
| **卡在 Swift AI 生态而非业务逻辑** | R1 分层兜底，每层限时 1 天（见 §8）；MCP server 侧 W2 前提前试通 |
| **原型生成范围蔓延（v0.4 新增，约 +3~5 天预算债）** | 执行到 M3 时若超时 1.5 倍，按门禁纪律从 M3/M4 **降级等量内容**（候选：卡片库 UI 简化、决策日志 UI 后置、学模式已在 V2），**砍深度不延期** |
| **结构阶段预算债（v0.8 新增，约 +2~3 天）** | Mermaid 源码即产物（无需图形编辑器，渲染用打包 mermaid.js）；M2 超时 1.5 倍按门禁砍深度（候选：业务流程图仅复杂产品出、映射表并入架构图注释） |
| **结构阶段拖慢简单产品 / 图被过度打磨** | 粒度自适应（简单产品轻量图，复杂产品全套）；图定位为脚手架——PRD/原型才是交付物；推进询问「稍后再说」不阻塞 |
| **HTML 原型质量不可控**（引用 CDN / 页面破碎 / 漏流程） | 「单文件零外部依赖」写进 ③ 环 prompt 硬约束 + E15 断网渲染验收；生成失败重试 1 次后降级为纯线框 |
| **调研工具在国内网络环境不稳定** | 工具层支持可配置搜索源（Tavily/Bing/自建）；失败降级为「用户提供资料」 |
| **对话意图误触发分支技能**（闲聊被当调研请求） | 自动命中前在 UI 展示将执行的技能、一键确认（可在设置关闭）；误执行产物可删，不污染主线状态 |
| **自评审流于形式（生成与评审同体的「灯下黑」）** | 清单化自检（维度固定不可自选）+ 自评审摘要落盘可查（E16）+ 连续零修正告警；独立毒舌评审分支保留外部第二意见入口 |
| **💀 凑数 / 悬空（Goodhart 病：告警指标逼模型每轮凑致命漏洞）** | 💀可为空明文合法（无真实致命假设只许写一句「最脆弱的假设是 X」）；登记必带「后果」与建议方案（说不清后果不准登记）；结算点固定在阶段确认门（跨门批量核），不靠 AI 逐轮自扫——凑数刷不出真实结算（E16a） |
| **流式渲染瓶颈伪装成「模型慢」（v0.10，bugs.md B001，实测踩过）** | 症状：长文档轮 15–30 分钟、酷似端点限流。根因是三层 UI 渲染瓶颈叠加（@Published 字典每 delta 触发整页重渲染 / 流式 Text 全量重排 / 展开态思考流每次发布重排尾窗）。对策三刀：**per-session StreamBox 拆盒**（增量只触碰盒自身）+ **发布尾窗化**（text 12000 / think 4000 字符，全量态照常累积落盘）+ **识别与展示解耦**（块判定/进度等事实在发布点用全量算好、结构化下发，视图只渲染不解析）。纪律：**流式 UI 新增任何渲染路径时自问「每 delta 重排多少字符」**，渲染量必须常数化 |
| **静默覆盖用户手改的文件** | `writeMeasured(expectedSHA:)` 乐观校验——指纹失配抛冲突、不落盘；原型冲突降级落「原型-修订-MMdd-HHmm」修订槽、主槽不动（E27）；并发 jsonl 追加用进程级写锁串行化 |
| **竞态：多会话并行下的状态错乱** | busy 判据分级（会话/版本/项目），版本级状态写入按版本归属串行；引擎 `deriveStage` 可从磁盘重建阶段位置（confirmed / skipped 标记重推导），阶段状态不靠内存缓存；并行回合互不污染（E29） |
| **横切层做成假的**（记忆全塞回去 / 卡片没去重 / skill 全量注入） | E9 / E10 / E11 是横切层的存在证明，通不过等于没做；开发者模式检查器让「没发生」可观测 |
| **作用域泄漏：跨项目记忆污染** | scope 过滤写在**检索层**而非 prompt 层；每次检索打出 scope 与命中来源（否则 E12 无法验证） |
| **LLM 结构化输出不稳定** | JSON Schema + 失败重试 1 次 + 降级正则提取 |
| **web_fetch 安全面** | SSRF 防护（禁内网/本地地址 + 超时限制）；`project`/`version` 入参做路径规范化校验，防目录遍历 |
| **Git 并发写冲突** | Git 操作串行化（互斥队列）；同一版本同时只允许一条流水线运行 |
| **换栈诱惑**（W3-W5 最强） | 这是本项目允许的最后一次栈级变更（TS→Swift 已用过）。撞坑时执行 R1 分层兜底，不重开项目 |
| **纸面复发**（写文档不写代码） | 本页是唯一设计文档；M1 跑通前不再新建任何设计页；当日必须提交可运行 commit |
| **拿不到可点开的链接** | GitHub README（截图+架构图）+ 3 分钟录屏为主载体；面试优先屏幕共享现场演示；公证（$99/年）确有需要再办，提前两周 |

---

## 13. 开源与合规

- **协议**：MIT
- **README**：中英双语，含 30 秒 GIF 演示、架构图、clone 自构建指南（Xcode 打开 → 填 Keychain → Run）
- **数据主权**：所有内容本地 Markdown + Git，索引可重建，导出走开放格式
- **借鉴边界**：app-maker 仅借鉴架构模式（AGPL，代码不抄）；墨刀 MCP 仅参照接口设计（公开文档）
- **分发**：GitHub 源码为主；签名公证为可选后置项（见风险表）

---

## 14. V2 可插拔阶段（不动架构）

| 阶段 | 内容 | 前置 |
|---|---|---|
| ⑤ 数据分析 | CSV 手传 + 本地 Python 子进程计算（数字可复算、结论与数字分离标注不确定性）；以技能/插件分支接入，沿用竞品分析分支模板，不进主线 | Code Interpreter |
| ⑥ 迭代管理 | 基于报告开新版本（继承约束与否决项，不继承讨论） | 版本容器（MVP 已建） |
| ⑦ 复盘 | 根因分析 + 规则变更建议（回写技能库/规则库 + risk_hit 回写条目即预测 vs 实际的复盘素材，决策日志飞轮）——**封板盘点已落地（`ReleaseRetro`，见 §5.4）**，本项指其深化 | 决策日志、风险台账 |
| 知识反哺另两阶段（v0.10 标注） | ① **开工档案袋**：上版复盘素材在本版开工时注入；② **产物足迹**：产物在后续阶段被引用的链路视图。封板盘点已落地，这两项待做 | 封板复盘 + 决策档案 |
| 原型编辑画布 / 墨刀同步 | Figma 式精细编辑不做；墨刀 MCP 作为可选导出通道 | MCP client |
| 技能「学」模式 | 同一份 skill 正文给讲解器：解释+例子+练习+复述，落成方法论卡片（含实战注记） | 技能库 |
| 导出 | 飞书 / Notion | — |

---

## 15. 待用户确认的开放问题

1. **技能库初始内容边界**：哪些 AI 产品 / 方法论进技能库（阻塞 M4，W4 前必须定）→ **已有裁决稿**：skills-inventory.md v1.1（首批 14 个 + V2 分级候选池；front-matter 六字段 schema 与本文 §5.2 对齐）
2. **搜索源**：调研 Agent 的 web_search 默认用哪家？（Tavily 免费 1000 次/月，推荐起步）
3. **签名公证**：是否花 $99/年办 Apple Developer（影响能否发未拦截的构建包，M4 前决定）
4. **产品名**：暂用 PM Copilot，待定
5. **快速通道跳过的阶段与 MCP 前置校验的交互（v0.10 新增）**：MCP 按主线顺序校验前置（如 `generate_prd` 需已确认原型），而跳步会让某阶段没有产物——被跳过的阶段应视为「已闭环放行」（读 `skipped.json`），还是仍要求补做后才允许 MCP 调用？前者与快速通道语义一致，后者更保守（待定）
6. **知识卡结构口径统一（v0.10 新增）**：抽取侧强制「定义 / 做法 / 适用边界 + 为什么有效」，展示侧另有「是什么 / 为什么现在 / 怎么用 / 案例」四段口径——两者需收敛为一套，避免同一张卡在不同入口长得不一样（待定）

---

## 附录 A：干中学技术映射表（面试价值核心）

每一行回答三件事：这项技术在产品里**为什么必需**（不是硬塞）、**真实技术决策**是什么、对应哪条 Eval。覆盖面来自「每项技术各占一个必要环节或横切层」的架构自然结果，不是技术清单堆砌——**这是面试里「为什么全但不臃肿」的答案**。

| 技术 | 落点 | 必要性（真实约束） | 关键决策 | Eval |
|---|---|---|---|---|
| Prompt / 上下文工程 | 全阶段基座 | 规则 + 方法论 + 历史远超窗口，必须分层取舍——这个约束是真的，不是设计出来的 | system 三层拆分（全局约束/角色/场景）、token 预算分配、用户输入注入防护 | E1 |
| RAG | 竞品分析分支 + 卡片库 | 历史文档要被检索；卡片库每天产出新卡写回索引——**读写双向 RAG，不是只读 demo** | 分块策略、embedding 选型、哪些内容**不走** RAG（规范文档走确定性路由——过期规范比没有规范更危险） | E2 / E3 |
| Tool Calling | 竞品分析分支 + 全部落盘 | 分支调研要联网、产物要写文件、对话意图要能触发工具 | 工具描述怎么写才选得对、意图自动命中（E4a）、write-then-verify（写入后回读，防假成功）、超时重试 | E4 / E4a / E5 |
| MCP | 对外接口层 | 别人不一定用你的 UI——在 Claude Desktop / Cursor 里直接调你的能力，这是「别人为什么用你」的真答案 | 三原语划分（tool/resource/prompt）、stdio vs HTTP（本地 App 选 stdio）、异步任务模式（参照墨刀） | E7 |
| Workflow | 四阶段编排 | 阶段有确定性顺序、结构与原型要过用户确认闸口（HITL 现场）、每阶段推进要过阶段推进确认、质量靠各 Agent 内建自评审在产出当轮保证——这正是 Workflow 与 Agent 的分界现场 | 手写状态机（不用 LangGraph）、结构与原型确认闸口（confirmed.json + pipeline_runs 状态位）、阶段推进确认、过期传播分级标记、内建自评审约束与摘要落盘、运行态持久化 | E8 / E15a / E16 / E17a / E18 |
| Eval | ④ PRD 环 | 产物和验收标准必须同时生成，否则又是无验收的交付 | eval 4 要素 schema（场景/输入/判定标准/失败模式）、判定标准可转测试用例 | E6 |
| Memory | 记忆层 | 跨会话跨阶段，用户不可能每次复述；缺了它，主线各阶段=几个互不相干的聊天窗口 | 写入判据（结论/约束/否决项进，寒暄与中间推理不进）、**新结论覆盖旧结论不并存**（过期记忆比没记忆更危险）、注入位置与 token 预算 | E9 |
| 结构化抽取 | 卡片库 | 卡片不是自由生成，是 schema 约束下的抽取——否则无法去重、检索、累积 | 卡片 schema、手动「记下来」vs 自动抽取的打扰权衡、去重合并（>0.92 合并；**没有去重的知识库三个月后就是垃圾场**） | E10 |
| Skill 渐进式披露 | 技能库 | 方法论会到几十上百条，全量注入物理上不可能 | 元数据常驻（name/when_to_use）+ 命中后加载正文；小模型路由；「学/调」双模式共用同一份正文（学模式 V2） | E11 |
| Code Interpreter | V2 ⑤ 数据分析分支 | 模型心算数字必错是原理性问题，不是能力问题 | 计算下沉到本地 Python 子进程、数字与结论分离、可复算（误差 0） | （V2） |
| 作用域隔离 | 检索层 | 同一句「不做多用户」在项目 A 是硬约束、在项目 B 完全无关——检索不带 scope 必然串味 | scope 过滤**必须在检索层**（版本>项目>全局，窄的赢），不能靠 prompt 叮嘱模型自觉 | E12 |

**跨场景识别同一模式**（L2→L4 的分水岭）：「权限过滤在检索层」（RAG，E2）与「作用域隔离在检索层」（记忆/多项目，E12）是**同一原理的两次应用**。面试时主动指出这一点，证明你理解的是原理而不是招式。

---

## 附录 B：决策 WHY 记录

每条含 WHY / 排除方案 / 置信度 / 待验证——决策日志数据模型（§5.2）的产品化原型即此结构。

**决策 1：技术全覆盖，但每项绑定一个必要环节或横切层**
- WHY：目标是干中学，覆盖是学习目标本身；但「为证明能力而堆技术」在面试里是负分。绑定必要环节可同时满足覆盖与取舍论证
- 排除：只做单环节纵深 demo（不满足学习目标）／技术并列堆砌无环节绑定（答不出「为什么需要 MCP」）
- 置信度：中。架构合理，6 周能否跑完取决于 Swift AI 生态踩坑深度与砍深度纪律
- 待验证：M1 是否真跑通；每项技术是否真做了决策而非调用了现成库

**决策 2：macOS 原生 Swift/SwiftUI，而非 Web（含一次自我推翻的记录）**
- 推翻记录：原判 TS + Next.js，核查后发现事实前提有一半是错的——MCP 有官方 Swift SDK、sqlite-vec 有 Swift 绑定。仍然成立的只剩一条：Web 能给可点开的链接，Mac 不能（用录屏 + 屏幕共享补偿）
- WHY：唯一已验证的栈，决定成败的是开工摩擦不是生态丰富度；MCP stdio 在本地 App 是原生形态；无部署无冷启动，演示故障面积小一个数量级
- 排除：Next.js（TS 水平未知，6 周不容许顺便学语言）／Tauri/Electron 套壳（两头成本相加）／SwiftUI 外壳 + Node 侧车（两套进程管理）
- 置信度：高。最大未知项（生态踩坑深度）可用 R1 分层兜底限时化
- 待验证：Day 1 能否手写出流式吐字；MCP server 侧 W2 前试通；**这是本项目允许的最后一次栈级变更**

**决策 3：手写状态机，不用 LangGraph/LangChain**
- WHY：用框架 = 把要学的东西外包出去。手写多花 2-3 天，换来能画出自己的编排图——「Workflow vs Agent」那道题的最佳答案形态
- 排除：LangGraph（黑盒化编排，学不到判据）／无编排纯长 prompt（无法持久化与 HITL）
- 置信度：高
- 待验证：M3 内完成；超时 1.5 倍砍状态持久化、保主线四阶段顺序执行

**决策 4：知识与技能「同层双库」，而非两个独立横切层或混成一个库**
- WHY：两者共用全部基础设施（索引、scope 过滤、Context Builder 收口、trace 可观测），拆成两层是重复建设；但内容形态与检索机制必须分离——技能是方法论的可执行形态（被调用，渐进式披露），卡片是方法论的知识形态（被检索，top-k，v0.9.10 起与「是什么」类讨论萃取物分家，见决策 15）
- 排除：两个独立层（基础设施重复实现）／混成一个库一个检索机制（该执行流程时拿到一堆概念解释，该查概念时执行了错误方法论）
- 置信度：高
- 待验证：双库各自检索的 trace 在开发者模式检查器里是否清晰可分辨（E11 面板顺带验证）

**决策 5：迭代日志不搬语料，改做决策日志数据模型**
- WHY：直接搬只是把 20 条记录变成语料；做成数据结构后每次决策自动产生同构记录，构成数据飞轮——这才是产品级复用
- 排除：搬进向量库当语料（检索出来的是别人的历史决策，对当前场景价值低）
- 置信度：中高
- 待验证：用户是否真愿意填 WHY 与置信度；填写率低则改为 AI 自动草拟、用户只确认

**决策 6：技能库渐进式披露，而非全量注入或纯向量检索**
- WHY：方法论超出窗口，全量注入物理不可行；纯向量会命中语义相近但流程不同的 skill，误命中代价大（执行错的方法论）
- 排除：全量注入（token 爆炸 + 指令互相干扰）／纯向量检索（误命中）——代价是需要人工写 when_to_use 描述
- 置信度：高
- 待验证：skill 超 30 条后命中率是否下降，是否需要加小模型分类层

**决策 7（v0.4）：原型自研 HTML + WKWebView，而非墨刀 MCP 或不做**
- WHY：墨刀依赖外部云服务与账号，与本地优先、开源叙事冲突；自研断网可用、零外部依赖，且生成过程本身可被 write-then-verify 与内建自评审约束
- 排除：墨刀 MCP（云端依赖，降为 V2 可选导出通道）／不做只归档（用户拍板要生成；+3~5 天记为预算债，按门禁纪律消化）
- 置信度：中高。结构简单（LLM 产单文件 HTML），风险在质量可控性
- 待验证：E15 断网渲染的一次通过率；失败重试 1 次后降级纯线框是否可接受

**决策 8：MVP 不上向量库，内存暴力余弦**
- WHY：初始语料几百到一两千条 chunk，暴力余弦毫秒级；为不存在的规模先付复杂度是反模式。「因为量小所以不上」比「我用了向量库」更能证明工程判断力
- 排除：直接 SQLiteVec（中期最可能卡住的地方）／云端向量服务（违背本地优先）
- 置信度：高
- 待验证：约 2000 chunk 时延迟是否可感知——**阈值与接口现在写死，到点即换**，这个「阈值 + 接口」本身是演示时要指给对方看的东西

**决策 9：产物不可变封板快照 + 讨论决策 append-only 事件流**
- WHY：两类内容诉求相反——看 v1.0 的 PRD 要「当时那一份，逐字不变」；看「为什么 v1.1 改了方案」要的是变更过程
- 排除：全部用快照（看不到为什么变，PM 最值钱的部分丢了）／全部用事件流（回看要重放日志，慢且易错）／原地编辑覆盖（E13 必挂）
- 置信度：高（版本控制系统反复验证过的混合结构）
- 待验证：封板手动触发是否够用——倾向手动，自动会在用户还想改的时候锁死内容

**决策 10：项目文件夹是用户可见的真实目录，不是 App 内部黑盒**
- WHY：Finder 可打开、可拖进 Cursor、可自己 git init、可 Time Machine 备份——数据看得见 = 信任成本骤降，「你的东西存在你自己硬盘上，随时能拿走」这句话 SaaS 竞品说不出来
- 排除：全塞 SQLite blob（黑盒 + 失 git 兼容）／云端存储（违背本地优先，增账号与网络依赖）
- 置信度：中高。风险在「文件 + 索引」双写一致性——已用「索引可丢弃重建、文件为唯一真相源」消解，W1 数据模型阶段定死
- 待验证：用户在 Finder 手改文件后，索引重建是否无缝

**决策 11（v0.6）：原型先于 PRD；竞品分析从主线降级为技能/插件分支**
- WHY：a) 原型先行——用最低成本对齐产品认知，避免在错误方向上写长文档；PRD 只描述已确认的原型，从源头消灭「文档与原型各说各话」。b) 竞品分析非必要流程——熟悉领域可直接跳过；以分支形式存在（手动选用/意图自动命中），主线保持最短闭环，且该分支成为 V2 增值能力（数据分析等）的扩展模板
- 排除：五阶段固定顺序（强迫所有用户走调研；PRD 先行导致返工时改文档比改原型贵）／竞品分析做成独立阶段开关（状态机多一条路径，复杂度不成比例）
- 置信度：高。代价是意图自动命中需要可靠的 when_to_use 描述与 trace 可观测（E4a）
- 待验证：意图误触发率（E4a）；原型确认闸口是否拖慢熟练用户（考虑「跳过确认」偏好设置）

**决策 12（v0.7）：取消末端毒舌评审，评审内建为各 Agent 每轮自检**
- WHY：末端评审 → 打回 → 重跑是「先污染后治理」——问题在产出当轮就该被修，而不是最后被一锅端；同体自评审消灭打回循环，也省掉一个 Agent 的调用成本
- 排除：末端独立评审 + 定向打回（原设计——用户裁定多余：评审完再回头改，浪费且拖长主线）／只靠用户确认闸口把关（两个人工闸口管方向，不管质量细节）
- 置信度：中高。已知代价：**生成与评审同体有立场盲区（「灯下黑」）**，原「评审独立性」原则被废弃——以三条补偿：a) 清单化自检（漏项雷达+六视角按阶段特化，维度固定不可自选，防自评放水）；b) 独立毒舌评审降级为可选分支（技能库 / MCP review_doc，想要外部第二意见时按需触发，不阻塞主线）；c) 两个人工确认闸口不变
- 待验证：自评审有效性（E16——连续零修正即失效告警）；独立评审分支的实际触发量（埋点观测真实需求）

**决策 13（v0.8）：结构设计阶段前置（澄清与原型之间）+ 阶段推进确认模式**
- WHY：干预成本曲线——改一句话（澄清）＜ 改一张图（结构）＜ 改原型 ＜ 改 PRD。原方案从要点表直接跳原型，结构决策黑箱是致命问题高发区；图是比原型便宜得多的干预点（改一个模块几秒钟，改原型要重新生成）。Mermaid 源码 .md 完全对齐本地优先哲学（开放格式、Finder 手改合法、Git 可 diff、索引可重建），mermaid.js 打包进 App 与原型共享「零外部依赖、断网可用」承诺。阶段推进确认是 ① 环「提问→作答→继续」模式的自然泛化，成本低
- 排除：塞进澄清阶段（认知模式不同——澄清是提问暴露盲区，结构设计是方案收敛，混在一起两头做不透）／塞进原型阶段当前置稿（埋没独立确认闸口，用户失去对结构的显式干预点）／不做（结构黑箱＝致命问题高发区，用户明确要求）
- 置信度：高。已知风险两条并有对策：简单产品觉得繁琐（粒度自适应——阶段必须有、深度可分级）；图无客观对错易过度打磨（图定位为脚手架，PRD/原型才是交付物）
- 待验证：E17 结构产物质量；推进确认是否拖慢熟练用户（埋点观测选项分布）；过期传播分级标记（局部/全部）是否够用

**决策 14（v0.9）：自评审升级三层闭环——漏项雷达四档 + 决策 WHY 记录判据 + 💀 风险登记**
- WHY：自检清单只回答「检查了吗」不回答「漏了什么、什么会致命」；💀 作为一次性输出，下一轮就被冲掉——红队是烟花不是哨兵。三层设计各防一个病：四档雷达（✅❓⏭️💀）防漏项无声明；WHY 记录判据防决策不可复盘；💀 登记 + 触发信号 + 状态机结算防风险悬空。关键判据全部选**不可编造的可观测信号**：备选方案有落盘痕迹（犹豫声明不可观测——模型没有 token 层犹豫信号，事后自评「我犹豫过」等于编 30%）；💀 触发信号从状态机事件枚举中选（硬事件由状态机结算，模型凑不出「被触发」）
- 排除：💀 存在性告警（连续 N 轮💀为空→失效——**Goodhart 病**：指标逼模型每轮凑一条假致命漏洞，制造它想防的病）／犹豫信号判据（假观测，同编造 30%）／活跃💀硬上限（同时出现 4 条真实风险时丢风险比破坏上限更糟——改软上限+收敛动作）／模型每轮轮询回核全部历史💀（第 20 轮核 20 条，结局必然是机械勾「未触发」）／30% 排除方案差距（假精确，不可判定）
- 置信度：中高。机制层完整（落盘→结算→回写→终态，口径闭合），未知的是💀的实际产出质量与命中率分布
- 待验证：E16a（悬空口径）；💀 命中率（长期为 0 = 要么没有真实致命假设、要么红队放水，需人工抽查分辨）；WHY 事后被引用率（V2 埋点——盯「下轮对话/后续产物引用了这条 WHY」而非记录行为，同样防 Goodhart，架构不堵死）

**决策 15（v0.9.10）：知识与记忆三层语义划分——卡片库收窄为方法论，讨论类萃取归记忆层**
- WHY：原卡片库混收两类语义相反的东西——方法论（怎么做事：跨项目直接用，不会过时、只会被改良）与讨论萃取物（用户偏好、领域事实：发生过什么，会过时）。混层代价双向发生：把偏好当方法论跨项目复用 = 过期记忆冒充通用知识；把方法论当记忆做覆盖 = 好方法被新结论静默顶掉。收窄后配三个增量机制：**实战注记**（方法论被使用后 append 带项目出处的心得，只增不覆盖——官方定义谁都有，注记才是资产，这是卡片库的复利）；**主动推荐**（阶段开始扫描双库按项目特征推荐，技能库从被动命中升级为主动调度，可拒绝防骚扰）；**记忆校准注入**（使用方法论时注入该用户历史使用倾向——方法校准不了使用者，记忆补位）
- 排除：卡片库继续混收（双向语义污染，如上）／实战注记用覆盖语义（心得是增量不是版本，覆盖丢历史）／主动推荐不可拒绝（推荐变骚扰，Goodhart 前兆）／记忆校准做成全局 prompt（与具体方法论无关的倾向注入是噪音）
- 置信度：高。三层判据清晰（会不会过时）；三个增量机制各自独立可降级——注记可空、推荐可关、校准可缺，三层语义划分本身不受影响
- 待验证：E23（推荐含理由 + 同阶段不重复推销被拒项；注记 append-only；校准注入 trace 可证）；注记真实回读率（V2 埋点——盯「下次使用该方法论时是否引用了注记」而非记了多少条，防凑数式注记）

**决策 16（v0.10）：风险台账从「触发信号状态机」升级为「四态台账 + 采纳落实闭环」**
- WHY：旧设计的💀闭环只解决了「记账与结算」，没解决「处置」——用户看到一条风险，唯一动作是等它被触发或封板关闭，中间没有「做事」的位置。四态把处置纳入状态机：**采纳 ≠ 解除**（采纳只进入 mitigating，方案落地验证后才 resolved）——这条纪律防止「点了采纳就以为没事了」；采纳落实闭环让「采纳」真的产出可执行物（执行包落盘 + 可注入下一轮），而不是改个状态字段；结算点从「事件驱动」改到「阶段确认门」，因为门是用户本来就会经过的地方，而状态机事件在坏情况下可能永远不发生（风险自然悬空）
- 排除：保持触发信号状态机（只有记账没有处置，风险台账变成墓碑区）／采纳即 resolved（方案没落地就算数，风险被状态掩盖）／结算挂在事件上（事件不发生时风险永久 open，观测成本转嫁用户）
- 置信度：中高。四态流转简单可验证；未知的是「已挂方案」会不会长期不解除（说明验证环节空转——已列为埋点「风险处置分布」）
- 待验证：E16a（采纳与解除分离）；处置流转耗时中位数（「已挂方案」长期不解除 = 验证环节需要更明确的提示）

**决策 17（v0.10）：主线是建议节奏——快速通道 + 变更治理（候选池 + 草稿预演）**
- WHY：四阶段流水线是「对新手友好」的默认骨架，但对想快的人它成了负担；而反过来，随时响应「加个功能」又会打断收敛。两个机制分别对应两端：**快速通道**给「我已经想清楚了」的用户一条直达路（省逐步确认，不省质量把关——跳步不豁免自评审/模板/确认坞；跳过即留痕，可补做）；**变更治理**给「我又想到一个」的用户一个不打断主线的容器（提案 → 影响清单 → 用户裁决；默认进池保住灵感；封板前必须逐条毕业——不留说不清的尾巴）。共同设计原则：**LLM 只分诊/请求，用户裁决执行**——AI 不替用户宣布「将执行回退」，也不主动建议跳步
- 排除：跳步做成设置里的开关（静态偏好，无法逐次表达意图）／新想法直接回退（打断收敛，且用户往往只是「记一下」）／新想法静默并入本阶段产物（下游契约漂移，最坏情况）／候选池无毕业仪式（池内悬案污染下一版起点）／影响清单可省略（AI 轻描淡写「小改动」，用户失去判断依据）
- 置信度：中高。已知代价：分诊有误判率（校准原则选「宁可多问」——微调误判成新需求代价小，新需求被吞进修订代价大）
- 待验证：E24 / E25 / E26；跳过阶段的补做率（高 = 用户跳早了，需要更好的劝留时机）；池内毕业率（长期积压 = 收敛纪律没被使用）

**决策 18（v0.10）：多会话并行——busy 判据从全局单流闸改为会话/版本/项目三级**
- WHY：旧设计用一把全局锁防竞态，把实现成本转嫁成了用户成本（一个会话在生成，另一个会话连问个问题都被卡住）。正确做法不是「不防竞态」而是「只在真正需要串行的地方串行」：会话级（本会话发送）→ 版本级（改动版本状态的动作）→ 项目级（结构性操作），三级各管一段；竞态风险仍由「版本级写入串行 + 从磁盘重建阶段位置（deriveStage）」兜住，不靠内存缓存保证一致性
- 排除：保持全局单流闸（用户成本不可接受）／完全放开不加锁（版本级状态写入竞态，阶段位置错乱）
- 置信度：中高。并行路径多（后台完成、跨版本、停止生成、队列路由），需测试矩阵覆盖——已有 VersionBusyScopeTests / ParallelTurnSafetyTests / BackgroundCompletionTests / SessionStreamFanoutTests 四组
- 待验证：E29；跨版本后台完成挂起行的处理率（长期不处理 = 收尾体验有缺口）

**决策 19（v0.10）：决策档案走确定性渲染，不走 LLM 转述**
- WHY：决策记录是 append-only 的结构化数据，转成人看的 HTML 是纯粹的**格式转换**，不是语义任务。LLM 转述有三个不可接受的代价：每次生成结果不同（同一份数据两份文档）、贵（每次全量重渲染都要花 token）、可能失真（数字与出处被改写）。确定性渲染解决全部三条：纯函数、零 token、毫秒级、逐字一致。配套的三条工程决定同样重要：**内容比对幂等**（内容没变就不重写，mtime 不动）、**HTML 不嵌生成时刻**（否则内容比对永不相等，幂等失效——新鲜度由数据本身表达）、**档案是派生产物**（删了可重建，jsonl 仍是唯一事实源）。封板复盘（`ReleaseRetro`）沿用同一立场：磁盘事实聚合，不经 LLM 转述
- 排除：LLM 生成档案（上述三条代价）／档案当事实源（派生产物一旦可写就产生双事实源）／档案内嵌生成时间戳（幂等失效）
- 置信度：高。机制层确定性可验证（幂等跳过率接近 100% 即健康）
- 待验证：E27 / E28；幂等跳过率（长期为 0 = 每次内容都在变，正常；但若与「无新决策」并存则说明幂等在失效）

**决策 20（v0.10）：系统行静默化 + 开场承接——状态播报交给 AI 的那句话，不占独立行**
- WHY：对话流被三类系统行（⚡受理 / ⏳风险提醒 / ✅阶段推进）切成了碎片，用户读正文前先要读一遍「系统在干什么」。但这些信息本身是有价值的——解法不是删掉，而是**换载体**：由 AI 当轮回答的开场句用大白话交代（「这轮我先跳过结构，直接把原型做出来」），信息保留、行数归零。技术代价是系统行必须仍留痕（作链标记与时间轴节点），且开场承接注入必须**只传事实不传措辞**（不给模型发挥空间，防自己编进度）
- 排除：直接删除系统行（语义丢失，用户看不到状态变化）／保留原位但降级视觉（行数没减少，只是变淡）／让模型自行总结状态（会编造——必须注入事实）
- 置信度：中高。已知风险：静默行的链标记作用被破坏（合并行、快速通道链判定、值班单时间轴都依赖这些行）——按 bugs.md B004 的教训，新增系统行前缀必须同步登记全部白名单并配防回归测试
- 待验证：E32；静默行语义覆盖率（每条静默行都必须有一条开场承接对应——覆盖率缺失即用户看不到状态变化）

**WHY 记录执行规则（分级，与 §6.2 决策 WHY 记录判据配套）**：
- 🔴 重度输出（PRD、完整方案）：每个关键决策点完整覆盖 5 项，文档中显式输出
- 🟡 中等输出（方案设计、需求分析）：核心决策（1-3 个）完整覆盖 5 项，次要决策至少覆盖①决策②WHY③排除方案
- 🟢 轻量输出：至少一句话说明 WHY 与排除理由，不必展开完整格式
- 关键决策判据（决定用哪档）：跨阶段影响 / 不可逆 / 备选方案存在性（真实写出过 B 方案），满足任一即升🔴档；兜底用回看检验（三个月后答不上来为什么 = 漏记，补记）
