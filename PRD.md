# PM Copilot — 产品需求文档（PRD）

> 版本：v1.3.8 · 日期：2026-09-22 · 状态：待评审
>
> **v1.3.8 变更记录（同步 design.md v0.10.6）**：**寒暄轻量轮（新增 FR18）+ 思考卡内容口径收口（FR9 / A20）**——① **一句「你好」实测 127 秒**（两次串行 AI 调用、每次 2.2 万 token 输入），因为产品此前**没有寒暄这条路**：问候照样按所处阶段走完整流水线，并被 FR6「每轮必须自评审」逼着给客套话逐项填漏项雷达。现新增 **FR18 寒暄轻量轮**：明确问候/致谢不进入阶段轮，短答即可，不产出任何产物、不做自评审、不调用工具。② **FR6 补豁免口径**：「每轮内建自评审」的「每轮」此前从未排除寒暄/致谢轮，现明文豁免（同 FR16 计划卡已有的「防仪式化豁免」格局）——**质量机制不该作用在一句客套上**。③ **FR9 / A20 恢复 v1.2.6 自己写下的口径**：那条变更记录原文就写了「推理步骤为**面向用户可读的摘要**而非原始思维链」，但 2026-09-18 的改版把原始思维链接进了三处用户可见出口（折叠摘要行的「收束句」不点开也漏、展开态全文、流式实时尾随），实测漏出的是「memory injection already gives me what I need」这类内部机制自述——**违反 §5 用户信息展示规范**。现收口：思考卡只展示产品自己生成的结构化行（工具行 / 技能行 / 阶段时间线）+ 耗时，原始思考原文不再上屏，回看入口在 ⌘D 开发者模式（FR9 第 4 条同步补项）。④ **已知代价（有意接受）**：长等待期不再有「实时看到它在想什么」这层安慰，只剩阶段时间线与工具/技能行——本次问题恰恰是这层在不该想的时候想太久。⑤ 验收标准 46 → 47 条（新增 **A43**；A20 同步修订），**第二圈批次的验收项编号由「A43 起」顺延为「A44 起」**（v1.3.6 那条预留就此让位，避免同号歧义）。测试 705 → 727。
>
> **v1.3.7 变更记录**：**「数据指标」章补第五步——指标口径成为一等产物（机制同步 design.md v0.10.5）**——① **补齐既有矛盾**：FR5 第 4 条与三档模板此前只走四步（期望结果 → 基线 → 目标值 → 测量时间点），而 ④ prompt 早写了「缺口径的指标写入待定问题清单」——**四步里没有一步在问口径，那条判据无处落地**。现扩为五步，第 5 步即口径（分子 / 分母 / 统计时间窗三项为核心，另可填数据来源与排除条件）。② **口径卡（机读侧）**：`04-prd/metric-specs.jsonl`，append-only，按指标名 last-wins 折叠；**只存口径不存任何数值**——数值的唯一事实源仍是 4.2 表（AGENTS.md 衍生品不留副本；多一份数字副本就多一处腐烂）。③ **采集通道复用而非新造**：PRD 是单次流式生成，「对话式」不可能在同一次生成里中途提问，真实回路一直是既有的 **PRD 默认项卡**（FR5）——口径按常识起草的候选以 `assumed` 态出卡、首选项「保持默认」，用户一键确认后升 `confirmed`；**任何路径都不会自动升成已确认**，`confirmed` 且三项齐备才叫可对账。④ **A21 扩项 + 三档评审清单同步**：起草的口径冒充用户既定事实、口径记录与 4.2 表不是同一批指标、记录内出现数值，均判缺陷（独立评审兜底，评审材料已附口径清单）。⑤ **与第二圈的咬合**：本条是 §11.2 新增的第七处咬合点，也是 C 环（结果对账）的**准入门**——没有口径，「上线留存 38% vs 目标 45%」根本判不出达没达标，只会产出「数字不一样」这种比没有报告更坏的假对账。⑥ **口径改为引用槽，正文与记录强制同源（live 实测逼出来的改造）**：首跑真实生成即出现分叉——同一指标 PRD 正文写分母「该日打卡活跃用户」、口径记录写「该日活跃设备」，同一个 45% 目标对应两笔不同的账。现改为：正文 4.2 的口径位置只写 `[[SPEC:指标名]]`，落盘时由 App 用记录块渲染替换（复用 `[[MERMAID:]]` 的「模型只写引用、不抄内容」先例，但**事实源方向相反**——图表的事实源在上游产物，口径的事实源在记录侧，文档成了衍生品）；未解析的槽降级为行内 `⚠️ 口径未登记`，不静默不虚构；修订轮漏带记录块时退到盘上现有记录渲染。三档模板与评审清单同步（正文残留 `[[SPEC:` 即缺陷）。⑦ **副作用（有意为之）**：三档模板版本戳 2 → 3，按「产出必须按当前模板版本、存量不豁免」的既有钦定，存量 PRD 将被标失效、需重生成方可封板（实测确认：该标记不拦封板，`prdStale` 无封板链路读者）。测试与 live 探针见 design.md v0.10.5。
>
> **v1.3.6 变更记录**：**产品定位由「一圈」明确为「双圈闭环」，指标与范围层同步（机制同步 design.md v0.10.3 §14）**——① **§5.2 非目标解除自相矛盾**：原「❌ 数据报告分析、迭代管理深化、复盘」与本产品的最终方向直接冲突，改为「第二圈不在 MVP 主线，见 §11」；本节此后只列**明确不做的方向**，「本圈未做、下圈要做」不再算非目标。② **§4.2 北极星与里程碑验收线拆为两层**：原表把「M5 演示跑通 2 个项目」这条**里程碑验收线当成了产品北极星**（表头即写作「目标（MVP 验收线）」），导致第二圈在指标面上永不可见、永远排不上优先级。现北极星为「跑通完整闭环的项目数」（当前结构性为 0，作路标用），配 leading 指标「带出处的事实入库条数」提供当下可推动的杠杆；原北极星原样降级为 M5 验收线，新增 M6 = 闭环首个非零值。③ **§4.2 新增四条第二圈护栏**（导入事实出处率 / 对账结论无支撑数 / 「待验证」跨版闭环率 / 复盘过程信号占比），§10 埋点同步四条。④ **§11 由「V2 路线」重构为「第二圈路线」**：按 **A 事实入口 → C 结果对账 → D 项目级需求池 → E 开工档案袋**的依赖顺序排列，新增 §11.2 六处咬合点（第二圈复用了第一圈已留下的什么），并把不承载闭环的能力单列为旁支——**一张平铺表正是它此前读起来像「以后再说」的原因**。⑤ 双语 README 路线图加 M6 行，并把 M5 那行的「北极星验收」改回其真实身份「M5 验收线」。⑥ **新增 §4.2.4 双圈模型（词义钉死）**，据此同步 §4.1 定位段两处表述：「产品经理全流程」明示为双圈闭环、定位句由「走到 PRD 的全程同行者」改为「双圈闭环的全程同行者」并标明第二圈是规划不是能力；§12 开放问题加第 8、9 条；附录对应关系 §11 行更名。
> **本批次为纯文档与指标口径改动，零代码、零机制变更**；第二圈各环的 FR 立项与对应验收项（A43 起）待后续单独批次。
>
> **v1.3.5 变更记录**：**「真 Agent 成色」三项能力补齐（机制同步 design.md v0.10.2）**——① **§11 工具调用扩至四工具**：新增 `query_impact`（模型自主查询产物依赖图，回答「改 X 会牵连什么」并**在编制变更提案影响清单前先核实影响面**——影响分析由模型调用工具自己算，计算仍是确定性依赖图，回答不凭印象列）。② **新增 FR16 本轮任务计划（plan-act-reflect）**：②③④ 生成/修订产物的回合，Agent 先输出「本轮任务计划」卡（目标 + 编号步骤）再执行——先计划后执行、产物与计划对应、自评审对照计划收尾；歧义反问 / 变更提案 / 快速通道 / 纯讨论轮豁免（防仪式化）。③ **新增 FR17 Agent 主动简报**：新风险登记后 Agent 主动逐条点评并引导台账处置；封板被变更池拦截时 Agent 主动逐条给处置建议——Agent 不再只在被问到时说话。验收标准 43 → 46 条（A40 ~ A42 新增），测试累计 628 全绿。
>
> **v1.3.4 变更记录**：**风险建议前置陈列 + 决策回溯（补全 Agent 全流程能力）**——① **FR6 风险应对建议升级为「前置陈列」**：风险登记必带 Agent 应对建议（含依据与代价）之外，阶段确认门前 Agent 将本轮待处理风险的建议**逐条汇总陈列**——采纳 → 进入落实闭环 /「不等了，只记账」/ 暂不处理；建议不再是登记时的附带一句话，而是阶段推进决策的正式输入（A38）。② **FR7 新增决策回溯**：决策与产物 / 风险 / 版本建立双向指针——任意产物卡、风险卡可一键「回溯决策」直达当时依据的五要素；回退 / 重做时被跨越阶段的历史决策**自动注入上下文**（决策不因回退丢失），沿用或推翻在新决策中显式标注，推翻即触发旧决策碑文回链（A39）。验收标准 41 → 43 条。
>
> **v1.3.3 变更记录**：**§11 V2 路线首项「模型自主工具调用（Function Calling）」提前落地（机制同步 design.md v0.10.1 §6.11）**——模型在对话中可自主发起三类工具调用：`load_skill`（按需加载方法论技能全文，本地只读）、`web_search`（联网搜索，引用必带出处）、`propose_competitive_analysis`（发起竞品分析，走既有确认卡、用户裁决后才执行、封板拒绝）。闸口不变量不破：工具路径禁写确认/跳过标记与正式产物；工具消息循环局部不持久化，可追溯由思考卡工具行承担；anthropic-compat 优雅降级；总开关 `agentToolsEnabled`（设置页「Agent 工具调用」，默认开）= 一键回滚点。配套：测试 +21（累计 619 全绿）。
>
> **v1.3.2 变更记录**：**定位措辞与实现对齐（防外部质疑）**——§4.1「调用工具」收窄为「按需调用技能与工具」，并明示实现口径：工具与技能的触发当前由系统按语义命中与规则调度（全程可解释、可追溯，FR9 思考卡 / ⌘D 检查器可证），模型自主发起工具调用（Function Calling）新增为 §11 V2 路线首项。机制零变更，纯表述修正；全景文档 §1.1 与双语 README 同步。
>
> **v1.3.1 变更记录**：**Agent 理念显性化——定位段补齐智能体定义与人的介入边界**（§4.1 表述）——① 定位按智能体定义工作：掌握目的后**自主规划、拆解任务、调用工具、依据反馈自我修正**；② 人的介入明确为两处：定义目的与验收、不可逆节点校准方向与裁决分歧（产物定稿 / 阶段推进 / 变更提案处置）；③ 确认坞重述为 Agent 主动发起的**方向校准**而非逐步审批，与 FR13 快速通道、FR14 变更治理与重规划、弹坞纪律互为印证。机制描述不变。
>
> **v1.3.0 变更记录（同步 design.md v0.10 · 09-13 ~ 09-18 迭代全量回填）**：**四项新能力 + 十处既有条目修订**——
> ① **新增 FR13 快速通道**：用户明确要求跳步时直达下游产物（中间阶段写跳过标记、不生成产物、可事后补做）；目标为 PRD 且用户无跳步措辞时走「PRD 前置确认分诊」（未决项 1-3 出前置确认卡、≥4 照常回答并列待确定清单）。
> ② **新增 FR14 变更治理**：新想法默认进候选变更池、不打断当前版本收敛；变更提案卡带分类 / 建议回退目标 / **影响清单**（列不出具体影响不得建议回退），由用户裁决纳入 / 进池 / 继续讨论；**封板前池内提案必须逐条毕业处置**（纳入后续 / 放弃 / 顺延），否则封板被拦；**草稿预演**：会话可标记草稿、产物镜像到提案目录（主线零写入），满意后「合并入主线」；**产物依赖图与重规划循环**：影响清单由依赖图确定性解析（引用不到具体产物不采信回退目标），采纳执行回退前生成「将依次重做哪些产物」的重规划计划并随回退行可见化。
> ③ **新增 FR15 多会话并行与后台完成**：同版本多会话可并行回合（仅发起会话自身忙与同版本结构闸拦截，不再全局排队）；切走后任务继续跑、回复钉回原会话；跨版本后台完成不推进闸口、落挂起行。
> ④ **FR6 风险登记升级为四态台账 + 采纳落实闭环**：待处理 / 已挂方案 / 已解除 / 已接受；采纳 → 代发受理 → AI 生成实施执行包 → 转「已挂方案」（**采纳 ≠ 解除**，方案落地验证后才解除）；「不等了，只记账」排队逃生门；结算点从事件驱动状态机改为**阶段确认门**；自评审四档声明的审计归宿移到台账（对话流只报新增）。
> ⑤ **FR9 右栏由四 Tab 收敛为三 Tab（文件 / 决策日志 / 风险）**：「知识点」Tab 移除，语义检索与方法论推荐收口左栏知识库整页（聚光灯一次主推一张 + 处置结算条）。
> ⑥ **FR2 / FR2a 对话流交互升级**：确认坞与作答坞合并为**统一停靠卡**（作答段 → 分支确认段 → 确认段同卡切换）；新增**问题卡向导**（多题逐答 → 上一步回改 → 回顾页 → 提交）；确认坞新增**分档路径**（① 三推进、② 两推进、③ 两推进含「到此为止」）；**弹坞纪律**（每版本每阶段弹一次，静默持久化、跨启动跨会话不重复弹）。
> ⑦ **FR4 新增原型冲突分槽与选主**（磁盘被外部改写时新原型降级落修订槽、主槽不动，可手动「设为主原型」）与**回退坞**（重做原型 / 重做结构，revise / redo 双模）。
> ⑧ **FR7 新增决策档案与记忆治理**：决策日志改为**确定性渲染的 HTML 档案**（按天 + 版本总览，零 LLM、内容比对幂等、删了可重建），写入时富化（用户诉求 / 认知所有权标注 / 话题结束才写）；记忆收敛为**项目 / 全局两档**（版本降为溯源标签），约束与否决项为超预算不裁的保护层，失效条目走碑文回链。
> ⑨ **FR1 封板从「清账」升级为「对账」**：封板复盘聚合变更处置分布 + 回退分布 + 闸口 outcome 分布 + 方法论使用报告，落 release-notes 尾部并作下版本开工素材。
> ⑩ **FR8 / FR12 补充**：知识卡写时富化（定义 / 做法 / 适用边界三段 + 「为什么有效」原理）、AI 代写案例草稿（append-only，失败静默）、聚光灯推荐（可采纳 / 可跳过，同阶段不重复）；模型新增**思考力度**档位（Low / Medium / High / Max，默认 High）。
> ⑪ **系统行静默化**：⚡快速通道受理 / ⏳跨门风险提醒 / ✅阶段推进三类系统行不再单独渲染，语义由 AI 回答的**开场承接句**承载（仍留痕作链标记与时间轴节点）。
> ⑫ §8 非功能新增输出吞吐、写冲突保护、失败退避、长回答截断续写等稳定性要求；§9 验收标准 26 → 41 条（A23 ~ A37 共 15 条新增）；§10 埋点同步；§11 V2 路线补入知识反哺未落地两项。
>
> **v1.2.11 变更记录**：**产品定位表述明确化——本产品定位为「一个产品经理全流程的 AI Agent」**（§2.2、§4.1 表述对齐）——① §4.1 以定位声明开篇：产品本质上是一个产品经理全流程的 AI Agent（本地优先的 macOS 原生应用），四阶段（澄清 → 结构 → 原型 → PRD）是它的工作主线与节奏，不是单点提效工具、也不是一次性流水线；② §2.2 明确本产品即该市场空白的补位者；③ 机制描述与差异化五点不变。产品名「PM Copilot」为暂用工作名（见 §12）。
>
> **v1.2.10 变更记录（同步 design.md v0.9.12）**：**确认坞简化为单页直接确认 + 系统事件文案「去黑话」（FR2a / A18 / 概述段修订）**——① 确认坞取消第二页「确认摘要」，两段式提交收敛为**单页两步**：选择处理方式（① 确认并进入 ② 继续修改 ③ 稍后再说）→ 页脚按钮随所选选项直接执行；「确认后会发生什么」由「确认并进入」选项卡副标题以结果导向人话承载（如「结构定稿存档，AI 随即开始生成可交互网页原型」）。「不确认不推进」闸口原则不变。② 文案黑话清理：确认注记 / 产物事件去内部存储路径，「已落盘」改「已生成」并与产物 chip 同词汇表；🔒 拦截提示去「确认闸口」术语；「落盘失败」改「保存失败」。原则：**界面文案只说「你定稿了什么、AI 接下来做什么」，内部路径与架构术语留在实现层**。
>
> **v1.2.9 变更记录（同步 design.md v0.9.10）**：**知识与记忆三层语义划分（FR7/FR8/FR9 对齐）**——① 卡片库语义收窄为**方法论卡片**（「怎么做事」：跨项目直接用，不会过时只会被改良）；原 discussion 类萃取物（用户偏好、领域事实——「发生过什么」）改归**记忆层**，新增「经验」条目类型（带出处与置信度，沿用覆盖语义）——判据：会不会过时。② 方法论卡片新增**实战注记**：方法论被使用后在卡片注记区 append 一条带项目出处的心得，**只增不覆盖、越用越厚**（官方定义谁都有，注记才是资产）；技能文件同构支持。③ 新增**方法论主动推荐**：阶段开始时 Agent 扫描卡片库+技能库按项目特征推荐 1-3 个适用方法论（含理由、可拒绝、同阶段不重复推荐被拒项）——技能库从被动命中升级为主动调度。④ 新增**记忆校准注入**：Agent 使用某方法论时同步注入该用户的历史使用倾向（按假设态），方法校准不了使用者、记忆补位。⑤ 「这条记下来」弹窗新增**归属分流**（方法论卡片 / 经验记忆，默认判定可改）。验收标准新增 A22（26 条）、埋点新增「主动推荐采纳率」「实战注记回读率」。
>
> **v1.2.8 变更记录（同步 design.md v0.9.9）**：**FR5 新增「模板分级智能选择」与「成功指标对话式定义」**（借鉴 slgoodrich/agents 的 requirements-engineer 机制，模板正文自写）——① PRD 模板从单一固定模板升级为**分级模板集**（精简 1-2 页 / 标准 3-5 页 / 完整 5-10 页），进入 PRD 阶段时按**三维度评分**（技术复杂度 / 风险影响 / 范围广度，各 0-10，评分依据锚定上游已确认产物而非自由发挥）自动选档，**评分卡在生成前透明展示、用户可一键切换**（强化「上下文透明可解释」差异化）；模板集仍走确定性路由取最新版（不走向量检索的既有约束不变）。② 「数据指标」章新增**四步对话式定义**（期望结果 → 有无基线 → 目标值 → 测量时间点）：**数字 100% 来自用户**，Agent 只引导对话、不建议不编造具体数值；无基线指标标「实施期建立基线」；任一步可跳过、按待定问题兜底不阻塞。验收标准新增 A21（25 条）、埋点新增「PRD 模板选择分布」。
>
> **v1.2.7 变更记录（同步 design.md v0.9.8）**：**阶段推进确认升级为「确认坞（ConfirmDock）」**——确认交互从对话流内联提问改为**输入框上方停靠卡 + 两段式提交**（选择 ①确认并进入 / ②继续修改 / ③稍后再说 → 下一步展开确认摘要（将执行什么/推进到哪）→ 提交生效）；提交后卡收起、对话流内落确认记录胶囊（封版回看可见），Agent 思考卡出现继续推进；停靠期间输入框始终可用——自由作答中明确推进指令（「进入下一个阶段」）等价选①并自动结算卡片，其余反馈等价选②；「稍后再说 / 继续修改」将卡折叠为输入框上方细条，可随时展开、不阻塞输入。FR2a 通用模式、A18、指标同步修订。
>
> **v1.2.6 变更记录（同步 design.md v0.9.6）**：**FR9 新增对话思考过程展示（思考卡）**——Agent 每轮回复前置思考卡：思考中显示 spinner +「正在思考…」；完成后默认折叠为摘要行（思考了 Ns · M 步 · 技能 ×K），点击展开推理步骤与技能调用行（命中技能 + 触发词 + 注入内容 + 耗时）；推理步骤为**面向用户可读的摘要**而非原始思维链。验收标准新增 A20（24 条）。
>
> **v1.2.5 变更记录（同步 design.md v0.9.5）**：FR6 漏项雷达**判据显式化**——四档声明新增**「判断依据」**条目（每个点对照本阶段自检清单 + 上游已确认产物逐项对账，不靠 AI 自由发挥；质量下限由「维度固定 + 连续 3 轮零修正告警」兜住）；✅ 档补「列具体名称，禁『均已覆盖』式空话」；四档各标注本质（对账结果 / 等输入 / 边界声明 / 证伪）。
>
> **v1.2.4 变更记录（同步 design.md v0.9.4）**：**新建任务页右栏面板**——「面板」入口补全至新建任务页（此前仅对话页可见）；新建任务页输入框新增**关联项目选择器**（不关联 / 关联已有项目），FR1 新增第 9 条需求明细（关联决定落盘位置与面板预览内容）；面板内容随关联驱动：关联项目 → 产物 Tab 预览该项目文件（项目无产物则空态），未关联 → 四 Tab 空态；**决策日志在新建任务页恒为空**——决策与 WHY 是执行中才写入的运行时数据，新任务无记录不是缺失而是尚未开始；空态文案引导、不填示例数据。验收标准新增 A19（23 条）。
>
> **v1.2.3 变更记录（同步 design.md v0.9.3）**：风险登记册**用户侧可读性**——FR6 右栏「漏项雷达」Tab 状态展示翻译为**人话三态**（还盯着 / 被说中了 / 安全落地），每条 💀 卡一句话假设 + 触发条件或结局；命中卡改对照式（当时担心 ↦ 实际发生 ↳ 回写决策日志）；默认只展开在盯风险、命中/关闭折叠为「已了结 N」计数；状态枚举、软上限、收敛动作等工程口径移入开发者模式（⌘D）。闭环机制本身（登记带触发条件 / 事件结算 / 软上限 / 命中回写 / 封板终态）不变。
>
> **v1.2.2 变更记录（同步 design.md v0.9.2）**：右栏升级为**四 Tab**（产物 / 决策日志 / 漏项雷达 / 知识点）——FR9「上下文」面板更名「知识点」Tab；FR7 决策日志新增右栏 Tab 入口（紧凑卡 + 点击展开五要素，与独立页面/按阶段弹窗并存）；FR6 漏项雷达与 💀 风险登记册新增右栏常驻入口（对话雷达卡「查看登记册」直跳）。
>
> **v1.2.1 变更记录（同步 design.md v0.9.1）**：FR9 上下文检查器降级为**开发者模式**——用户侧右栏「上下文」面板只展示**注入的知识卡**（query + 命中卡 + scope 过滤说明），token 构成条 / 未命中技能列表等工程调试信息移入开发者模式（⌘D 切换）；技能调用改由对话思考过程展示、记忆写入由对话系统行展示。FR8、A4a 同步修订。
>
> **v1.2 变更记录（同步 design.md v0.9）**：FR6 自评审升级为三层闭环——**漏项雷达四档声明**（✅已覆盖 / ❓可能遗漏 / ⏭️刻意不展开 / 💀致命漏洞假设；💀可为空是明文合法输出，禁止凑数）+ **决策 WHY 记录判据**（跨阶段影响 / 不可逆 / 备选方案存在性，满足任一记全五要素）+ **💀 风险登记**（登记必带可观测触发条件 → 事件结算 → 命中回写决策日志 → 封板终态；活跃软上限 3 条）；FR1 封板新增风险终态结算；FR7 决策日志新增 💀 命中回写条目；验收标准新增 A16a（告警采用悬空口径——盯💀事后被结算，不盯💀有无，防凑数指标制造它想防的病）；护栏指标与埋点同步。
>
> **v1.1 变更记录**：主线由三阶段扩为**四阶段**（澄清 → 结构 → 原型 → PRD）——新增 **FR2a 结构设计 Agent**：澄清确认后先产出功能架构图 / 核心流程图 / 模块-页面映射表（Mermaid 开放格式），**结构经用户确认后**才生成原型，PRD 以「结构 + 原型」双重基准生成；新增**阶段推进确认**通用模式（每阶段完成主动询问是否进入下一阶段，不确认不推进）；FR1/FR4/FR5/FR6/FR11 同步修订；验收标准新增 A17/A17a/A18。
>
> 本文档为纯产品需求文档，由 design.md 提炼而成，剥离全部技术实现细节（架构、选型、数据 schema、开发计划）。技术方案见 design.md。

---

## 1. 文档说明

- **读者**：产品负责人、开发协作方、设计与测试
- **范围**：仅定义「做什么、为谁做、做到什么程度算合格」；不约束「怎么实现」
- **优先级定义**：
  - **P0**：MVP 必须交付，缺失则产品主流程不成立
  - **P1**：MVP 应交付，可降级但不能砍掉语义
  - **P2**：第二圈或旁支范围（见 §11），本期不实现但架构上不堵死

---

## 2. 背景与机会

### 2.1 竞品格局

| 竞品 | 形态 | 优势 | 短板 |
|---|---|---|---|
| 起念 AI | 云端闭源 | 引导式需求分析 | 原型弱、纯云端、闭源 |
| 墨刀 AI | 云端闭源 | 调研→PRD→原型→评审一站式，已开放 MCP | 生态封闭、依赖云服务与账号 |
| lighthought/app-maker | 开源（AGPL） | 多 Agent 流水线编排成熟 | 面向开发者、代码不可复用 |

### 2.2 市场空白

开源世界没有「PM 全流程 Agent」；商业产品均未同时具备以下五点。**本产品的定位正是补上这个空白：一个产品经理全流程的 AI Agent**——不是单点提效工具，而是从一句话想法走到 PRD 的全程同行者；以下五点是它的差异化支撑：

1. **对话自动沉淀知识**——结论、约束、否决项、经验、决策不靠用户手动整理
2. **方法论技能化**——PM 方法论（KANO / RICE / 用户故事地图等）按需命中 + 阶段开始主动推荐，越用越厚（实战注记只增不覆盖）
3. **上下文透明可解释**——用户能看到每次 AI 回答「用了什么、没用什么」
4. **先结构、后原型、再 PRD，一键生成且断网可用**——结构图先行锚定功能架构与核心流程，原型先行验证形态，PRD 基于已确认结构与原型；零外部依赖
5. **数据完全本地**——数据不出机、无账号、可离线（模型调用除外）

---

## 3. 目标用户与画像

### 3.1 目标用户

| 用户 | 主次 | 核心诉求 |
|---|---|---|
| 想用 AI 提效的 PM | **主** | 输入一句话想法，拿到可用的 PRD、可点击原型和带出处的调研材料 |
| 会写代码的 Builder | 次 | clone、自构建、改造、贡献方法论技能包 |
| 面试官 / 评审人 | 隐含 | 15 分钟内看懂产品价值与工程决策质量 |

### 3.2 非目标用户（本期明确不服务）

- 需要多人协作 / 权限管理的团队（本期是单用户本地工具）
- 需要精细原型编辑的视觉设计师（只做生成，不做 Figma 式编辑）
- 非中文用户（中文为主，英文 README 为辅）

---

## 4. 产品目标与成功指标

### 4.1 一句话定位

**产品定位：一个产品经理全流程的 AI Agent**——本地优先的 macOS 原生应用。**「全流程」在本产品里指双圈闭环**（第一圈：想法 → PRD 封板；第二圈：上线事实 → 下一版开工），**不是 PM 职业工作的全流程**——需求评审会、开发排期、跨团队沟通、上线部署不在范围内（词义见 §4.2.4，边界见 §5.2）。它按**智能体的定义**工作：掌握目的之后，**自主规划、拆解任务、按需调用技能与工具，并依据反馈自我修正**。实现口径：模型自主发起工具调用（Function Calling，四工具：技能加载 / 联网搜索 / 竞品分析提议 / 产物依赖查询，见 §11），技能与知识的系统侧语义命中调度与它并行工作（全程可解释、可追溯，见 FR9 思考卡与 ⌘D 开发者检查器）。它不是单点提效工具，而是**双圈闭环**的全程同行者：第一圈从一句话想法走到 PRD 封板，第二圈从上线后的事实走回下一版的开工（两圈词义见 §4.2.4，第二圈路线见 §11——**第二圈当前是已确定方向的规划，不是已实现能力**）。人的介入只有两处——**定义目的与验收**（一句话想法、对追问的作答）、**在不可逆节点校准方向与裁决分歧**（产物定稿、阶段推进、变更提案处置）；怎么规划、拆成哪些任务、调用哪些技能与工具、发现偏差后如何修正与重规划，全部由它自主完成。输入一句话产品想法，它以**需求澄清 → 结构设计 → 原型生成 → PRD 生成**四阶段为工作主线（主线是**建议节奏而非硬向导**：用户明确要求跳步时走**快速通道**直达下游产物，对上游不满时可随时**回退重做**）：先用功能架构图与核心流程图锚定产品结构（Mermaid 开放格式，用户可改），**结构经用户确认后**生成可点击原型对齐产品认知，**原型经用户确认后**才撰写 PRD；**每个阶段完成时 Agent 在输入框上方弹出确认坞发起阶段推进确认**（单页直接确认：选择处理方式 → 页脚按钮随所选选项直接执行生效；① 阶段另带「跳过结构直出原型 / 跳过中间阶段直出 PRD」、② 阶段带「跳过原型直出 PRD」、③ 阶段带「到原型为止」的**分档路径**——这是它主动发起的**方向校准**而非逐步审批：不确认不推进，用户也可随时主动说「进入下一个阶段」等价确认并自动结算；同一版本同一阶段的确认坞只主动弹一次，用户说「稍后再说 / 继续修改」后转为静默，推进由用户发起）；**每个生成 / 修订产物的回合先输出本轮任务计划再执行**——计划卡（任务目标 + 编号步骤）对用户可见，产物与计划对应、计划外不凭空产出，自评审对照计划收尾（先计划后执行；歧义反问、变更提案、快速通道、纯讨论轮豁免，防仪式化，见 FR16）；**每个 Agent 每轮内建自评审**——产出后对照自检清单自检、发现问题当轮修正，输出末尾附**漏项雷达四档声明**（致命假设登记入风险台账，按四态闭环处置），**不设末端总评审**；**它还会主动开口**——新风险登记时主动逐条点评并引导台账处置，封板被变更池拦截时主动逐条给处置建议（简短、克制、闸门不满足即静默，不打扰，见 FR17）。最终产出经逐轮打磨的 PRD 与已确认的结构、原型；竞品调研等增值能力以**技能/插件分支**按需触发（用户主动选用或对话中自动命中）；过程中的知识自动沉淀，方法论按需命中，全部数据存于用户本地。

### 4.2 北极星与护栏指标

**北极星是产品级、跨里程碑的唯一指标；里程碑验收线是阶段性的达成口径。此前两者被写进同一张表的同一列（表头即「目标（MVP 验收线）」），结果是第一圈的交付达成即被视为产品目标达成，第二圈在指标面上永不可见、也就永远排不上优先级。本节起拆为两层。**

#### 4.2.1 北极星（产品级，唯一）

| 类型 | 指标 | 定义 | 当前值 | 采集方式 |
|---|---|---|---|---|
| **北极星** | 跑通完整闭环的项目数 | 一个项目从一句话想法走完**第一圈**（四阶段 → PRD 封板），再走完**第二圈**（导入 ≥1 份上线后事实 → 产出结果对账 → 开出下一版且开工档案袋非空） | **0**（第二圈未实现，属结构性为 0） | 人工验证 |
| 北极星 · leading | 带出处的事实入库条数 | 进入证据目录、带来源标签与时间戳的外部事实条数（上线数据 / 用户纪要 / 行业报告 / 问卷导出） | 0（入口未实现） | 产品内统计 |

**为什么允许一个恒为 0 的北极星**：本产品当前最大的排期风险不是第一圈质量，而是第二圈永远排不上——一个每日显示 0 的结果指标本身就是最强的排期信号。leading 指标负责提供当下可推动的杠杆，**两者不互相顶替，也不得用 leading 的进步抵充北极星的空缺**。

#### 4.2.2 里程碑验收线（阶段性）

| 里程碑 | 指标 | 目标 | 采集方式 |
|---|---|---|---|
| M5 | 完整走通四阶段主线并产出合格产物的项目数 | 演示中至少 2 个真实项目跑通 | 人工验证 |
| M6 | 跑通完整闭环的项目数 | 首个非零值：1 个项目走完第二圈 | 人工验证 |

#### 4.2.3 护栏与反指标

| 类型 | 指标 | 目标 | 采集方式 |
|---|---|---|---|
| 质量 | 分支调研事实带出处率 | 100%（无出处的事实不得出现） | A4 |
| 质量 | 自评审修正数 | 每阶段自评发现并修正的问题数 > 0（连续为 0 = 自评审失效，需检查清单有效性） | 本地运行日志 |
| 质量 | 💀 风险未处置数 | 待处理 + 已挂方案的风险条目数不随版本推进持续累积（跨阶段确认门批量核，出现长期悬空即告警） | 风险台账 |
| 信任 | 跨项目知识污染数 | 0 条 | A12 |
| 信任 | 并行会话串流数 | 0 条（同版本多会话并行回合互不污染） | A29 |
| 数据完整性 | 静默覆盖数 | 0 条（产物被外部改写时检出冲突并降级落修订槽，不覆盖主槽） | A27 |
| 信任 | 过期记忆干扰数 | 0 条（新结论必须覆盖旧结论） | A9 |
| 第二圈 · 质量 | 导入事实的出处率 | 100%（与「调研事实带出处」同一纪律，扩到导入物；**无出处的事实不得参与对账**） | 待第二圈 FR 立项 |
| 第二圈 · 质量 | 对账结论中无事实支撑的比例 | 0（第二圈最大的失效模式是 AI 编复盘——每条结论必须指向已导入事实） | 待第二圈 FR 立项 |
| 第二圈 · 闭环 | 「待验证」跨版闭环率 | 上版决策的「待验证」条目在新版开工时被对账（转已验证 / 已推翻 / 明示仍待验证）的比例 > 0；**分母现在就存在，分子为 0 即闭环未通** | 决策日志 |
| 第二圈 · 反指标 | 复盘段的过程信号占比 | 应随 M6 下降。当前封板复盘 100% 为过程信号（变更处置 / 回退分布 / 闸口 outcome / 方法论使用），**零结果信号**——该占比即闭环健康度 | release-notes |
| 成本 | 单次全流程 token 消耗 | 有上限且用户可见（具体数值待定，见 §12） | 产品内成本统计 |
| 性能 | 首 token 响应 | < 3 秒 | 本地运行日志 |
| 性能 | 知识检索延迟 | < 200ms | 本地运行日志 |

**第二圈两条质量护栏刻意采用「盯反面」口径**（无出处数、无支撑数），与 💀 告警采用悬空口径同理：**盯正面数量会制造它想防的病**——催出凑数的事实与编造出来的复盘。

#### 4.2.4 双圈模型（词义钉死）

- **第一圈** = 四阶段主线：想法 → 澄清 → 结构 → 原型 → PRD → 封板。**「闭环」指这一圈内的产物依赖与确认闸口闭合，不代表产品范围闭合。**
- **第二圈** = 上线后：事实回收 → 结果对账 → 项目级需求池 → 开工档案袋 → 开下一版（见 §11）。
- 定位所称「产品经理全流程」指**双圈完整闭环**，不是 PM 职业工作的全流程——需求评审会、开发排期、跨团队沟通、上线部署不在范围内（见 §5.2）。
- 术语纪律：**新增表述**描述一次流水线运行时不得再用「全流程」，改写「完整一圈」或「四阶段主线」（避免与产品级「全流程」撞词）。**存量出现处尚未清理**（本表「单次全流程 token 消耗」、§12 第 3 条、§1 优先级定义等），属待办的纯文案批次——**本条纪律只约束今后新写的文字，不追溯**（§12 第 9 条为对外词义的待决项）。
- 第二圈是**规划中的路线图，不是已实现能力**——对外表述须带时态。

---

## 5. 范围界定

### 5.1 MVP 范围（四阶段主线 + 快速通道与回退 + 变更治理 + Agent 内建自评审 + 阶段推进确认 + 可选技能分支 + 三横切 + 版本容器）

见 §7 功能需求，全部 P0/P1 条目。

**主线与分支**：澄清 → 结构 → 原型 → PRD 为**默认主线**（每阶段推进前 Agent 主动发起**阶段推进确认**，见 FR2a；质量由各 Agent **内建自评审**保证，不设末端评审，见 FR6）；主线是建议节奏而非硬向导——用户明确要求跳步时走**快速通道**（见 FR13），对上游产物不满时可**回退重做**（见 FR4）；过程中的新想法默认进**候选变更池**、不打断当前版本收敛（见 FR14）。竞品调研、独立毒舌评审等增值能力不在主线上，以技能/插件分支形式存在（见 FR3、FR6）。

### 5.2 非目标（明确不做）

**本节只列产品明确不做的方向。「本圈未做、下一圈要做」不是非目标——那类条目一律写进 §11 第二圈路线，不得混入本节。**

- ❌ Web 版 / 可分享链接（平台定死 macOS 原生）
- ❌ 原型**编辑**画布（只做生成 + 归档；Figma 式编辑、墨刀同步为可选旁支，见 §11.3）
- ❌ 多人协作 / 权限
- ❌ 多语言界面
- ❌ App Store 上架（GitHub 开源分发）
- ❌ 联网调研之外的云依赖（模型调用与调研需网络，其余全部离线可用）
- ❌ PM 职业工作的全流程：需求评审会、开发排期、跨团队沟通、上线部署——**「全流程」在本产品里指双圈闭环**（第一圈想法 → PRD，第二圈上线事实 → 下一版，见 §4.2.4 与 §11），不含上述人工环节
- ⏳ **第二圈（复盘与数据分析 / 新需求分析与优先级 / 下一版规划）不在 MVP 主线，是已确定的下一圈路线，见 §11**——v1.3.6 前本条曾误列为「❌ 非目标」，与产品定位直接冲突，已解除

---

## 6. 核心用户旅程

### 6.1 主旅程（首次使用）

```
用户输入一句话想法（"想做个健身打卡App"）
  （可选：输入框关联已有项目——右栏「面板」预览该项目已有产物，避免重复澄清已确认的问题；不关联则落「默认」项目）
  → 澄清 Agent 苏格拉底式追问（≤5 轮，每轮一个问题；信息型盲区批量走问题卡向导）
  → 用户确认澄清要点表（人工闸口 1）→ 确认坞分档路径 → 进入结构阶段
  → 结构 Agent 产出功能架构图 / 核心流程图 / 模块-页面映射表（Mermaid，三层可修改）
  → 【用户确认结构】（人工闸口 2）→ 确认坞分档路径 → 进入原型阶段
  → 原型生成（页面清单取自映射表、流程基准取自核心流程图）→ 用户浏览、点验原型
  → 【用户确认原型】（人工闸口 3）→ 确认坞分档路径 → 进入 PRD 阶段
  → PRD 生成（以已确认结构 + 原型为双重基准，每轮自评审：产出→自检→当轮修正＋漏项雷达四档声明）→ 产物落盘
  → 用户浏览产物：PRD / 原型 / 结构图（App 内预览）/ 各阶段自评审摘要（右栏「文件」Tab 台账 + 预览）
  → 可选：手动封板该版本（封板前须逐条处置候选变更池 → 两段式确认 → 生成本版复盘）；
    或单独触发独立毒舌评审（可选分支）拿第二意见
  → 过程中任一环节：对上游产物不满 → 回退坞选「重做原型 / 重做结构」→ 自动回退并重新生成，
    下游产物自动标记「已过期」
```

### 6.1a 分支旅程（可选能力按需触发，以竞品分析为例）

```
方式一（用户主动选用）：
  用户在技能库中选中「竞品分析」技能/插件，对该项目手动运行
    → 产出结构化竞品分析包（带出处），归档到该项目

方式二（对话自动触发）：
  澄清或任意阶段中，用户说"帮我调研下竞品" / "看看市面上有没有类似产品"
    → 系统识别意图，自动命中「竞品分析」技能/插件
    → 执行调研 → 结果带出处落盘，并可作为 PRD 生成的可选输入
```

**关键约束**：分支不阻塞主线——用户不做竞品分析也能直接走完 结构 → 原型 → PRD；分支产物存在时，PRD 生成 Agent 引用其事实（同样要求标注出处），不存在时 PRD 基于澄清要点与项目记忆生成。

### 6.2 沉淀旅程（产品越用越聪明的来源）

```
每轮对话/每阶段结束
  → 结论/约束/否决项进记忆层（新结论覆盖旧结论，不并存）
  → 萃取物按语义分流：「怎么做事」→ 方法论卡片（去重合并，相似度
    >0.92 合并；正文写时富化：定义/做法/适用边界 + 为什么有效）；
    「发生过什么」（偏好/事实）→ 记忆层「经验」（带出处与置信度，覆盖语义）
  → 记忆分 项目 / 全局 两档（版本只作溯源标签）；约束与否决项是保护层，
    上下文预算再紧也不裁；失效条目留碑文回链、可追溯「被谁覆盖」
  → 方法论被使用 → 该卡「实战注记」区 append 一条带项目出处的心得
    （只增不覆盖，越用越厚）；AI 另代写一条更具体的「怎么用的」案例草稿
  → 决策进决策日志（append-only，含 WHY 与被排除方案）；一个话题收尾时
    一次性写入完整结构，再由系统确定性渲染成按天 HTML 档案 + 版本总览
  → 下次任何阶段，以上内容自动注入（带来源、可追溯）；
    阶段开始另有方法论主动推荐（含理由、可拒绝）；
    回答底部列出本轮「参考知识卡」引用行，可点开卡片全文
  → 版本封板时做一次对账（变更处置 / 回退 / 闸口 outcome / 方法论使用），
    素材随版落 release-notes，并作为下版本的开工素材
```

### 6.3 多版本旅程

```
v1.0 封板发布 → 开 v1.1（继承约束与否决项，不继承讨论记录）
  → v1.1 可以推翻 v1.0 的决策，但决策日志能看到"为什么变"
  → 随时回看 v1.0 封板快照（逐字一致，只读）
  → 封板前的对账：候选变更池必须逐条毕业处置（纳入后续 / 放弃 / 顺延），
    否则封板被拦——不留"说不清"的尾巴
```

### 6.4 跳步与变更旅程（主线被打断时怎么办）

```
场景一 · 用户想快点出东西：
  用户在澄清阶段说"别问了，直接出 PRD"
    → Agent 不提问、不生成当前阶段产物，走快速通道直达目标产物
    → 被跳过的中间阶段写入跳过标记、不生成产物（可事后补做）
    → 目标产物照常走质量门与确认坞（快速通道省的是"逐步确认"，不是质量把关）
  用户只说"生成 PRD"而没有跳步措辞：
    → 先盘点影响 PRD 走向的未决项：1-3 个 → 出前置确认卡点选后自动出稿；
      ≥4 个 → 照常回答并列出待确定清单，提示"确定完这些再生成 PRD"

场景二 · 用户在流程中途冒出新想法：
  用户说"再加个消息通知"
    → Agent 不直接改产物，先给分诊意见 + 输出变更提案卡
      （分类 / 建议回退目标 / 影响清单：具体哪些产物、哪个模块受影响）
    → 用户裁决：纳入当前版本（回退重生成）/ 放进候选池 / 继续讨论
    → 默认进候选池——保住灵感，但不打断当前版本收敛

场景三 · 用户想先试试再决定：
  用户把某个会话标记为草稿
    → 该会话的产物只镜像到提案目录，主线零写入
    → 满意后「合并入主线」：对拷产物 + 推进主线阶段 + 提案毕业；
      不满意则放弃草稿，主线完全不受影响
```

---

## 7. 功能需求

### FR1 项目与版本管理（P0）

**描述**：一个产品 = 一个项目；一次发布 = 一个版本（v1.0 / v1.1 …）。版本是用户视角唯一的版本概念。

**用户故事**：
- 作为 PM，我要新建项目并输入一句话想法，以便开始全流程
- 作为 PM，我要在不同项目间切换且互不干扰，以便并行推进多个产品
- 作为 PM，我要为项目开新版本并继承上一版的约束与否决项，以便做迭代而非从零开始
- 作为 PM，我要手动封板一个版本并自动得到更新说明，以便归档一次发布

**需求明细**：
1. 新建项目即创建用户可见的真实文件夹（可在 Finder / 文件管理器中打开）
2. 项目主页展示：目标收敛句（可编辑）、长期约束列表、否决项列表（红色醒目）、版本时间线、四阶段状态点
3. 版本生命周期：规划中（定版本号+需求清单）→ 进行中 → 已封板（只读）
4. 封板：手动触发 + **两段式确认**（第一段说明后果与一行复盘摘要，第二段最终确认）；自动生成「本版更新说明」；封板后目录只读且 UI 明确提示；封板时风险台账中全部未处置的条目统一结算——每条风险有始有终（见 FR6）
5. **封板前必须完成变更对账**：该版本候选变更池内若有未处置提案，封板被拦并引导先去处置（见 FR14）——不留「说不清」的尾巴
6. 版本继承规则：继承 project 级约束与否决项；不继承讨论记录与中间稿
7. 版本对比：scope 差异 + 同名产物并排对比（项目主页提供入口）
8. 历史回看：旧版本产物与封板时**逐字一致**
9. 产物保存自动快照，支持回退（对用户无感）
10. **新建任务页的项目关联**：新建任务页输入框常驻「关联项目」选择器——不关联（默认）或关联任一已有项目。关联决定新任务的落盘位置（不关联 → 「默认」项目；关联 → 该项目，见下方「默认」文件夹规则）；右栏「面板」在新建任务页同样可用，内容随关联实时切换：**关联项目 → 文件 Tab 预览该项目文件**（项目有产物则显示、无则空态，让用户发消息前看清已有内容、避免重复澄清已确认的问题）；未关联 → 三 Tab 全部空态。**空态即真实状态**：新建任务页的决策日志 / 风险恒为空——决策与 WHY 是执行中才写入的运行时数据，新任务无记录不是缺失而是尚未开始；空态以引导文案说明，不填充示例数据
11. **新建任务页即开工台**：按当前时间给问候语 + 四个意图建议入口（写需求 / 看竞品 / 找方法论 / 继续上次），一键带入对应起点，降低「第一句话说什么」的启动成本
12. **左栏导航分两个模块**：任务（项目 → 版本 → 会话三级树）与空间（功能导航整页：知识库 / 决策日志 / 技能库等）分段切换，选中项驱动落段——避免把「进行中的事」与「沉淀下来的东西」混在同一棵树里
13. **项目主页即版本驾驶舱**：已封板时顶部置黄条提示最新封板版本；未封板版本提供封板入口（两段式确认）；变更池有未处置条目时封板被拦并给出处置入口

**任务列表三级层级展示（左栏导航）**：
1. 三级嵌套：第一层按**项目**划分 → 第二层按**版本号**划分 → 第三层展示该版本下的**会话**列表
2. 各层级命名规则：第一层项目名与磁盘文件夹同名（用户自定义）；第二层版本号采用语义化版本（`v主.次`，如 v1.0 / v1.1），封板版本显示只读标识；第三层会话标题默认取首条用户消息摘要（可重命名），并显示最后活跃时间
3. **第三层 = 会话窗口**：每个会话节点点击后打开对应的会话窗口；一个版本下可含多个会话（流水线全程、追问、单独调用某能力等）；流水线阶段进度不在导航树中展示，展示于项目主页的四阶段状态点（见需求明细 2）。任务列表头部提供「＋ 新建会话」按钮：在当前项目/当前版本下创建新会话并自动跳转到该会话窗口；未选择项目时落入「默认」项目；当前处于封版版本时落入该项目首个活跃版本
4. **「默认无版本号」文件夹（每项目固定一个）**：用户可能不知道具体版本号时，未指定版本的会话与文件（手动归档素材、临时讨论等）统一存放在该文件夹；后续归入版本时由系统自动迁移，不阻塞用户操作
5. **「默认」项目文件夹（系统预建，不可删除）**：当用户未选择任何项目时（首次启动、新建任务前的初始状态），在项目层级顶部作为初始展示项；用户选中任一项目后不再置顶显示。使用场景：首次使用的引导态、不建项目直接发消息的快速开始、外部调用未指定项目时的兜底容器
6. 层级导航与磁盘目录一一对应，导航操作不产生额外数据；会话内的结论、决策、知识点沉淀到所属项目，与会话本身解耦（会话可删，沉淀不丢）

**验收要点**：详见 §9 验收标准 A13、A19。

### FR2 需求澄清 Agent（P0）

**描述**：围绕一句话想法，对话式暴露需求盲区，产出结构化澄清要点表。全程采用「Agent 执行中主动提问 → 用户作答 → 继续推进」的交互模式（对齐主流 AI Agent 产品）：有疑问必问，不擅自假设。

**用户故事**：
- 作为 PM，我要 AI 针对我的模糊想法提出关键追问，而不是直接给我一个通用方案
- 作为 PM，AI 追问时我要能直接点选候选选项（也支持自由作答），以便低成本回答
- 作为 PM，5 轮追问还问不完时，我要系统强制收束放行而不是卡死流程

**需求明细**：
1. 苏格拉底式提问，每轮只问一个最关键的问题，最多 5 轮
2. **主动提问（必须保证）**：澄清过程中凡遇到影响后续产出的疑问，Agent 必须暂停向用户提问，用户作答后基于答案继续，不得自行假设跳过；交互回路固定为「提问 → 作答 → 继续」
3. **选项式提问**：每轮提问可附 2-4 个候选选项供用户直接点选，同时保留自由输入（可自定义答案或补充说明）；用户可跳过任一问题，跳过项按待定问题处理，流程不卡死
4. 每轮记录「已知信息 / 盲区 / 本轮新决策」
5. 5 轮耗尽仍有必填项缺失 → 强制收束：缺失项写入 open_questions 后放行，UI 明示
6. 结束产出澄清要点表，字段：目标用户 / 核心场景 / 核心价值 / 约束 / 待定问题
7. 用户确认要点表后，Agent 发起阶段推进确认进入结构阶段（这是主线第一个、也是最前端的人工闸口；结构确认是第二个，见 FR2a；原型确认是第三个，见 FR4）
8. 支持运行中随时回到澄清阶段（顺序是建议不是硬约束，不设强制向导）
9. **问题卡向导（多题批量作答）**：信息型盲区（事实类、可一次性问清的问题）由 Agent 输出问题卡，在输入框上方停靠卡的作答段以向导承载——多题逐答（单选 / 多选 + 自定义 + 跳过）→ 上一步回改（已答内容保留）→ 回顾页可跳题修改 → 确认提交；提交内容作为一条用户消息进入对话流（逐题答案留痕、可回看）
10. **选项式提问不限于澄清阶段**：任何阶段的 Agent 提问都可附候选选项供点选（自由作答始终可用），作答段优先于确认段显示——先把当前问题答完，再谈推进

**验收要点**：A1、A30。

### FR2a 结构设计 Agent（P0）

**描述**：澄清要点确认后、原型生成前，先把产品结构显式化——功能架构图、核心流程图、模块-页面映射表（复杂产品另出业务流程图）。结构与原型共同构成 PRD 的双重基准：只有这些准备到位，后续出原型、出文档才不会出现致命性问题。

**用户故事**：
- 作为 PM，我要在出原型之前先看到功能架构图和核心流程图，以便用最低成本发现结构性问题（漏模块、断流程），而不是到原型甚至 PRD 阶段才发现
- 作为 PM，图不对时我要能低成本修改——对话里说一句、App 里改源码、或直接改本地文件
- 作为 PM，简单产品我不想被迫画一堆图；复杂产品我要图足够细

**需求明细**：
1. 输入：已确认的澄清要点表（+ 可选竞品分析产物 + 项目记忆）
2. **产物**（Mermaid 源码写入 .md 开放格式文件）：
   - 功能架构图（模块 / 子功能层级）——必出
   - 核心流程图（P0 场景用户路径，闭环校验）——必出
   - 业务流程图——仅复杂产品（多角色 / 多实体）按澄清阶段判断的复杂度决定是否产出及粒度
   - 模块-页面映射表——必出，是原型页面清单与 PRD 功能需求的锚点
3. **粒度自适应**：简单产品允许轻量图（可合并架构 + 流程为一张）；复杂产品出全套——阶段必须有，深度可分级
4. **修改交互三层**：对话式改（AI 改图并展示变更差异）/ App 内改源码 + 实时预览 / 文件管理器直接改 .md（合法行为，索引兼容）
5. App 内嵌本地渲染（渲染引擎打包进应用，零外部依赖、断网可用）+「在浏览器打开」双入口
6. **结构确认闸口**：用户确认图后才进入原型阶段（主线第二个人工闸口）；确认前可基于反馈重新生成/调整
7. **过期传播（分级）**：结构确认后修改并重新确认 → 原型自动标记「已过期」（AI 标注影响范围：局部 / 全部）→ PRD 相应过期
8. **统一停靠卡**：输入框正上方只有**一张**卡（与输入区同宽、居中），按优先级切换内容段——**作答段**（存在待答问题，最高优先）→ **分支确认段**（分支执行待用户裁决，见 FR3）→ **确认段**（阶段闸口就绪）；段切换时卡体不重建、内容弹簧过渡，替代此前「确认坞 + 作答坞」两张卡同时叠挂的旧设计
9. **阶段推进确认（通用模式，适用于本 PRD 所有阶段推进点）**：每阶段产物落盘且自评审完成后，Agent 在停靠卡确认段主动询问是否进入下一阶段——**单页直接确认**：选择处理方式 → 页脚按钮随所选选项直接执行生效；「确认后会发生什么」由选项卡副标题以结果导向文案承载（你定稿了什么 / AI 接下来做什么），**不出现内部存储路径与架构术语**；**用户不确认不推进**；提交后卡收起、对话流内落确认记录胶囊（封版回看可见）；停靠期间输入框始终可用——自由作答中明确推进指令（「进入下一个阶段」）等价于选①并自动结算卡片，其余反馈等价于继续修改
10. **分档路径（确认坞不只「下一步」）**：选项按当前闸口分档——① 澄清后：进入结构 / **跳过结构直出原型** / **跳过中间阶段直出 PRD**；② 结构后：进入原型 / **跳过原型直出 PRD**；③ 原型后：进入 PRD / **到原型为止（本版不出 PRD）**。被跳过的阶段写入跳过标记、不生成产物，可随时补做（时间线补做入口）；「继续修改」与「稍后再说」不占选项位
11. **弹坞纪律（不骚扰）**：同一版本同一阶段的确认坞只主动弹一次——产物落盘时弹；用户选「稍后再说 / 继续修改」后该阶段转为**静默**（跨启动、跨会话持久生效，不重复弹、不重复留痕），推进改由用户发起（自由作答「进入下一阶段」或摘要条兜底行）；静默期间的修订轮由 AI 在回复末尾融一句推进邀请
12. **PRD 前置确认分诊（目标为 PRD 但用户没说要跳步时）**：先盘点影响 PRD 内容走向的未决项——1-3 个 → 出前置确认卡供点选（首选项 = AI 建议默认值，一句话说明默认依据或影响哪一章），用户选完自动串链生成 PRD；≥4 个 → 不出卡，照常回答并在末尾列出待确定清单，提示「确定完这些问题再生成 PRD」
13. **PRD 产物只能由系统落盘**：任何阶段都禁止 Agent 直接把 PRD 全文写进回复——PRD 落盘只走「前置确认卡提交」或「快速通道受理」两条路径，确保模板选档、评分卡与确认坞协议不被绕过

**验收要点**：A17、A17a、A18、A30。

### FR3 竞品分析（可选技能/插件分支）（P1）

**描述**：竞品分析**不是流水线固定阶段**，而是一个可独立触发的技能/插件分支。用户可主动选用，或在对话中表达调研意图时由系统自动命中触发。

**用户故事**：
- 作为 PM，我不一定要做竞品分析就能走完主线，以便对熟悉的领域直接产出 PRD
- 作为 PM，我需要调研时，我要在技能库里能主动选用「竞品分析」，或直接跟 AI 说"帮我调研下竞品"就能自动触发，而不是被强制走一遍流程
- 作为 PM，我要拿到每个竞品的定位、核心功能、差异点、可借鉴之处，且每条事实带出处链接，以便放心引用
- 作为 PM，查不到的信息我要看到明确的「未找到」标注，而不是被编造的内容误导

**需求明细**：
1. **手动触发**：技能库中「竞品分析」可对该项目手动运行
2. **自动触发**：对话中出现调研类意图（如"帮我调研下竞品""看看市面上有没有类似产品"）时，系统自动命中该技能/插件并执行；触发过程在检查器（开发者模式）中可追溯（命中了哪个技能、为何命中）
3. 每个竞品的分析含五要素：定位 / 核心功能 / 差异点 / 可借鉴 / 出处链接
4. 所有事实必须带出处；找不到标注「未找到」，禁止编造
5. 联网是 MVP 中该分支的必要能力（主线四阶段均不依赖联网）
6. 分支产物归档到项目，并作为 PRD 生成的**可选输入**（存在则引用并标注出处，不存在不阻塞）
7. 搜索源支持配置（具体供应商待定，见 §12）
8. 该分支模式是后续增值技能/插件的模板：**主线保持精简，增值能力以技能/插件分支形式扩展**（§11.3 的数据计算等旁支沿用此模式）
9. **分支执行前请用户裁决（防误触发）**：对话中自动命中的分支在真正执行前，由停靠卡的**分支确认段**请用户确认是否执行；分支执行是对用户最新消息的直接回应，故优先于常驻确认段显示；裁决完毕后阶段闸口状态自动回补，不丢失

**验收要点**：A4、A4a、A30。

### FR4 原型生成 Agent（P0）

**描述**：基于**已确认的结构产物**（模块-页面映射表 + 核心流程图）（+ 澄清要点 + 可选竞品分析产物 + 项目记忆）生成可点击原型，**在 PRD 之前**生成；原型经用户确认后才进入 PRD 阶段。

**用户故事**：
- 作为 PM，我要在写 PRD 之前先看到可点击原型，以便用最低成本验证产品形态、避免在错误方向上写长文档
- 作为 PM，我要原型的页面和流程严格对应我确认过的结构图，不要原型和结构各说各话
- 作为 PM，我要原型在断网时也能打开，以便任何场合都能演示

**需求明细**：
1. 基于已确认结构产物生成：页面清单取自模块-页面映射表（覆盖 P0 核心流程，3-5 个页面），流程基准取自核心流程图；页面间跳转可点击，**页面与映射表一一对应**
2. 单文件、零外部依赖（无 CDN / 无网络请求），断网可渲染
3. 灰盒线框风格即可，不追求视觉稿；只做流程演示，不实现业务逻辑
4. App 内嵌预览 + 「在浏览器打开」双入口
5. **原型确认闸口**：用户浏览、点验后明确确认（主线第三个人工闸口）；确认后原型与结构共同成为 PRD 生成的**双重基准**，进入 PRD 阶段
6. **原型修改**：确认前可基于用户反馈重新生成/局部调整；PRD 阶段发现原型缺口时，可回到原型阶段修改（修改后需重新确认，PRD 相应标记过期）；发现**结构**缺口时，可回到结构阶段修改（重新确认后原型自动标记「已过期」，分级标注影响范围：局部 / 全部）
7. 支持手动归档外部素材（截图 / 链接）
8. **原型冲突分槽（不静默覆盖）**：生成前比对磁盘上现有原型的内容指纹——若已被外部改写（用户在文件管理器里改过、或用外部工具生成过），新原型降级落到「原型-修订-月日-时分」修订槽、**主槽位不动**，并落一条用户可见的说明行；用户可在「文件」Tab 把任一修订版**设为主原型**（提升为基准；版本忙时拦截并提示）
9. **回退坞（对上游不满时的快捷入口）**：PRD 阶段停靠卡旁提供收起态小胶囊入口（不占空间的可选动作），展开可选「重做原型 / 重做结构」，回退后果写在选项副标题里；执行即回退 + 自动重新生成
10. **重做双模（修订 vs 推翻）**：回退重做时区分两种意图——在上一版基础上改（如「太简单了，加点效果」）→ 带上一版作修订基底，只改用户说的部分；明确推翻重来（如「完全重来」）→ 不带上一版，从零重做

**验收要点**：A15、A15a、A27、A31。

### FR5 PRD 生成 Agent（P0）

**描述**：以**已确认的结构与原型为双重基准**，结合澄清要点 + 项目记忆/知识（+ 可选的竞品分析产物），生成结构完整、优先级明确、**详略与产品复杂度匹配**的 PRD。

**用户故事**：
- 作为 PM，我要 PRD 严格围绕我确认过的结构与原型展开，不要文档和图、原型各说各话
- 作为 PM，我要拿到一份章节完整、引用有出处、不含 AI 私自夹带假设的 PRD
- 作为 PM，我要 PRD 的需求优先级在命中方法论（如 RICE）时按该方法论排序并说明理由
- 作为 PM，我要 PRD 的详略与产品复杂度匹配——简单迭代不被迫写十页长文、高风险产品不被两页糊弄，且我能看到 AI 为什么选这个详略程度（评分与理由），必要时一键切换
- 作为 PM，我要 PRD 里的成功指标数字是我给的或明确标「实施期建立基线」，而不是 AI 编的漂亮数——编造的指标会直接变成错误的验收标准

**需求明细**：
1. 输入基准：已确认结构（模块-页面映射表、核心流程图）+ 已确认原型（页面清单）+ 澄清要点表；**结构或原型未确认不得进入本阶段**
2. **分级模板集，确定性路由不变**：模板为三档——精简版（1-2 页，简单迭代 / 低风险小功能）/ 标准版（3-5 页，默认）/ 完整版（5-10 页，高风险 / 大范围 / 涉资金权限数据）；同目录整组管理、始终使用最新版（模板类内容不走模糊检索——过期模板比没有模板更危险）；三档共享章节骨架、差异在深度：精简版允许章节合并，完整版细化各章（数据指标含基线表、非功能需求分维展开）
3. **模板分级智能选择（透明可切换）**：进入 PRD 阶段时按三维度评分自动选档——技术复杂度 / 风险影响 / 范围广度（各 0-10）；**评分依据必须锚定上游已确认产物**（澄清要点表的复杂度与约束、结构产物的模块数与集成点、原型的页面数；迭代版本以版本语境信号参与判断），不凭空打分；阈值：均值 <4 → 精简版，均值 ≥7 或风险维度 ≥8 → 完整版，其余 → 标准版（默认兜底）；**评分卡（三维度分数 + 各一句理由 + 所选档）在生成 PRD 前的对话流展示，用户可一键切换三档任一**——模板选择本身是可解释决策，不是黑盒
4. **成功指标对话式定义（五步）**：「数据指标」章的数字指标按固定流程向用户获取——① 期望结果（开放式：做成什么样算成功）② 有无基线（有 → 用户提供当前值；无 → 该指标标「实施期建立基线」）③ 目标值（用户给数字）④ 测量时间点（点选或自填，如上线后 30/60/90 天）⑤ **口径（这个数到底怎么算：分子 / 分母 / 统计时间窗三项为核心，另可给数据来源与排除条件）**，且**正文不抄口径**——4.2 的口径位置只写引用槽 `[[SPEC:指标名]]`，落盘时由 App 用机读记录渲染替换，正文与记录不可能各写一套；**Agent 只引导对话，不建议、不编造任何具体数值**；口径是唯一允许 Agent 起草的项（因为它是定义不是数字），但**起草即假设态**：必须标注假设并经默认项卡确认，任何路径都不得自动升为已确认；提问沿用澄清阶段的交互回路（选项式 + 自由作答 + 可跳过），任一步跳过按待定问题处理（写入待定问题清单、该指标标待定），不阻塞生成；口径同步落一份机读记录 `04-prd/metric-specs.jsonl`（append-only，按指标名 last-wins；**只存口径不存数值**，数值的事实源是 4.2 表），它是第二圈 C 环结果对账的准入门——`confirmed` 且三项齐备的指标才可参与对账
5. 引用竞品分析等分支产物中的事实时标注出处文件；无分支产物时基于澄清要点与项目记忆生成，不引入输入之外的新假设
6. 功能需求与已确认结构（模块-页面映射表）及原型的页面/流程**一一对应**，结构与原型中不存在的功能不得写入 PRD
7. PRD 生成后自动产出 5-10 条可执行的验收用例（每条含：场景 / 输入 / 判定标准 / 失败模式）
8. 结构或原型阶段回退修改并重新确认后，已生成的 PRD 自动标记「已过期」（分级标注影响范围）
9. **经快速通道出稿（跳过中间阶段）走精简路径**：被跳过的阶段不生成产物，PRD 以现有已确认材料为基准起草，缺失信息按合理假设补齐**并在产物中明确标注为假设**（不冒充已确认结论）；模板选档照常执行（评分依据不足时按标准版兜底）
10. **PRD 落盘只由系统执行**：Agent 不得把 PRD 全文写进回复直接交付，落盘必须走模板选档 + 评分卡 + 确认坞协议（见 FR2a 第 13 条）

**验收要点**：A3、A6、A21、A23。

### FR6 Agent 内建自评审——每轮自检 + 漏项雷达 + 风险台账（P0）

**描述**：不设末端总评审阶段。每个主线 Agent（澄清 / 结构 / 原型 / PRD）的指令中内建自评审约束：**每轮产出后对照专属自检清单自检、发现问题当轮修正**，输出末尾附**漏项雷达四档声明**；其中识别的致命假设进入**风险台账**，形成「登记 → 挂方案 → 验证解除 → 留痕」的四态闭环。质量在产出过程中保证，而非产出后把关。

**「每轮」的边界（v1.3.8 补）**：寒暄 / 致谢轮不适用本条（见 FR18）——自检清单与漏项雷达是**产物**质量机制，没有产出的回合上执行它，只会得到一份为凑数而写的空表。此前这条边界从未写明，实测一句「你好」因此被要求逐项填完六档声明。

**用户故事**：
- 作为 PM，我要每个阶段的产出在当轮就经过自查修正，而不是最后被一锅端打回重来，以便省时省钱
- 作为 PM，我要能看到每个 Agent「自查发现了什么、修正了什么、还遗留什么」，以便信任产出质量
- 作为 PM，我要每轮输出末尾看到漏项声明——覆盖了什么、什么待我补充、什么被刻意跳过、哪个假设一旦不成立全盘崩塌——以便知道 AI 想到了什么、没想什么
- 作为 PM，我要致命漏洞假设不是一次性烟花：登记后我能采纳应对方案、方案落地后能验证解除、命中时能看到「当初预测 vs 实际发生」——红队是哨兵，不是表演
- 作为 PM，我不需要每轮都读一遍完整的四档声明，也别让状态播报把对话流刷满——新增的才提，完整对账我自己去台账翻
- 作为 PM，偶尔想要更苛刻的外部视角时，我要能单独触发一次独立毒舌评审，而不是被迫走固定流程

**需求明细**：
1. **每轮自评审**：Agent 每次产出（含多轮对话的每一轮）后对照自检清单自检，发现即改；自检与修正发生在当轮，不留到下游
2. **漏项雷达四档声明**（每轮输出末尾，四档必须声明、档内可为空）：
   - **判断依据**：每个点不是 AI 自由发挥，而是对照两项输入逐项对账——① 本阶段自检清单（维度固定、不可自选，见下条第 3 点）；② 上游已确认产物（结构对澄清要点、原型对映射表、PRD 对结构与原型）。判断质量上限取决于清单特化程度，下限由「维度固定 + 连续 3 轮零修正告警」兜住
   - ✅ **已覆盖**：本次已展开的维度/模块（**列具体名称，禁「均已覆盖」式空话**）——本质是「对账结果」
   - ❓ **可能遗漏**：信息不足导致的待定方向 + 需要用户补充的问题清单（走待定问题回路）——本质是「等输入」
   - ⏭️ **刻意不展开**：识别到但主动跳过的点 + 原因（超出本次范围 / 需单独展开）——本质是「边界声明」
   - 💀 **致命漏洞假设**（红队视角）：假设本次结论是错的，揪出「哪个核心假设一旦不成立、整个结论就崩塌」——本质是「证伪」
   - 分工硬规则：需要用户回答才能消除的进 ❓；现有信息即可证伪的进 💀
   - **💀 可为空是明文合法输出**：无真实致命假设时只许写一句「未发现致命漏洞，最脆弱的假设是 X」——禁止为填而填凑数
3. **各阶段自检清单**（漏项雷达 + 多视角检查，按阶段特化）：
   - 澄清：五字段盲区覆盖度；追问是否流于形式；是否未经澄清直接给方案
   - 结构：核心场景覆盖度（每个核心场景在流程图中有路径）；流程闭环（无断头路、无死循环）；模块边界清晰（无重叠、无孤儿模块）；映射表完备（每个 P0 模块都映射到页面）；与澄清要点一致
   - 原型：核心场景覆盖；页面跳转闭环；零外部依赖；**页面与模块-页面映射表一一对应**；与澄清要点一致性
   - PRD：功能需求与已确认结构及原型一一对应；引用出处齐全；无输入外新假设；模板章节完整
4. **决策 WHY 记录**：关键决策记全五要素（决策 / WHY / 排除方案 / 置信度 / 待验证，与 FR7 决策日志同构）。关键决策判据（满足任一即触发）：**跨阶段影响 / 不可逆 / 存在真实备选方案**（生成过程中真实写出过有实质内容的 B 方案——备选可验证；犹豫声明不可观测，不作为判据）；其余决策一句话 WHY 即可——防琐碎全记稀释真决策，也防漏记
5. **风险台账四态闭环**（红队从烟花变哨兵，2026-09-15 升级为四态台账）：
   - **登记带后果与建议**：💀 写入风险台账时必须带「后果」——对用户或进度的具体影响（缺省即不合格），并附一句可执行的**建议应对方案**（含依据与代价，如「本地队列兜底 · 代价：弱网首次签到延迟 1–2s」）；说不清后果的不允许登记
   - **建议前置陈列（v1.3.4）**：阶段确认门前，Agent 将本轮**待处理**风险的应对建议逐条汇总陈列在确认坞上方——每条给出 采纳（进入落实闭环）/「不等了，只记账」/ 暂不处理 三个去向；建议是阶段推进决策的正式输入，而不是登记时附带的一句话（验收 A38）
   - **四态**：**待处理**（登记后等用户决定）→ **已挂方案**（**采纳 ≠ 解除**：方案落地并确认没出事才算数）→ **已解除**（验证通过，回写决策日志）；或 → **已接受**（风险自留，封板时带入 PRD 的已知风险）
   - **结算点在阶段确认门**：跨阶段确认门时批量核「已挂方案 · 等验证」条目，不再由 AI 每轮自行扫描回核（防机械勾选）
   - **采纳落实闭环**：用户点「采纳方案」→ 系统代发受理 → AI 生成**实施执行包**（可预览、可注入下一轮）→ 流完成后状态转「已挂方案」并落一条带证据指针的决策日志；中途中断则保持「待处理」可重试；降级为「仅记账」时必须显式告知用户（不许无声降级）
   - **排队逃生门**：同版本排队期间提供「不等了，只记账」——立即按普通采纳处理并钉住排队坐标，不阻塞用户
   - **软上限**：活跃条目上限 3 条，超限时先执行收敛动作（关闭已证伪 / 降级为待定问题 / 合并同源）而非拒收——同时出现 4 条真实风险时，丢风险比破坏上限更糟
   - **封板终态**：封板时全部未处置条目统一结算——每条有始有终（见 FR1）
   - **右栏常驻（用户侧人话四态）**：风险台账在右栏「风险」Tab 常驻展示，分三分区——**待处理 / 已挂方案·等验证 / 已处理·留痕**；每条卡 = 一句话假设 + 「后果」警示标签 + 建议方案块；状态徽章用**待处理 / 已挂方案 / 已解除 / 已接受**四态人话（旧版「被说中了 / 安全落地」等枚举仅作历史条目只读兼容）；工程口径（状态枚举、软上限、收敛动作）在开发者模式（⌘D）展示，不进用户界面
6. **自评审对账 trace（对话流只报新增）**：漏项雷达四档声明的完整审计归宿在右栏「风险」Tab 顶部——**「N 轮对账 · 累计修正 M 处」计数器**，逐轮可展开看 ✅ 已覆盖清单与 ❓ / ⏭️ / 💀 计数；对话流只播报本轮**新增**（事件驱动），不再每轮重复刷一遍四档声明
7. **自评审摘要落盘**：每阶段产物附自评审摘要（发现的问题 / 已修正项 / 遗留问题 + 漏项雷达四档），UI 可见、可追溯
8. **自评漏项回补**：自评发现的澄清类漏项回写澄清要点表（open_questions），形成「自评审 → 澄清」回路；❓ 档条目同走此回路
9. **独立毒舌评审降级为可选分支**：技能库手动运行或 MCP `review_doc`（见 FR11）按需调用，对任意产物拿独立第二意见；不阻塞主线、不自动打回，默认不触发
10. **系统行静默化与开场承接**：⚡快速通道受理 / ⏳跨门风险提醒 / ✅阶段推进三类系统行不再在对话流单独渲染——其语义由 AI 当轮回答的**开场承接句**用大白话交代（「这轮我先跳过结构，直接把原型做出来」）；系统行仍留痕，作为回答链的链标记与值班单时间轴节点。AI 不得在无开场承接注入时自行播报状态变化
11. **已知取舍**：自评审与生成同体存在立场盲区（「灯下黑」），以清单化自检（维度固定、不可自选）+ 独立评审分支兜底；💀 有凑数与长期悬空风险，以「可为空合法 + 登记必带后果 + 结算点固定在确认门」兜底

**验收要点**：A16、A16a、A26、A32、A38。

### FR7 记忆与知识沉淀（读写双向）（P0 语义、P1 完整形态）

**描述**：对话中的结论、约束、否决项、经验、方法论卡、决策自动沉淀，并在后续阶段自动注入。**三层语义划分（v1.2.9）**：记忆层收「发生过什么」（what/why，会过时，覆盖语义），方法论卡片库收「怎么做事」（how，跨项目直接用，不会过时只会被改良，注记只增不覆盖）——判据是「会不会过时」。

**用户故事**：
- 作为 PM，我在第二轮对话不想复述第一轮的结论，系统应该自己记住
- 作为 PM，我推翻了早期决定后，系统要以新结论为准，不能新旧两条同时生效自相矛盾
- 作为 PM，我要随时点「这条记下来」手动沉淀，而不是只能等阶段结束
- 作为 PM，我在这个项目总结的方法论，换项目要能直接用（不是被当成会过时的记忆降级成假设）

**需求明细**：
1. **注入**：每个 Agent 工作前自动注入——有效记忆（按 项目 > 全局 的作用域）、相关方法论卡片、命中的方法论技能；阶段开始时另有方法论主动推荐（见 FR8）
2. **记忆覆盖语义（核心）**：新结论覆盖旧结论；旧条目标记失效并留回链，不并存、不物理删除。「过期记忆比没记忆更危险」
3. **「经验」条目（v1.2.9 新增）**：讨论中的用户偏好、领域事实（「发生过什么」）沉淀为记忆层「经验」条目——带出处（source_ref）与置信度，沿用覆盖语义；跨项目引用时按**假设态**注入（供记忆校准，见 FR8），不冒充通用结论
4. **方法论卡片**：结构化 schema（非自由文本），只收「怎么做事」；与现有卡片相似度 >0.92 时合并而非新建；同主题冲突时旧卡标失效
5. **实战注记（v1.2.9 新增）**：方法论被实际使用后，在该卡「实战注记」区 append 一条带项目出处与日期的心得——**只增不覆盖、越用越厚**（官方定义谁都有，注记才是资产）；技能文件同构支持
6. **手动入口 + 归属分流（v1.2.9 升级）**：对话中随时「这条记下来」→ 弹窗先做归属分流（默认判定可改）：「怎么做事」→ 方法论卡片（生成 + 相似卡片合并确认）；「发生过什么/为什么」→ 经验记忆（入记忆层，带出处）
7. **决策日志 → 决策档案**：每条决策记录五要素——决策 / WHY / 排除方案 / 置信度 / 待验证；append-only 永不删除；「待验证」未闭环条目在 UI 高亮；风险条目解除或命中时自动追加「预测 vs 实际」回写条目——当初预测的致命假设与实际发生的对照，是最值钱的复盘资产（见 FR6）。**写入时富化**：一个话题收尾时一次性写入完整结构（含用户诉求、备选方案、关键转折点、认知所有权标注——这条是用户原创还是 AI 建议被采纳），避免事后让 AI 猜。**展示层改为确定性渲染的档案**：系统把记录渲染成**按天 HTML 档案 + 版本总览**（零模型调用、毫秒级、内容比对幂等——内容没变就不重写；点「生成」则无条件全量重建，保证与记录严格一致）；档案是派生产物，**删了可随时重建**（与「删库可重建」同一条不变量）。入口三处：左栏「决策日志」整页（**决策 / 变更池**两个分段）、产物「按阶段」视图弹窗、右栏「决策日志」Tab（紧凑卡 + 点击展开五要素）
8. **记忆作用域（两档）**：**项目级 > 全局级**（具体性优先，同池新覆盖旧）；**版本不是作用域**，只作条目的溯源标签；跨项目知识默认隔离，检索 0 泄漏（记忆校准的经验条目除外——按假设态注入，可追溯）；「提升为全局」是两个记忆池之间**唯一**的流通通道，需用户显式操作
9. **记忆治理（P1）**：**保护层**——约束与否决项（硬边界）在上下文预算裁剪时永不丢，假设类经验最先裁；**碑文回链**——失效条目不物理删除，保留可追溯的碑文；**整理动作白名单**——整理只允许白名单内的动作（合并 / 失效 / 提升），不许 AI 自由改写；记忆管理页支持添加 / 编辑 / 整理 / 交接
10. **卡片库 UI（P1）**：列表 + 方法论名搜索 + 来源跳转 + 相似卡片合并弹窗 + **卡片详情 = 正文 + 实战注记时间线（只增不覆盖）**
11. **记忆抽屉（P1）**：当前注入的记忆列表，按两档作用域分组（含「经验」条目——带出处与置信度、假设态标签），失效记忆折叠，可跳转「被谁覆盖」
12. **AI 代写案例草稿（v1.3 新增）**：方法论被采纳时，系统先落一条确定性草稿（项目 / 版本 / 阶段 + 推荐时机线索），AI 再依阶段上下文代写一条更具体的「在哪个环节、怎么用的、预期结果是什么」——成功则追加为第二条（append-only，越用越厚），失败静默（确定性草稿兜底，不打扰用户）
13. **知识引用条（v1.3 新增）**：AI 回答底部列出本轮**参考知识卡**引用行（本轮注入命中的卡，确定性数据、注入即显示），chip 可点开卡片全文与案例——回答的「用了什么」不只在开发者模式可见
14. **决策回溯（v1.3.4 新增）**：决策条目与产物 / 风险 / 版本建立**双向指针**——① 任意产物卡、风险卡提供「回溯决策」入口，一键直达当时依据的决策五要素（跨条目可多对多，回溯页按影响关系聚合同批决策）；② 回退 / 重做（FR4 回退坞、FR14 重规划）时，被跨越阶段的历史决策**自动注入重做上下文**——决策不因回退丢失，重做不是失忆重来；③ 沿用或推翻在新决策中**显式标注**（沿用注明「沿用 10-06 决策」，推翻触发旧决策碑文回链，见第 9 条）——回溯链完整可查（验收 A39）

**验收要点**：A2、A9、A10、A14、A22、A27、A33、A39。

### FR8 技能库——方法论技能化（P0 语义、P1 完整形态）

**描述**：PM 方法论作为「技能」入库；仅元数据常驻，命中场景才加载正文；阶段开始时主动推荐适用方法论。

**用户故事**：
- 作为 PM，我在做优先级排序时希望系统自动用上 RICE 方法论，而不是我背诵它
- 作为 PM，我要确认没被用到的技能没有混进上下文（不白花钱、不被干扰）
- 作为 PM，进入一个新阶段时希望系统提示我「这类项目通常用得上什么方法论」及理由——但要能拒绝，别推销
- 作为 PM，我上次用某个方法论踩过的坑，这次再用时希望系统提醒我，而不是重蹈覆辙

**需求明细**：
1. 技能 = Markdown 文档 + 元数据（名称 / 适用场景 / 标签）
2. 渐进式披露：仅元数据常驻；命中场景才加载对应技能正文，未命中技能的正文不进入上下文（可验证）
3. **方法论主动推荐（v1.2.9 新增）**：阶段开始时 Agent 扫描卡片库 + 技能库，按项目特征推荐 1-3 个适用方法论——推荐**必含理由**（引用项目特征或卡片注记，非空话）、**可拒绝**、同阶段不重复推荐被拒项（防推荐变骚扰）；推荐/采纳/拒绝全程可追溯
4. **记忆校准注入（v1.2.9 新增）**：Agent 使用某方法论时，自动注入记忆层中该用户的历史使用倾向（「上次用 KANO 把所有需求都标成基本型」类经验，按假设态注入）——方法校准不了使用者，记忆补位
5. **技能实战注记（v1.2.9 新增）**：技能被使用后同样在其「实战注记」区 append 心得（与方法论卡同构，只增不覆盖）
6. 每个技能含「学这个 / 用这个」双模式入口（学模式 = 讲解练习，见 §11.3 旁支；用模式 = MVP）
7. 技能库 UI（P1）：列表 / 搜索 / 正文预览（含实战注记时间线）/ 命中历史计数
8. 初始内容：预置 30+ 条真实技能与卡片（含 KANO、RICE、用户故事地图等），产品第一天不是空壳；产品复盘中的方法论心得预填为初始实战注记
9. **知识卡写时富化（v1.3 新增）**：卡片正文强制三段结构（**定义 / 做法 / 适用边界**）并附 **「为什么有效」原理段**——避免单行整段卡与「只有结论、没有原因」，让卡片在被回读时能直接指导下一次决策
10. **推荐收口到知识库整页（聚光灯，v1.3 新增）**：方法论推荐在左栏「知识库」整页以「本阶段聚光灯」呈现——**一次主推一张**（匹配度 + 为什么现在），处置完翻下一张；可采纳、可跳过，同阶段不重复推被拒项；推荐处置完落一句结算（采纳 N 张 · 跳过 M 张，案例已记进各自卡片）；左栏导航带**待处置数字胶囊**，不错过也不打扰

**验收要点**：A11、A22、A33。

### FR9 上下文透明化（P1，差异化核心）

**描述**：每次 AI 请求注入的方法论卡可视，检索过程可追溯（开发者模式），「没用什么」可证明；AI 回答的思考过程与技能调用在对话内可见；主动推荐有理由、可追溯。

**用户故事**：
- 作为 PM，我要看到这次回答注入了哪些方法论卡（相关度、来自哪个作用域、积累了多少条实战注记），以便信任它的结论
- 作为 PM，我要能验证「AI 没有偷偷用别的项目的内容」，以便放心存敏感资料
- 作为 PM，我要看到 AI 回答前想了什么、用了哪些技能（思考过程），以便判断结论是否可信、何时需要追问
- 作为 PM，系统推荐某个方法论时我要能看到理由，拒绝后也别再纠缠

**需求明细**：
1. **对话思考卡**（v1.2.6 新增，v1.3.8 内容口径收口）：Agent 每轮回复正文之上前置思考卡——① 思考中：「正在思考…」流光文字 + 阶段时间线 + 技能行 + 工具行；② 完成后默认折叠为摘要行「思考了 Ns · 技能 · 工具 ×N」，**有结构化行时**点击展开工具调用行（工具名 + 人话结果）与技能调用行（命中技能 + 触发词 + 注入内容 + 耗时）。**卡片只展示产品自己生成的文案，不展示模型原始思维链**（v1.3.8）：思维链含内部字段名与实现自述，属 §5 用户信息展示规范的禁列内容；无工具/技能行且无时间线时整行不可点，不渲染展开箭头、也不造「无内容可看」的提示文案。**阶段时间线（v1.3 新增）**：一次用户消息触发多跳（判断 → 跳步/回退 → 重做）时，思考卡展示阶段时间线——已完成的跳打勾、当前跳流光，用户能看清「现在跑到哪一步了」
2. **右栏收敛为三 Tab（文件 / 决策日志 / 风险）**（v1.3 改造）：原「知识点」Tab 移除——语义检索与方法论推荐**收口到左栏「知识库」整页**，右栏回归「会话伴随上下文」；「产物」Tab 更名「文件」并升级为**分区台账**：上半区扫编号产物目录按四类（文档 / 原型 / 图表 / 报告）归类 + 类型筛选 + 点击整行预览 + 右键菜单（预览 / 添加到对话 / 设为主原型），下半区为版本目录全量内容的**工作空间文件**档案树（面包屑标注磁盘目录，与 Finder 口径一致），两区之间可拖分隔条调整高度
3. **值班单交接条（v1.3 新增）**：一次用户消息触发多次 AI 调用时，每个续段顶部渲染一行段级计量——进行态给「计时 + 已生成字数」，完成态定格为「耗时 + 步数 + 阶段」，点击展开链内交接明细时间轴（历史会话重开同样可见）
4. **上下文检查器**（开发者模式，⌘D 切换）：token 构成条（规则 / 记忆 / 技能正文 / 检索 / 历史）、检索 trace（query → 命中条目 + 作用域标签 + 相似度）、未命中技能列表（证明没被注入）、**主动推荐 trace（推荐了什么/理由/采纳或拒绝/同阶段防重推荐）**、**记忆校准注入 trace（注入了哪条「经验」· 假设态标签）**、风险台账工程口径（状态枚举 / 软上限 / 收敛动作）、**原始思考全文（v1.3.8 新增）**——本轮回复的思维链原文只在这里可见，对话气泡不渲染；它是「AI 这次到底怎么想的」的排查出口，沿用 v1.2.1「工程调试信息移入开发者模式」的同一分工，不另开面向用户的开关
5. 每个 AI 输出必须有来源入口：回答底部「参考知识卡」引用行可点开卡片全文（见 FR7）；卡片跳回出处、检索结果点开原文

**验收要点**：A11、A12、A20、A22、A30 的可观测支撑。

### FR10 数据资产与本地优先（P0）

**描述**：所有产物是用户本地真实文件夹中的开放格式文件，非数据库黑盒。

**用户故事**：
- 作为用户，我要在文件管理器里直接看到并手动编辑我的所有数据，以便随时迁移、备份、用别的工具打开
- 作为用户，我要索引坏了能一键重建且不丢数据，以便不怕系统出错

**需求明细**：
1. 产物全部为 Markdown / JSONL 等开放格式，存于用户可见目录
2. 用户在文件管理器中手动修改文件是**合法行为**，系统需兼容（索引可从文件重建）
3. 除模型调用与联网调研外全部功能可离线
4. 无账号、无遥测、数据不出机；模型 Key 仅存本地安全存储
5. **派生产物一律可重建**：索引、决策档案等派生内容删掉后可从事实源（Markdown / JSONL 文件）完整重建，不产生数据丢失——「删了能重建」是全仓同一条不变量
6. **把本地文件带进对话**：用户可在输入框引用本地文件（或从「文件」Tab 右键「添加到对话」），系统读盘后作为本轮材料注入，并附反幻觉约束（只依据文件内容作答、不外推）；引用文件缺失时逐条明确标注（不静默丢弃），越界路径拒绝读取

### FR11 开放能力——MCP（P1）

**描述**：本产品可被外部 AI 客户端（如 Claude Desktop / Cursor）作为工具调用。

**用户故事**：
- 作为 Builder，我要在我的 AI 编程工具里直接调用「生成 PRD / 生成原型」能力，以便融入现有工作流

**需求明细**：
1. 本地进程级协议（stdio），无远程暴露
2. 对外工具：需求澄清 / 生成结构图（异步）/ 生成原型（异步）/ 生成 PRD（异步）/ 独立毒舌评审任意文档（即 FR6 可选分支的按需入口）/ 任务查询
3. 长任务异步：返回任务 ID，轮询取结果；App 重启后未完成任务标记失败由调用方重试
4. MCP 触发的运行与 UI 内运行共享同一条流水线状态（界面上可见）
5. 一键「复制外部客户端配置」按钮
6. 前置条件按主线顺序校验：生成结构图需该版本已完成澄清；生成原型需该版本已有**已确认的结构**（否则报错并引导先完成结构确认）；生成 PRD 需该版本已有**已确认的原型**（否则报错并引导先完成原型确认）
7. 入参缺省兜底：调用未指定项目 → 落「默认」项目；指定项目未指定版本 → 落该项目的「默认无版本号」文件夹（见 FR1）；不因缺参报错丢数据，产物事后可迁移

**验收要点**：A7。

### FR12 设置、模型与成本（P0 最简、P1 完整）

**描述**：用户自带模型 Key（BYOK），分阶段配置模型，成本透明。

**需求明细**：
1. API Key 存本地安全存储，不入库不入 Git
2. 每阶段可配置不同模型（澄清用便宜的、生成用强的）
3. 成本统计：本次 / 本月 token 与费用
4. 单次全流程 token 上限可设
5. 数据目录位置可查 + 「重建索引」入口
6. **思考力度档位（v1.3 新增）**：模型调用支持 **Low / Medium / High / Max** 四档思考力度（默认 High），本地持久化；按调用类型自动适配——意图分类等内部辅助调用强制压到最低档（省时省钱），生成类调用沿用用户所选档位

### FR13 快速通道——跳步直达（P0）

**描述**：主线是建议节奏，不是硬向导。用户明确要求跳过中间环节时，系统直达目标产物——**省掉的是「逐步确认」，不是质量把关**。快速通道有两个入口：用户对话中的跳步指令，以及确认坞里的分档路径选项（见 FR2a）。

**用户故事**：
- 作为 PM，我已经很清楚要什么，不想为了流程完整而多走两道确认，我要一句「别问了，直接出 PRD」就能拿到东西
- 作为 PM，我要知道哪些环节被跳过了、之后还能不能补做，而不是糊里糊涂拿到一份不知道底细的产物
- 作为 PM，我说「生成 PRD」时如果还有几个关键取舍没定，我希望系统先问我这几件事，而不是先出一稿再让我返工

**需求明细**：
1. **触发条件必须是用户明确要求跳步**：措辞含「直接出 / 别问 / 不用确认 / 跳过确认」一类，或明确表达不想逐项确认；只说「生成 PRD / 做个原型」而无跳步措辞不算跳步请求
2. **合法跳步范围**：① 澄清后 → 直达原型或直达 PRD；② 结构后 → 直达 PRD；③ 原型后 → 到此为止（本版不出 PRD）
3. **跳过即留痕**：被跳过的中间阶段写入跳过标记、**不生成产物**（不伪造一份空产物充数），阶段位置按闭环链重新推导；跳过事件记入事件流，用户可在时间线上看到
4. **可事后补做**：跳过的阶段保留补做入口——用户随时可回来补课（补做时删除跳过标记、正常生成产物）
5. **目标产物照常走质量门**：跳步不豁免自评审、不豁免确认坞、不豁免模板选档——到手的仍是经确认的产物
6. **跳步请求信息不足时不得反问拦截**：缺失项由系统按合理假设补齐并在产物中标注（这正是快速通道的语义——用「直接出」换速度）；但**禁止 AI 主动建议跳步**，只转述用户明确意图
7. **PRD 前置确认分诊**：用户要求生成 PRD 但无跳步措辞时，先盘点影响 PRD 内容走向的未决项——1-3 个 → 出前置确认卡点选（首选项 = 建议默认值，一句话说明默认依据或影响哪一章），选完自动串链出稿；≥4 个 → 不出卡，照常回答并在末尾列出待确定清单（见 FR2a 第 12 条）
8. **PRD 只能由系统落盘**：任何阶段禁止 Agent 把 PRD 全文写进回复直接交付（见 FR2a 第 13 条）
9. **受理反馈融进回答**：跳步受理不再单起一条系统播报行，由 AI 当轮回答的**开场承接句**交代（见 FR6 第 10 条）

**验收要点**：A23、A32。

### FR14 变更治理——候选变更池与草稿预演（P0）

**描述**：产品在推进过程中一定会冒出新想法。系统的立场是：**保住灵感，但不打断当前版本的收敛**。新想法默认进候选变更池；要真改，必须走「提案 → 用户裁决 → 影响清单 → 回退重生成」这条显式路径，且封板前必须清账。

**用户故事**：
- 作为 PM，我聊到一半突然想到「还得加个消息通知」，我不希望这一句话就把已经确认好的结构和原型全推翻，但也不想这个想法丢掉
- 作为 PM，我要知道这个新想法到底影响什么（哪些产物、哪个模块），而不是 AI 轻描淡写地说「小改动」
- 作为 PM，我想先按新想法试做一版看看效果，试完不满意就把草稿扔掉，主线一点不受影响
- 作为 PM，封板时我要清楚这一版到底放进去了什么、放弃了什么、什么留到下一版——不想留一笔糊涂账

**需求明细**：
1. **变更提案卡（AI 只分诊、不裁决）**：用户消息引出新想法 / 新诉求或要求重做上游时，Agent **不生成本阶段产物**，先给一两句分诊意见，再输出变更提案块（由系统转成提案卡交用户裁决）——AI 不得替用户宣布「将执行回退」
2. **提案卡要素**：新想法概述（提炼非照抄）/ 分类（局部修订 · 页面流程 · 模块核心 · 目标范围 · 需验证）/ 建议回退目标（澄清 / 结构 / 原型）/ **影响清单**（具体到产物文件与部位，如「核心流程图 · 支付模块」）/ 重做模式（修订 / 推翻）
3. **影响清单纪律**：建议回退时必须列出具体影响；**列不出具体影响的，说明这不是回退级变更**，应走本阶段正常修订，不得输出提案块（防轻描淡写）；**App 侧双重核验**：提案登记时按产物依赖图解析影响清单，②/③ 回退建议引用不到任何具体产物的，不采信回退目标（提案卡降级为仅登记，想法级纳入走 ① 增补澄清兜底）
4. **默认进池**：全新功能 / 范围变化类诉求（超出既有澄清要点表范围）**默认建议进候选池**——保住灵感、不打断当前版本收敛；仅当用户明确表示「这版必须有」时才建议立即回退（且新需求先回澄清判断能不能做、适不适合做，禁止静默并入本阶段产物）
5. **用户裁决**：纳入当前版本（执行回退 + 重生成）/ 放进候选池 / 继续讨论（不采纳）；裁决结果 append-only 留痕
6. **候选池与毕业仪式**：左栏「决策日志」整页的**变更池**分段是池内提案的处置现场——**封板前池内每条提案必须逐条毕业处置**（纳入后续版本 / 放弃 / 顺延），否则封板被拦并引导先去处置
7. **草稿预演（先试再定）**：会话可标记为草稿——该会话的产物只镜像到提案目录，**主线零写入**；满意后「合并入主线」（对拷产物 + 推进主线阶段状态 + 提案毕业 + 决策留痕），不满意则放弃草稿（主线完全不受影响）；合并入口要求当前选中即目标版本（否则按钮禁用并引导切换）
8. **封板复盘**：封板时对本版的变更处置分布做确定性统计（见 FR1 与 FR15）
9. **产物依赖图（影响分析的推理底座）**：系统维护一张产物依赖图——节点为真实产物（澄清要点表 / 功能架构图 / 核心流程图 / 业务流程图 / 模块-页面映射表 / 交互原型 / 产品需求文档 / 竞品分析报告），边为**严格锚点**（页面清单 ← 映射表、流程基准 ← 核心流程图、PRD ← 映射表 / 核心流程图 / 原型 / 竞品分析）；任一产物变更可**确定性推导下游受影响集合**（不靠 AI 自由联想）。影响清单中的自由文本引用（带路径、文件名或产物名均可）由依赖图解析为已知产物，解析不出即视为「未引用具体产物」
10. **重规划循环（计划 → 执行 → 重生成 → 过期清除）**：变更被采纳执行回退时，系统先按依赖图生成**确定性重规划计划**——「将依次重做哪些产物」（如 ② 回退 → 功能架构图 → 核心流程图 → 模块-页面映射表 → 交互原型 → 产品需求文档；推翻重做时注明），随回退确认行可见化后再执行；补做跳过阶段不产计划（本无产物可失效）。计划文案只含产物中文名、不出现内部路径（对齐界面文案原则）；计划与状态机的过期传播保持一致（由回归测试钉住）

**验收要点**：A24、A25、A37。

### FR15 多会话并行与后台完成（P0）

**描述**：一个版本下常有多个会话在跑（主线推进、追问、单独调用某能力）。系统不再用一把全局锁把同版本的会话串成一队，而是按**版本级 / 会话级**判据放行；生成中途切走也不丢结果。

**用户故事**：
- 作为 PM，我在一个会话里等长文档生成时，想在另一个会话里继续问别的，不希望被卡住
- 作为 PM，我要在生成中途切去别的项目看看，回来时结果还在，而且落在正确的会话里
- 作为 PM，我采纳一条风险方案时不想因为别的会话正在生成就被拒绝

**需求明细**：
1. **版本级 / 会话级忙判据**：同一版本内的多个会话可并行回合——只有「**发起动作的会话自己正忙**」与「**同版本的结构闸**」才拦截；其他会话正在生成不再阻塞本次采纳 / 发送
2. **版本级状态写入仍串行**：采纳、阶段推进、确认等会改动版本级状态的动作，按版本归属排队串行执行（防竞态），但排队期间给出可见状态与逃生门（见 FR6 的「不等了，只记账」）
3. **后台完成**：用户切走后任务继续跑，回复按**发起会话**钉回原会话并落盘（不串到当前会话）；同版本内后台完成照常触发闸口；**跨版本**后台完成不推进闸口，落一条「需回发起会话处理」的挂起行
4. **会话互不污染**：并行会话的流式状态、停止生成、队列路由均按会话隔离——停止生成只停目标会话；历史投影按会话过滤
5. **写冲突保护**：产物写入带指纹校验——磁盘内容与预期不符时判定冲突，**拒绝写入并报错**（不静默覆盖用户的手改）；并发追加记录用进程级写锁串行化，不丢行
6. **封板复盘（对账）**：封板时确定性聚合本版信号——变更处置分布 + 回退分布（集中在哪个阶段）+ 闸口确认 outcome 分布 + **方法论使用报告**（采纳 / 被跳过 / 连续被跳过需校准的卡）；一行摘要进封板确认弹窗，完整段落持久化进 release-notes 尾部，并作为下版本的开工素材。**纯磁盘事实聚合，不虚构、不经 AI 转述**

**验收要点**：A28、A29。

### FR16 本轮任务计划——先计划后执行（P0）

**描述**：②③④ 阶段生成或修订产物的回合，Agent 在回复最开头先输出「本轮任务计划」卡（任务目标一句话 + 编号步骤），随后按计划逐项生成产物、自评审对照计划收尾——阶段内执行从「单次大生成」升级为 plan-act-reflect 三段，计划对用户可见可核对（机制同步 design.md v0.10.2 §6.12）。

**需求明细**：
1. **计划卡内容**：任务目标一句话 + 3-6 个编号步骤（修订轮可缩至 1-3 步，只列将要修改的点）；每步具体可验收（做什么、产出什么，可附依据），禁止「优化流程」式空话
2. **计划锚定执行**：产物块与计划步骤对应——计划里没有的不凭空产出，计划里列了的必须落实；全部产物输出后自评审对照计划逐项自查，发现偏差当轮修正
3. **可见性**：计划卡先于正文与产物渲染（流式与最终消息同位）；点击可选中复制；随回复原文留痕、历史会话可回看
4. **豁免规则（防仪式化）**：歧义反问、输出变更提案、输出快速通道、纯问答讨论的回合不输出计划卡；① 澄清为对话阶段不适用
5. **不改变闭环**：计划是增强不是依赖——计划缺失或不合法时回复照常解析落盘，产物仍走机器门与确认坞

**验收要点**：A40。

### FR17 Agent 主动简报——主动开口，不只在被问到时说话（P0）

**描述**：Agent 的两个主动触发点：自评审新登记风险后主动发起风险简报；版本封板被变更池拦截时主动发起催办回合（机制同步 design.md v0.10.2 §6.13）。简短、克制、不打扰。

**需求明细**：
1. **风险简报**：新风险登记成功后，Agent 主动追加一条简短回合——逐条点评（为什么值得关注 + 建议怎么处理）、指出最该先处理的一条、结尾引导右栏「风险」面板逐条决定（采纳方案 / 接受风险）；全程不超过 6 句
2. **变更池催办**：封板被池内未处置想法拦截时，Agent 主动逐条给**明确的**处置建议（纳入后续版本 / 放弃 / 顺延 + 一句理由，不和稀泥）、引导左栏「决策日志」裁决；全程不超过 8 句；同一池内容不重复催办，池内容变化后可再催
3. **不打扰纪律**：版本已封板、该版本有生成在途、快速通道进行中、采纳落实在途——任一命中即静默放弃，不排队不重试（简报是增值不是必需，台账与拦截弹窗已承载事实）
4. **口吻与边界**：以产品合伙人口吻说人话，不出现内部术语；禁止输出任何产物协议块；主动回合不阻塞、不抢用户的输入权

**验收要点**：A41、A42。

### FR18 寒暄轻量轮——非阶段轮（P0）

**描述**：用户发出的明确问候或致谢，不该被当成一次需求提待。此前产品**没有寒暄这条路**：一句「你好」照样按所处阶段走完整流水线、背完整协议、被 FR6 要求逐项填漏项声明——实测 **127 秒、两次串行 AI 调用、每次 2.2 万 token 输入**，而用户得到的只是一句客套。现为其单开一条轻量轮（机制同步 design.md v1.3.8 §6.4.2）。

**用户故事**：
- 作为 PM，我打开应用随口打个招呼，不该等两分钟才看到回应，以便不把产品当成一台反应迟钝的机器
- 作为 PM，我道一声「辛苦了」，不需要 AI 为此输出一份自检清单和四档声明

**需求明细**：
1. **只认明确的问候与致谢**：整句归一化后逐字命中寒暄/致谢用语才走轻量轮；「好的」「嗯」「ok」「继续」这类纯推进语**不算寒暄**——它们在 ① 澄清语境里可能承载确认语义，误判的代价是吞掉用户的真实意图，不判的代价只是慢
2. **带任何东西都不算寒暄**：附了图片、引用了文件、或问候后面接了任何实义内容（「你好，帮我看下原型」「你好吗」），一律走常规阶段轮
3. **轻量轮该做什么**：一两句自然短答，可顺带一句能帮用户做什么；**不产出任何产物、不做内建自评审、不出计划卡、不调用工具、不推进阶段、不落任何文件**
4. **轻量轮不付的钱**：不组装阶段协议骨架、不做向量检索、不做技能判定、不注入记忆/技能/检索材料——寒暄不需要项目背景（这是省时的主要来源，不是「少说两句」）
5. **不改变任何既有链路**：判闸排在封板只读、插话入队、竞品命中、草稿预演等所有既有分支之后，只新增一条出口，不截断任何一条现有出口
6. **成本可见**：轻量轮的用量照常计入成本面板（FR12），不因是短答就不记账

**边界**：判不准时一律按常规阶段轮处理——本条的失效方向是「维持现状」，不是「答错」。

**验收要点**：A43。

---

## 8. 非功能需求

| 项 | 要求 |
|---|---|
| 响应 | 首 token < 3s；知识检索 < 200ms |
| 输出吞吐 | **流式渲染量必须常数化**——界面每秒重渲染的字符量不随输出文本长度增长（长文档生成不得越写越慢）；「模型已经输出完但界面还在慢慢显示」属不可接受状态 |
| 离线 | 主线四阶段（除模型调用外）与结构图、原型渲染全部离线可用；联网仅竞品分析等分支需要 |
| 安全 | Key 不落明文；调研抓取做内网地址防护；无遥测 |
| 数据完整性 | 落盘必校验（防「说成功但没写进去」）；产物快照可回退；**写入前校验内容指纹，被外部改写时检出冲突并拒绝静默覆盖**；并发追加记录串行化、不丢行 |
| 长输出完整性 | 回答撞到输出长度上限时**自动续写并合并为一条回复**（不出现半截回复或重复段落）；续写失败给明确的截断提示与「重新发送」入口 |
| 失败处理 | 模型端限流 / 超时按退避策略自动重试，并以人话向用户说明（不暴露原始错误码）；流式连接挂死有总时长上限保护，不会无限等待 |
| 并发 | 同版本多会话并行回合互不污染；切换上下文不丢进行中的任务，结果落回发起会话 |
| 可观测 | 每次检索可查作用域与命中来源；上下文构成可回放 |
| 平台 | macOS 原生 App（版本与芯片要求见技术文档） |

---

## 9. 验收标准（47 条）

| # | 场景 | 判定标准 |
|---|---|---|
| A1 | 输入一句模糊需求 | 产出五字段齐全的澄清要点表；澄清中凡有疑问 Agent 主动提问（开放式或选项式，可点选可自由作答）且作答后继续，无擅自假设跳过；5 轮耗尽走强制收束且待定问题非空；不直接给方案 |
| A2 | 项目内有历史文档后检索提问 | Top3 命中率 ≥70%（30 条人工标注样本），且标注来源 |
| A3 | 问「PRD 该包含哪些模块」 | 100% 返回当前版模板，0 次召回历史版本 |
| A4 | 在技能库手动运行竞品分析 | 五要素齐全；返回含出处；查不到标「未找到」，不编造 |
| A4a | 对话中说"帮我调研下竞品" | 自动命中竞品分析技能/插件并执行；触发命中记录可在检查器（开发者模式）追溯；主线未被阻塞或重置 |
| A5 | 产物落盘 | 写入后回读校验一致，不一致报错 |
| A6 | PRD 生成后 | 自动产出 5-10 条四要素齐全的验收用例 |
| A7 | 外部 AI 客户端经 MCP 调用 | 能发现并成功调用，异步任务可轮询到结果 |
| A8 | 流水线中途退出重开 | 恢复到中断前状态，不从头开始 |
| A9 | 早前定「不做多用户」，后改口 | 以新结论为准；旧结论被覆盖不并存；后续产物（原型/PRD）不包含多用户 | 忘记早期约束；新旧结论同时生效自相矛盾 |
| A10 | 说「这条记下来」 | 弹窗含「方法论卡片 / 经验记忆」归属分流（默认判定可改）；方法论卡四字段齐全并可被检索；同一概念第二次触发时合并不新建；偏好/事实类默认分流至经验记忆（带出处与置信度） |
| A11 | 提问命中某方法论技能 | 仅加载该技能正文；未命中技能正文不出现在上下文（token 可测） |
| A12 | 切到项目 B 提问 | 检索结果 0 条来自项目 A（trace 日志可验证） |
| A13 | 在 v1.1 回看 v1.0 产物 | 与封板快照逐字一致；决策日志可见推翻记录 |
| A14 | 决策写入日志 | 五字段齐全 append-only；「待验证」高亮；不可删除 |
| A15 | 结构确认后生成原型 | 零外部依赖断网可渲染；P0 流程齐全可点击；页面与模块-页面映射表一一对应；覆盖澄清要点表的核心场景 |
| A15a | 未确认原型即触发 PRD 生成 | 被拦截并引导回原型阶段；原型确认后 PRD 的功能需求与结构映射表及原型页面/流程一一对应，无凭空功能 |
| A16 | 任一主线阶段的产物 | 附自评审摘要（发现的问题/已修正项/遗留问题）+ **漏项雷达四档声明**（四档齐、💀 可为空但须写明最脆弱假设）；修正发生在产出当轮；连续多轮零修正触发自评审失效告警 |
| A16a | 风险台账四态闭环 | 登记的 💀 100% 带「后果」与建议应对方案（说不清后果不允许登记）；状态只在**待处理 / 已挂方案 / 已解除 / 已接受**四态之间流转，**采纳 ≠ 解除**（采纳后为「已挂方案」，验证通过才「已解除」并回写决策日志）；结算发生在阶段确认门（非 AI 逐轮自扫）；活跃条目超 3 条触发收敛动作；封板时全部未处置条目统一结算；右栏「风险」Tab 三分区（待处理 / 已挂方案·等验证 / 已处理·留痕）可见，工程口径只在开发者模式出现 |
| A17 | 澄清确认后进入结构阶段 | 产出功能架构图 + 核心流程图 + 模块-页面映射表（复杂产品另含业务流程图）；Mermaid 源码 .md 落盘、可在文件管理器手改；图与澄清要点一致、无输入外新假设；App 内渲染断网可用；支持三层修改（对话改/App 改源码/改文件） |
| A17a | 未确认结构即触发生成原型 | 被拦截并引导回结构阶段；确认后原型的页面与模块-页面映射表一一对应；结构回退修改重新确认后，原型自动标记「已过期」且分级标注影响范围 |
| A18 | 每个阶段产物完成时 | 停靠卡确认段发起阶段推进询问，**单页直接确认**（选择处理方式 → 页脚按钮随所选选项直接生效）；「确认后会发生什么」由选项副标题以结果导向文案承载（不出现内部路径与架构术语）；**选项按闸口分档**（① 进入结构 / 跳过结构直出原型 / 跳过中间阶段直出 PRD；② 进入原型 / 跳过原型直出 PRD；③ 进入 PRD / 到原型为止），跳步后对应阶段写入跳过标记且不生成产物；不确认不推进；提交后卡收起、对话流内落确认记录胶囊（封版回看可见）；停靠期间输入框可用，用户主动说「进入下一个阶段」等价确认并自动结算卡片；**同一版本同一阶段的坞只主动弹一次**，选「稍后再说 / 继续修改」后转静默（跨启动跨会话不重复弹、不重复留痕），推进改由用户发起 |
| A19 | 新建任务页关联 / 切换项目（含不关联） | 右栏「面板」内容随关联实时切换：关联有产物的项目 → 文件 Tab 显示该项目文件；项目无产物或未关联 → 文件 Tab 空态；新建任务页的决策日志 / 风险恒为空态（空态文案引导，不填充示例数据冒充真实记录）；发送后新任务落盘位置与关联一致（不关联 → 「默认」项目）；新建任务页提供按时间问候 + 四意图建议入口，一键带入起点 |
| A20 | Agent 回复携带思考卡 | 思考中显示「正在思考…」流光 + 阶段时间线 / 技能行 / 工具行；完成后默认折叠为摘要行（思考了 Ns · 技能 · 工具 ×N），有结构化行时可点击展开工具调用行与技能调用行；**卡片任何一态都不得出现模型原始思维链的文字**（折叠摘要行、展开态、流式实时区三处均是判定点，含 2026-09-18~09-22 间落盘的历史消息行）；原文只在 ⌘D 开发者模式可见，且 ⌘D 中确实可回看本轮原文；技能调用行含命中技能名 + 触发词 + 注入内容 + 耗时，与检查器 trace 一致；**多跳链展示阶段时间线**（已完成跳打勾、当前跳流光）；无结构化行可展示时整行不可点、不渲染展开箭头 |
| A21 | PRD 生成前（模板选档）与生成中（数据指标章） | 评分卡在生成 PRD 前展示（三维度各 0-10 + 各一句理由 + 所选档，理由锚定上游已确认产物的具体依据、非空话），用户可一键切换三档任一且切换后按新模板生成；「数据指标」章走五步对话式定义，**数字 100% 来自用户输入（对话记录可追溯），Agent 建议或编造数值即失败**；无基线指标标「实施期建立基线」；第 5 步口径落机读记录（每条量化指标一条口径，`confirmed` 须来自用户确认、Agent 起草的一律 `assumed` 并带假设说明，记录内出现目标值 / 基线值即失败）；正文口径位置只写 `[[SPEC:指标名]]` 并由记录渲染，**落盘正文残留 `[[SPEC:` 即失败**；跳过项按待定问题兜底、不阻塞生成；模板集 100% 走确定性路由取最新版（0 次召回历史版本，与 A3 同口径） |
| A22 | 阶段开始（主动推荐）+ 方法论被实际使用 | 推荐 1-3 个含**理由**（引用项目特征或卡片注记，非空话），可拒绝且**同阶段不再重复推荐被拒项**；推荐在左栏知识库整页以聚光灯呈现（一次主推一张，可采纳/可跳过，处置完翻下一张并落结算句），左栏导航带待处置数字胶囊；推荐/采纳/拒绝在检查器（开发者模式）可追溯；方法论被使用后该卡「实战注记」区新增一条（**只增不覆盖**，带项目出处与日期，回读可验），AI 另代写一条具体案例草稿（失败静默、不阻塞）；使用方法论时该用户的历史使用倾向被一并注入（trace 可证，假设态标签） |
| A23 | 澄清阶段说「别问了，直接出 PRD」 | 不继续提问、不出问题卡、不生成当前阶段产物，直接受理跳步并直达 PRD；被跳过的阶段写入跳过标记且**不生成产物**（阶段位置按闭环链重推导）；目标产物仍走模板选档 + 评分卡 + 确认坞；缺失信息按合理假设补齐并**在产物中标注为假设**；跳过阶段保留补做入口；只说「生成 PRD」而无跳步措辞时不出跳步，改走前置确认分诊（未决项 1-3 → 前置确认卡，选完自动出稿；≥4 → 照常回答 + 末尾待确定清单） |
| A24 | 流程中途说「再加个消息通知」 | Agent 不直接改产物、不静默并入本阶段产物，先给分诊意见再出变更提案卡；卡片含分类 / 建议回退目标 / **影响清单（具体到产物文件与部位）**；**建议回退但列不出具体影响即失败**（应改走本阶段正常修订）；新功能类诉求默认建议进候选池，用户明确说「这版必须有」才建议立即回退（且先回澄清判断）；用户可裁决纳入当前版本 / 进池 / 继续讨论，裁决 append-only 留痕 |
| A25 | 候选池有未处置提案时点封板 | 封板被拦并引导先去处置；池内提案可逐条毕业（纳入后续版本 / 放弃 / 顺延），处置完封板放行；草稿预演：草稿会话产物只落提案目录、**主线零写入**，「合并入主线」执行对拷 + 推进主线阶段 + 提案毕业 + 决策留痕，放弃草稿后主线产物与阶段状态完全不受影响；当前选中非目标版本时合并按钮禁用并引导切换 |
| A26 | 风险台账点「采纳方案」 | 采纳后系统代发受理 → AI 生成实施执行包（可预览、可注入下一轮）→ 流完成后状态转「已挂方案」并落带证据指针的决策日志；中途中断保持「待处理」可重试；降级为「仅记账」时必须显式告知用户（不许无声降级）；同版本排队期间提供「不等了，只记账」逃生门并钉住排队坐标；操作失败落可见反馈行（不许无声） |
| A27 | 磁盘上的原型被外部改写后再生成原型 | 检出内容指纹不一致 → 新原型降级落到「原型-修订-月日-时分」修订槽，**主槽位不被覆盖**，并落用户可见说明行；同分钟多次生成自动递增序号防覆盖；可在「文件」Tab 把任一修订版「设为主原型」（版本忙时拦截并提示）；决策档案：点「生成」产出按天 HTML + 版本总览，内容未变不重写、内容变了全量重建，删掉档案后可从记录完整重建 |
| A28 | 封板时看本版复盘 | 封板第一段确认弹窗给一行摘要（变更处置 + 回退次数与集中阶段 + 闸口 outcome）；release-notes 尾部持久化「本版复盘」段，含变更处置分布 / 回退分布 / 闸口确认 outcome 分布 / 方法论使用报告（采纳 N 张、被跳过 M 次、连续被跳过需校准的卡）；数据全部来自磁盘事实聚合，**不经 AI 转述、不虚构**；无复盘内容时不渲染该段 |
| A29 | 同版本两会话，其一正在生成 | 另一会话的发送与风险采纳**不被阻塞**（仅发起会话自身忙与同版本结构闸拦截）；版本级状态写入按版本归属串行、不竞态；生成中切走到别的项目再回来，回复**钉回发起会话**（不串到当前会话）；同版本内后台完成照常触发闸口，跨版本后台完成不推进闸口并落「需回发起会话处理」挂起行；停止生成只停目标会话；并行会话历史投影按会话过滤、互不污染 |
| A30 | 输入框上方的停靠卡与问题卡 | 同一时刻只有**一张**卡，按优先级切换内容段（作答段 → 分支确认段 → 确认段），段切换时卡体不重建；问题卡向导支持多题逐答（单选 / 多选 + 自定义 + 跳过）→ 上一步回改（已答保留）→ 回顾页跳题修改 → 确认提交，提交作为一条用户消息进入对话流；单题选项行不限于澄清阶段；自动命中的分支在执行前由分支确认段请用户裁决，裁决完毕闸口状态自动回补不丢失 |
| A31 | PRD 阶段点回退坞 | 收起态为虚线小胶囊（不占空间），展开可选「重做原型 / 重做结构」，选项副标题写明回退后果；执行即回退 + 自动重新生成，下游产物标记「已过期」；重做区分双模——带上一版修订（只改用户说的部分）与推翻重来（不带上一版、从零重做）；跳过阶段可事后补做（补做时清除跳过标记） |
| A32 | 一次消息触发多跳（判断 → 跳步/回退 → 重做） | 续段顶部渲染值班单交接条（进行态：计时 + 已生成字数；完成态：耗时 + 步数 + 阶段），点击展开链内交接明细时间轴，历史会话重开同样可见；⚡快速通道受理 / ⏳跨门风险提醒 / ✅阶段推进三类系统行**不再单独渲染**，其语义由 AI 当轮回答的**开场承接句**用大白话交代；系统行仍留痕并作为链标记；无开场承接注入时 AI 不得自行播报状态变化 |
| A33 | 记忆治理与知识卡富化 | 记忆注入按**项目 > 全局**排序，版本只作溯源标签；约束与否决项（硬边界）在预算裁剪时**永不丢**，假设类经验最先裁；失效条目不物理删除、留碑文可回链追溯；「提升为全局」是两池之间唯一流通通道且需用户显式操作；新沉淀的知识卡正文为三段结构（定义 / 做法 / 适用边界）+「为什么有效」原理段；AI 回答底部「参考知识卡」引用行可点开卡片全文 |
| A34 | 长回答撞输出长度上限 | 自动续写并**合并为一条回复**（不出现半截回复或重复段落）；续写失败给明确截断提示与「重新发送」入口，点击能找回原始消息；同一请求重复发送时命中缓存快速返回、不重排 |
| A35 | 模型端限流 / 超时 / 流挂死 | 按退避策略自动重试并以人话向用户说明（不暴露原始错误码）；流式连接有总时长上限保护，不会无限等待；生成中可随时停止，停止只影响目标会话且不残留错误行 |
| A36 | 长文档生成过程的界面吞吐 | 输出正文阶段与思考阶段的界面消费速率不随文本长度衰减（渲染量常数化）；不出现「模型已输出完但界面仍缓慢显示」的状态；展开态思考流只渲染尾部窗口 |
| A37 | 采纳引用了具体产物的变更提案（纳入当前版本） | 采纳即执行**重规划循环**：回退确认行内给出**确定性重生成链**（如 ② 回退 → 功能架构图 → 核心流程图 → 模块-页面映射表 → 交互原型 → 产品需求文档；推翻重做注明「推翻后依次重做」），文案只含产物中文名、零内部路径；计划与状态机过期传播一致（② 回退连带原型 + PRD、③ 回退仅 PRD，回归测试钉住），且折叠进回退行、不新发独立系统行（不切断快速通道链游走）；提案的 ②/③ 回退建议若引用不到任何具体产物（含模糊文本）则不采信回退目标（提案卡降级为仅登记，① 增补澄清豁免）；补做跳过阶段不产计划；任一产物变更可由依赖图确定性推导下游集合（原型具体槽位文件归并到原型家族） |
| A38 | 阶段确认门前的风险建议陈列 | 确认坞出现时，本轮**待处理**风险的应对建议逐条陈列在确认坞上方（建议含依据与代价一句话），每条可 采纳 /「不等了，只记账」/ 暂不处理；采纳走 FR6 落实闭环并落决策日志；无待处理风险时不渲染该区（不占位骚扰）；「暂不处理」保持待处理态、不静默转已接受 |
| A39 | 产物 / 风险的决策回溯 | 任意产物卡与风险卡有「回溯决策」入口，点击直达当时依据的决策五要素（多决策时按影响关系聚合展示）；回退 / 重做执行后，被跨越阶段的历史决策自动注入重做上下文（重做轮的产出引用历史决策而非从零假设）；新决策显式标注「沿用 / 推翻」，推翻后旧决策条目转为碑文态且可从新决策回链跳转；决策档案删除后可从记录确定性重建，回溯链不断 |
| A40 | ②③④ 生成 / 修订产物的回合（FR16） | 回复最开头出现「本轮任务计划」卡（任务目标 + 编号步骤，步骤具体可验收、常规 3-6 步 / 修订 1-3 步），先于正文与产物渲染、流式与最终消息同位；产物与计划步骤对应、无计划外凭空产出；歧义反问 / 变更提案 / 快速通道 / 纯讨论轮不出计划卡，① 澄清恒不出；计划缺失或不合法时回复照常解析落盘（计划是增强不是依赖） |
| A41 | 自评审新登记风险（FR17） | Agent 主动追加一条简报回合：逐条点评（后果 + 建议处理方式）、指出最该先处理的一条、结尾引导右栏「风险」面板，全程 ≤6 句；无新增登记不触发；版本封板 / 有生成在途 / 快速通道进行中 / 落实在途任一命中即静默放弃（不排队不重试）；简报不输出任何产物协议块 |
| A42 | 候选池有未处置提案时点封板（FR17） | 封板被拦的同时 Agent 主动发起催办回合：逐条给明确处置建议（纳入后续版本 / 放弃 / 顺延 + 一句理由，不和稀泥）、引导左栏「决策日志」裁决，全程 ≤8 句；同一池内容不重复催办，池内容变化后再拦可再催；简报不输出任何产物协议块 |
| A43 | 在任意阶段发一句纯问候或纯致谢（FR18） | 秒级返回一两句短答；本轮**不出现**漏项雷达声明、任务计划卡、任何产物块、工具调用行；不写入版本目录任何文件、不推进阶段、不弹确认坞；用量照常计入成本面板。**反向判据同条必测**：「你好，帮我看下原型」「你好吗」「好的」「继续」、带图片或引用文件的「你好」——一律仍走常规阶段轮（误短路的后果是吞需求，比慢更严重） |

---

## 10. 数据与埋点

**原则**：本地工具，无云端埋点；所有指标从本地运行日志 / 统计面板采集，用户可见。

| 指标 | 定义 | 用途 |
|---|---|---|
| 自评审修正数 | 每阶段每轮自评发现并修正的问题数 | 为 0（连续多轮）说明自评审失效，需优化自检清单 |
| 💀 风险命中率 | 登记的风险被真实事件触发结算的比例 | 长期为 0 需人工抽查分辨：确实没有致命假设，还是红队放水 |
| 风险处置分布 | 待处理 / 已挂方案 / 已解除 / 已接受 四态的存量与流转耗时（登记到处置的中位时长） | 验证「采纳 ≠ 解除」是否被真实执行；「已挂方案」长期不解除说明验证环节空转 |
| 快速通道使用率 | 跳步触发次数 + 跳步阶段分布（跳过结构 / 跳过原型 / 到原型为止）+ 跳过阶段的补做率 | 补做率高说明跳早了；使用率极低说明用户不知道能跳，或入口不好找 |
| 变更处置分布 | 变更提案的裁决分布（纳入当前版本 / 进候选池 / 放弃 / 顺延）+ 池内毕业率 + 草稿预演使用率与合并率 | 池内长期积压说明收敛纪律未被使用；草稿合并率低说明预演没起到「先试再定」的作用 |
| 决策档案生成次数 | 档案生成的触发次数 + 幂等跳过率（内容未变未重写） | 幂等跳过率接近 100% 说明档案与记录严格一致，机制健康 |
| 系统行静默率 | 被标记静默的系统行占全部系统行的比例 + 开场承接句出现率 | 静默行语义必须 100% 被开场承接覆盖——覆盖缺失即用户看不到状态变化 |
| 并发冲突数 | 写冲突检出次数 / 原型修订槽生成次数 / 后台完成挂起行数 | 冲突数长期为 0 说明用户没手改文件；挂起行长期不处理说明跨版本后台完成的收尾体验有缺口 |
| 独立评审触发次数 | 可选分支毒舌评审 / review_doc 被调用的次数 | 验证独立第二意见的真实需求量 |
| 沉淀量 | 每项目方法论卡数 / 决策数 / 记忆覆盖次数 / 实战注记新增数 | 验证飞轮是否转起来 |
| 技能命中率 | 各技能被命中加载的次数（含自动触发与手动选用） | 决定 when_to_use 描述质量是否需优化 |
| 思考卡展开率 | 携带思考卡的消息中被用户展开的比例 | 折叠默认是否正确——长期为 0 说明摘要行已够用或卡内容无价值，需优化步骤摘要质量 |
| 分支触发方式分布 | 竞品分析等分支：手动选用 vs 对话自动触发的占比 | 验证意图识别是否可靠、入口是否好找 |
| token 成本 | 每阶段每次调用 token 与折算费用 | 成本面板 + 超限预警 |
| 断网渲染成功率 | 原型 / 结构图断网打开的成功率 | 验证零依赖承诺 |
| 阶段推进确认分布 | 每阶段确认坞的选项分布（确认并进入 / 继续修改 / 稍后再说）、卡上停留时长、自由作答等价结算占比（自然语言推进指令 vs 点选提交） | 验证确认坞是否拖慢用户；自然语言等价路径是否被真实使用；结构图粒度自适应是否够用 |
| PRD 模板选择分布 | 自动选档的三档占比 + 用户手动切换率（及切换方向：自动选的哪档被切成哪档） | 用户切换率高说明三维度评分校准不佳（阈值或评分依据需调整），是模板分级机制的核心质量信号 |
| 主动推荐采纳率 | 各阶段推荐的方法论被采纳的比例 + 拒绝原因分布 + 被拒项同阶段重复推荐次数（应为 0） | 采纳率长期过低说明推荐理由与项目特征关联弱需优化；重复推销 >0 是骚扰 bug |
| 实战注记回读率 | 下次使用同一方法论时，AI 回答实际引用该卡注记的比例（非注记条数） | 盯回读而非记数——防止凑数式注记（Goodhart 防护），验证「注记才是资产」是否成立 |
| Agent 主动回合 | 主动简报触发次数与类型分布（风险简报 / 变更池催办）+ 触发后用户的实际处置率 + 计划卡出现率（应出未出为协议失效信号） | 验证主动性真实价值——触发频繁但无处置动作说明简报噪声化需收敛；处置率高说明主动开口在推动闭环 |
| 事实入库分布 | 导入的外部事实条数（按类型：上线数据 / 用户纪要 / 行业报告 / 问卷 / 竞品资料）+ 出处标签齐备率 + 时间戳跨度 | 北极星 leading 指标。**按类型分布失衡是信号**：只有「上线数据」没有「用户纪要」，说明复盘只看数字不看人；反之说明没有真数据 |
| 口径待确认数 | 指标口径卡里 `assumed` + `pending` 两态的条数（盯反面，不设「口径齐备率」正面指标） | **C 环可用性的先行指标**：长期不为 0 说明口径一直靠 Agent 起草、用户从未确认，届时对账结论全是假账；随默认项卡使用率下降才健康 |
| 「待验证」跨版闭环率 | 上版决策「待验证」条目总数（分母）→ 新版开工时被对账的条数（分子）+ 对账结论分布（已验证 / 已推翻 / 明示仍待验证） | **直接测「闭环是否真的闭上」，且分母现在就存在**——分子恒为 0 即第二圈未通；「明示仍待验证」占比过高说明对账走过场 |
| 复盘信号构成 | 本版复盘段中「过程信号条数 / 结果信号条数」比值 | **比值恒为 ∞（纯过程信号）说明第二圈只是第一圈的重跑**——当前基线即如此（复盘只有变更处置 / 回退 / 闸口 / 方法论四类过程信号），M6 应把它拉下来 |
| 开工档案袋来源分布 | 新版第一轮注入素材按来源计数与占比（上版导入事实 / 对账结论 / 顺延提案 / 未闭环待验证） | **档案袋非空但全部来自上版决策记录（零外部事实）= 闭环假通**——这条盯的是"形式上闭上、实质上空转" |

---

## 11. 第二圈路线（上线事实 → 复盘 → 需求池 → 下一版）

**定位**：第一圈解决「把一个需求想清楚、写到可交付」；第二圈解决「上线后到底对不对、下一版做什么」。**第二圈不是再造一个产品，而是把第一圈已经留下的六处结构化痕迹接起来**（见 §11.2）——所以它的施工量与各环的「新造」程度不成比例：多数环节是接线，不是造轮子。

### 11.1 主线四环（严格依赖顺序，不可并倒）

| 环 | 能力 | 产出物 | 落点 | 前置 |
|---|---|---|---|---|
| **A · 事实入口** | 导入外部事实（上线数据 / 用户纪要 / 行业报告 / 问卷导出），逐条带来源标签与时间戳，进检索 | 带出处的事实档案 | 证据目录（新增） | 无——**唯一硬前置** |
| **C · 结果对账** | 把上版 💀 风险、决策「待验证」、北极星目标值逐条对上线事实，产出「当时预期 ↦ 实际发生」 | 本版复盘报告（含根因与规则回写建议） | 报告目录 | A |
| **D · 项目级需求池** | 收三个来源：变更池「顺延」裁决 + 对账产出的未达标项 + 上版未闭环「待验证」 | 需求池（**项目级容器，非版本级**） | 项目层（新增） | C |
| **E · 开工档案袋** | 新版本创建时把 A/C/D 的产出注入第一轮上下文，并产出下一版澄清要点表初稿（逐条标来源，用户只改不写） | 开工档案袋 + 下版澄清初稿 | 新版本容器 | C、D |

**为什么 A 必须先做、且排序功能不能先做**：优先级与方案评估能力**已经具备**（KANO / RICE / OST 三个技能 + ① 阶段的「开工前对齐门禁」——含分级阻断、隐性假设、敏感度映射、防仪式化验收）。缺的不是机制，是喂给它的数字：优先级的 Reach 与 Impact 当前没有任何数据来源。先做排序界面等于做一个让用户填脑补数字的表单，与「数字 100% 来自用户、Agent 不编造」的既有纪律直接冲突。**排序功能的产出质量上限等于事实的质量。**

**第二圈与第一圈不是同一种工作**：第一圈是**生成**（输入模糊、输出结构化产物，prompt 工程可到位）；第二圈是**判断**（输入必须是事实、输出是取舍，没有事实就是编）。因此第二圈**不做成「再走一遍四阶段」**——那会把生成的仪式套在判断上。它的原生形态见 §11.2 第一条。

### 11.2 六处咬合点（第二圈复用第一圈的什么）

| 现成机制 | 在第二圈里的角色 |
|---|---|
| **风险命中回写条目**（预测 vs 实际的对照式回写，「当时担心 ↦ 实际发生」） | **第二圈的原生形态样板**——机制已在，但当前只能被状态机事件触发；把它扩到被导入事实触发，对账的最小形态就成立了 |
| **决策五要素之「待验证」**（未闭环时 UI 高亮） | 下版需求的一半答案藏在这里；它天然就是对账的分母，不需要新造字段 |
| **版本继承链**（开新版本继承项目级约束与否决项） | 开工档案袋的载体已就位，只缺注入内容 |
| **报告目录**（版本容器第七个阶段目录，现仅存发布说明） | 复盘报告与数据报告的落点已预留 |
| **变更池「顺延 / 纳入后续版本」裁决** | 上版主动放弃的需求——池化的现成来源，目前只落一条决策记录、下版不会自动捞出 |
| **封板复盘聚合**（纯磁盘事实聚合、不经模型转述） | 复盘的聚合框架与「不虚构」纪律已在，缺的只是结果信号这一类输入 |
| **指标口径卡**（`04-prd/metric-specs.jsonl`，v1.3.7 起） | **C 环的准入门与匹配键**——导入的上线数字先按口径卡的分子/分母/时间窗认领到 4.2 表的哪条指标，认不上即标「不可对账」；`assumed` / `pending` 态的指标不参与对账结论，只出现在「待补口径」清单里。**没有它，C 环只会产出「数字不一样」这一种结论** |

### 11.3 旁支（不承载闭环，按需插拔）

| 能力 | 说明 | 与本路线的关系 |
|---|---|---|
| 数据计算 | CSV 上传 + 可复算报告（数字与结论分离、标注不确定性） | **可延后**：C 环的降级版（数字由用户算好导入、产品只做对账与归因）不含计算即可拿到闭环价值。是否自研计算，属学习目标与产品价值的取舍，见 §12 |
| 原型编辑画布 / 外部工具同步 | Figma 式精细编辑不做；第三方工具作可选导出通道 | 与闭环无关 |
| 技能「学」模式 | 同一份技能正文生成讲解 + 练习 + 复述 | 与闭环无关 |
| 导出通道（飞书 / Notion） | — | **紧迫性已被 MCP 部分抵消**：同一条流水线已可被外部客户端调用并读取产物，导出从「缺口」降为「锦上添花」 |
| 产物足迹 | 产物在后续阶段被引用的链路视图 | 依赖图已提供底层数据，属展示层增强 |

### 11.4 已提前落地项

**模型自主工具调用（Function Calling）**——原列为 V2 路线首项，v1.3.3 起三工具、v1.3.5 扩至四工具已落地：技能按需加载 / 联网搜索 / 竞品分析提议 / 产物依赖查询（编制变更影响清单前先核实影响面）。确认闸口与产物不变量不破（工具只读或走既有分支通道，不绕闸）。总开关默认开。

---

## 12. 开放问题（待产品负责人决策）

1. **技能库初始内容边界**：哪些方法论进首批技能库（阻塞 M4）
2. **搜索源供应商**：调研默认搜索源选哪家
3. **单次全流程 token 上限**的默认值
4. **产品名**：暂用 PM Copilot，待定
5. **签名与公证**：是否购买开发者账号以提供免拦截构建包
6. **快速通道跳过的阶段与 MCP 前置校验的交互**：MCP 按主线顺序校验前置（如生成 PRD 需已确认原型），而跳步会让某阶段没有产物——被跳过的阶段应视为「已闭环放行」，还是仍要求补做后才允许 MCP 调用（待定）
7. **知识卡结构口径统一**：抽取侧强制「定义 / 做法 / 适用边界 + 为什么有效」，展示侧另有「是什么 / 为什么现在 / 怎么用 / 案例」四段口径——两者需收敛为一套，避免同一张卡在不同入口长得不一样
8. **第二圈是否自研数据计算**（v1.3.6 新增，见 §11.3）：C 环可用「用户算好数字导入、产品只做对账与归因」的降级版拿到全部闭环价值；自研计算（可复算报告）则额外覆盖一项技术能力但不增加闭环价值。**这是学习目标与产品价值的冲突，只有产品负责人能定**——早期技术方案已判过「接真实埋点系统投入产出比最差」，本条问的是不接外部系统、纯本地计算的取舍
9. **「全流程」词义是否对外保留**（v1.3.6 新增）：本文档已在 §4.2.4 把词义钉死为「双圈闭环」并加了术语纪律（描述单次流水线运行不再用「全流程」）。**待定的是对外口号**：README 首行仍以「产品经理全流程 Agent」开头且紧跟四阶段副标题，读者会先被拔高再被收窄。保留（配 §4.2.4 定义句）还是改窄，属对外表述决策

---

## 附录：与 design.md 的对应关系

| 本文档章节 | design.md 来源 |
|---|---|
| §2 背景 | design.md §1 |
| §3-§5 用户/目标/范围 | design.md §0、§2 |
| §6 旅程 | design.md §0、§6.1 |
| FR1-FR6 四阶段主线（含 FR2a 结构设计）、阶段推进确认、内建自评审与版本 | design.md §5、§6、§9（剥离实现；FR3 由固定阶段改为技能/插件分支、FR6 由末端毒舌评审改为 Agent 内建自评审，为对原方案的修订；FR2a 结构设计与阶段推进确认为 v1.1 新增，对应 design.md v0.8；FR6 升级为「自检清单 + 漏项雷达四档 + 决策 WHY 记录 + 风险登记」三层闭环为 v1.2 新增，对应 design.md v0.9；FR1 第 10 条新建任务页项目关联为 v1.2.4 新增，对应 design.md v0.9.4；FR6 风险四态台账与采纳落实闭环、FR2a 统一停靠卡/分档路径/弹坞纪律、FR4 原型冲突分槽与回退坞为 v1.3.0 新增，对应 design.md v0.10） |
| FR7-FR9 横切层产品语义 | design.md §6.3、§6.4（剥离架构；三层语义划分/实战注记/主动推荐/记忆校准为 v1.2.9 新增，对应 design.md v0.9.10；决策档案与记忆治理为 v1.3.0 新增，对应 design.md v0.10） |
| FR10 本地优先 | design.md §3、§5.1 |
| FR11 MCP | design.md §7 |
| FR13 快速通道 / FR14 变更治理 / FR15 多会话并行与后台完成 | design.md §6.6、§6.7、§6.8（v1.3.0 新增，对应 design.md v0.10） |
| FR12+§11 工具调用（Function Calling 四工具，含 query_impact 依赖查询） | design.md §6.11（v1.3.3 新增对应 v0.10.1；四工具扩展对应 v0.10.2） |
| FR16 本轮任务计划（plan-act-reflect）/ FR17 Agent 主动简报 | design.md §6.12、§6.13（v1.3.5 新增，对应 design.md v0.10.2） |
| FR7 决策档案 / FR1+FR15 封板复盘 | design.md §6.9、§5.4（v1.3.0 新增，对应 design.md v0.10） |
| §8 非功能 | design.md §8 |
| §9 验收 | design.md §10（编号 E→A） |
| §11 第二圈路线 | design.md §14 |

**已剥离（属技术方案，不在本 PRD）**：五层架构图、技术选型表、数据 schema 与目录规范细节、状态机与 Prompt 设计、里程碑排期、风险对策、决策 WHY 记录、面试能力映射。
